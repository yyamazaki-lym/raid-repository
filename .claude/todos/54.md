# TODO #54 — Vercel デプロイ後の遷移ロード再発

## 報告 (2026-04-30)

Vercel デプロイ後、動画ページ / コンテンツカテゴリページへの遷移がロードで止まる事象が再発。何度かリロード or ページ移動繰り返しで表示。読み込み / ネット回線 / 通信量増加も原因か要検証。

## part1 完了 (2026-04-30)

**真因切り分け**: 仮説 2 (chunk hash mismatch) は `ChunkErrorHandler` + `deploymentId` で二重防御済のため非該当。実体は **仮説 1 (cold start) + `loading.tsx` 不在による無音 stuck UX 問題**。

**打ち手**: `next-nprogress-bar` 採用、新規 client wrapper `TopProgressBar` を portal layout の `ChunkErrorHandler` 直後に配置 (height=2px / color=#06b6d4 / showSpinner=false / shallowRouting)。`usePathname` 変化を listen し遷移開始-完了の間だけバー描画 → UX 主問題 (無音 stuck) は解消。

**過去対策の現存確認** (Explore agent の誤報告に注意):
- `ChunkErrorHandler` ([src/components/portal/chunk-error-handler.tsx](../../src/components/portal/chunk-error-handler.tsx))
- `next.config.ts:127` の `deploymentId = VERCEL_GIT_COMMIT_SHA?.slice(0, 12)`

chunk hash mismatch 系の二重防御は健在。仮説 2 を再検討する場合はまず両者がコードに存在することを必ず確認すること。

## part1 本番検証結果 (2026-04-30)

ユーザー報告:
- **上端 cyan バーは production で見えない** ← 実装側不具合。dev preview では起動エラー無し / login 画面まで render OK だったが本番未検証のまま push。
- **待ち時間は依然長い**、ある程度経つと遷移する
- **Supabase region: North Asia** (= Vercel hnd1 Tokyo と一致、地理的不整合なし)
- **Function Invocations**: Success 43,578 / Timeout 7 (0.0%) → 通信失敗ではなく純粋な init 時間

仮説 1-(a) リージョン整合は OK が確定。残るは Pooler / middleware / Edge cold init の組合せ。

## part2 候補 (cold start 自体の短縮 + progress bar 修正)

**(0) progress bar 不可視の修正 (最優先)**:
- production DevTools で `#nprogress` DOM が body に挿入されているか確認
- console error / CSP violation の有無 (CSP `style-src 'self' 'unsafe-inline'` だが progress bar のインライン style が弾かれている可能性)
- 不具合修正できなければ自前実装 (~30 行、`usePathname` + framer-motion) に置換

**(a)** ~~Supabase Dashboard で project リージョン確認、Tokyo (hnd1) と一致するか~~ → **確認済 (North Asia = Tokyo 整合)**

**(b)** Supabase Pooler (Transaction mode) URL への切替検討。Edge 環境 + cold start での DB 初接続コストを Pooler 経由にして抑制

**(c)** `proxy.ts` の `updateSession()` で毎リクエスト `getUser()` してる重さの軽量化余地

**(d)** `<Link prefetch={false}>` で cold start 中の不可視 RSC ロードを抑制する逆張り

## part2 試行と全 revert 撤退 (2026-05-01)

**経緯**: 35991e8 (top progress bar 自前実装置換 + `<PortalLink>` ラッパー導入) 以降、Vercel build container が `npm install` 完了後 `next build` 進入直後に無音 SIGKILL で連続失敗 (cache cleared redeploy + 同 commit 2-3 回試行で同位置再死亡)。Build Logs は `added 648 packages in 15s` で切断、Vercel 側は generic "transient issue" message のみで真因特定不能。ローカル `next build` は 6.2s で完走するためコード自体は正常 — Vercel infra 側 native binary crash / OOM 起因と推定。本番が 5 commit ぶん反映されない状態で、ユーザが「左上色変化なし / 設定更新履歴が part1 のまま」で気付いて発覚。

**撤退**: aae6891 で 5 commit (`35991e8` / `9a6f14d` / `881ebda` / `8fbbb87` / `6833c8b`) を 1 commit に統合 revert、ad4b29a (part1) state へ復帰。本番安定化済。

**次回再投入予定** (build-safe な単独変更):
- **part2-c**: `proxy.ts` の `getUser()` → `getClaims()` 化 (JWKS local 検証で /auth/v1/user 往復削減、本番計測で 0 件達成済の実績あり)。`<PortalLink>` 非依存。
- **part2-d**: リスト系 4 箇所 (category-list.tsx 2 箇所 / schedule-list.tsx / schedule-past-simple.tsx) の素 `<Link>` に直接 `prefetch={false}`。`<PortalLink>` ラッパー経由しない実装。

**保留**: top progress bar 自前実装 (35991e8 本体 = `<PortalLink>` + `<NavReporter>` + `navigation-pending-store` + `<TopProgressBar>` の 4 ファイル機構) は Vercel build SIGKILL の真因特定後に別アプローチで再着手。当面 `next-nprogress-bar` 採用版 (本番不可視の既知問題あり) のまま。

## 仮説 1 (有力): コールドスタート + Supabase DB 接続遅延

参考記事 https://note.com/light_systems/n/n8431aa59d7f7 (Vercel + Neon の cold start で「DB 接続だけで 2-3 秒」「朝イチアクセスが 3-5 秒」)。Raid Repo は Supabase だが同様の現象が起きうる。記事の主因は「Function リージョン と DB リージョンの不一致 → 太平洋横断で RTT 数百ms × PostgreSQL TCP 8 RT = 累積遅延」。

**制約**: 全ポータルページ Edge runtime は FFLogs scrape (Cloudflare bot 判定を Edge IP ですり抜け) のため固定。Node Lambda 化は FFLogs 系ルートで 403 を誘発する。動画/カテゴリページが FFLogs に触れていないなら個別に Node runtime + リージョン固定への切替検討余地あり。

## 仮説 2: deploy 後 chunk hash mismatch / RSC payload skew (part1 で非該当判定)

1.9.x で `<Link>` 復活 + `ChunkErrorHandler` + Next.js `deploymentId` skew protection を入れた経緯あり。両者現存し機能している前提で part1 では棚上げ。再発経路 (RSC stream stuck / Suspense fallback 永久 / Service Worker / browser cache が古い chunk URL を保持 / prefetch 暴走 等) を疑う場合のみ再着手。
