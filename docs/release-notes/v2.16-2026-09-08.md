# v2.16 (2026-09-08)

## 🐛 練習ログ: 拡張をまたいだ絶 (Ultimates (Legacy)) のレポートが取り込めなかったのを修正 — pull の encounter 名で振り分け

**実機報告**: 絶オメガ検証戦の練習ログが 2024-06-18 で止まり、FFLogs 側にある 2024-07 以降のレポート (Dawntrail 期) が取り込まれない。「分類が拡張ごとに違っているのでは」という指摘どおりでした。

**原因**: FFLogs の zone は拡張ごとに切り直され、旧拡張の絶は現行拡張では **"Ultimates (Legacy)" という 1 つの zone** にまとめられます (絶バハ / 絶アルテマ / 絶アレキ / 絶竜詩 / 絶オメガがすべて同じ zone)。レポートのタイトルも「絶シリーズ（過去）」のように何の絶か書かれません。portal のカテゴリ判定は **レポート単位** の zone 名 / タイトルしか見ていなかったため、こうしたレポートは「カテゴリ未確定」のまま、どの練習ログにも出ませんでした。

**修正**: fight (pull) には encounter 名 ("The Omega Protocol" / "Dragonsong's Reprise" など) が付くので、**fight ごとに** encounter 名をコンテンツ分類器に通してカテゴリを決めるようにしました (`src/lib/fflogs-category.ts` に純関数として切り出し)。優先順位は「fight 名で一意に決まるカテゴリ → レポート単位の解決 (zone ID / zone 名 / タイトル) → 動画リンク由来」。同じ夜に 2 種類の絶をやったレポートも、それぞれのカテゴリへ振り分けられます。レポート台帳の代表カテゴリは fight の最多で決めます。零式の fight 名はボス名なので分類器に当たらず、従来どおりレポート単位の判定です。

- **既に取得済みで未確定のレポート**: 次の同期の「オフライン再解決」で、保存済みの fight 名から fight ごとに再分類します (FFLogs は叩きません)。絶オメガの 2024-07 以降がこれで出るはずです。
- 取り込み難易度の下限 (`fflogs_min_difficulty`) も fight ごとのカテゴリで判定します。
- `expected_fflogs_zone_ids` に Legacy zone を複数の絶カテゴリへ設定していても (= レポート単位では曖昧) fight 単位で決まります。

**検証**: `scripts/check-fflogs-category.mjs` (新規、13 ケース: Legacy zone + 汎用タイトルは決められない / fight 名で決まる / 零式ボス名は決まらない / 代表カテゴリ) と `tsc` / `eslint` / `check-changelog`。実機では **次回同期後に絶オメガの 2024-07〜2025-01 のセッションが並ぶこと** を確認してください。出ない場合、そのレポートが portal に未登録 (動画にも日程ログにも URL が無い) の可能性があるので「URL から取り込む」に貼ってください。

## ⏱ 練習ログ: フェーズ滞在時間を「表示中の pull」ではなく登録ログ全件で集計

**実機報告**: 絶竜詩戦争 (1047 pull) の「フェーズ滞在時間」が「合計 … (表示中の分)」で、直近 1000 pull だけの集計になっていた。

**修正**: 明細 (最大 1200 件) とは別に、開始 / 終了 / フェーズ遷移 (jsonb) の軽い列だけを 1000 件ずつ全件読み、サーバー側で合計する `fetchCategoryPhaseTotals` を追加しました (`src/lib/supabase/fflogs-fights.ts`)。絶のページでは明細と並列に取得し、カードは全件の合計を「合計 … (登録ログ全 N pull)」と表示します。N はフェーズ遷移が保存されている pull 数 (= 合計の母数) で、遷移が入っていない古い同期分は含みません。取得に失敗したときは従来どおり表示中の明細から計算します。

pull ごとのフェーズバーと「ワイプ原因」カードは従来どおり表示中の明細に基づきます (ワイプ原因は死亡イベント列を全件読む必要があり別途)。

**検証**: `tsc` / `eslint` / `check-changelog`。Supabase が無いため実描画は未確認で、実機で絶竜詩のカードが「登録ログ全 N pull」表記になり合計が伸びることを確認してください。

## 🔁 練習ログ: 同期結果に「残り N レポート」を表示 / フェーズ滞在時間の母数 (情報のある pull 数) を明示

**実機報告**: 絶竜詩のフェーズ滞在時間が「登録ログ全 123 pull」で、1047 pull あるのに P7 が 12 分しかない。

**背景**: フェーズ遷移と死亡イベントは 2026-09-06 (2.15) 以降に取得した pull にしか入っていません。それより前に同期したレポートは「1 回の同期で最大 40 レポート」ずつ取り直す設計 (FFLogs のポイント消費と 120 秒の時間予算のため) なので、レポート数が多い固定では数回〜十数回の同期が要ります。ところが画面には「※途中まで」としか出ず、あと何回押せばいいのか分かりませんでした。

**変更**:
- 同期の結果に、今回の枠に入らず次回に持ち越したレポート数 (`remaining`) を含め、トーストに「※残り N レポートは次回 — もう一度「ログを同期」を押すと続きを取得します」と出します。0 になれば取り直し完了です (cron でも毎日 40 件ずつ進みます)。
- フェーズ滞在時間カードの括弧書きを、母数が全 pull より少ない間は「フェーズ情報のある N / 全 M pull — 残りは同期で取り直し中」に。全件そろえば従来の「登録ログ全 N pull」。

**検証**: `tsc` / `eslint` / `check-changelog`。

## 🚦 同期: 旧レポートの取り直しを private 再試行より先に処理 / 取得経路 (v2 ・ 代替) の内訳を表示

**実機報告**: 「ログを同期」を押しても、フェーズ情報のある pull が 123 のまま増えない。

**原因の候補は 2 つ**で、どちらも画面から見えなかったので両方に手当てしました。

1. **枠の取り合い**: 手動同期は恒久失敗 (private) のレポートも毎回再試行します。private が多い固定では、この再試行 (ほぼ同じ結果になる) が 1 回 40 件の枠を先に埋め、旧レポートの取り直しに順番が回りませんでした。取得順に優先度を付け、通常 (未同期 / 直近 / URL 指定) → 旧レポートの取り直し → private 再試行、の順で処理します。同じ優先度の中は従来どおり新しい日付から。
2. **取得経路**: フェーズ遷移と死亡イベントは **FFLogs v2 (OAuth) で読めたレポートだけ** に入ります。unlisted / private で v2 が拒否され、v1 API や cookie の代替経路で取れたレポートは fights の骨格 (pull 数・到達度・残 HP%) だけで、取り直しても埋まりません。同期結果のトーストに「経路: v2 N 件・代替 M 件 (代替経路はフェーズ・死亡情報なし)」を出し、フェーズ滞在時間カードの括弧書きにも hover でこの説明を付けました。代替経路になっているレポートを埋めたい場合は、アップロードした本人の FFLogs 連携 (OAuth) か、レポートを Public にする必要があります。

**検証**: `tsc` / `eslint` / `check-changelog`。

## 🌐 表示言語 第 3 段 (1/3): 設定ダイアログの全セクションを日英化

設定ダイアログの中身を辞書 (`src/lib/i18n/dict/settings.ts`、18 セクション・約 460 キー) に移しました。対象: スケジュールソースモード / 同期元 URL / 過去の活動の取り込み・スナップショット / メンバー・出欠記号・中止候補・既定時刻・Discord 通知・自動確定 / 出欠催促 / FFLogs 連携 (v1・OAuth・Cookie・連動実行・診断) / 初期化 (危険操作) / 更新履歴フッター / メンテナンスメニュー / 確認ダイアログの既定ボタン。日本語の文言は変えていません (JSX の改行由来の空白だけ整理)。

**日本語のまま残るもの (意図的)**: Discord ID や `HH:MM`、URL、環境変数名などの例示、Discord Bot が実際に投稿する文の書式 (`本日YYYY/MM/DD(曜) HH:MM~HH:MM`)、既に英語の見出し (Schedule Source / FFLogs Sync など)。Server Action のエラー文と Discord 通知本文はサーバー側なので次段。

**検証**: `tsc` / `eslint` / `check-changelog`。設定ダイアログは Supabase が無いためこの環境では描画できず、実機で English に切り替えて各セクションを確認してください。
## 🌐 表示言語 第 3 段 (2/3): 練習ログ / サブタブ / カテゴリ一覧を日英化

練習ログタブ (`logs-view.tsx`、約 90 箇所)、`/category` のカテゴリ一覧、サブタブ、ステータスバッジ、カテゴリ切替を辞書 (`src/lib/i18n/dict/logs.ts`、8 セクション・約 165 キー) に移しました。日本語の文言は変えていません。

- サブタブのラベルは `src/lib/sub-tab-defs.ts` を唯一の出典にし、`getSubTabDefs(locale)` / `getSubTabLabel(id, locale)` を追加。
- 純関数に `locale` 引数 (既定 ja): `formatWipeLabel` / `wipeCauseCounts` (「不明」→ Unknown)、`buildFloorMap` / `floorLabel` / `pullBreakdown` (「◯層」「前半 / 後半」→ `F3` / `F4a` / `F4b`)、`formatDurationLong` / `formatFirstClear`。層の前半 / 後半判定は表示文字列を見る方式から `FloorMap.halfByIndex` に変え、言語に依存しないようにしました。
- 同期トーストの「残り N レポート」「経路: v2 / 代替」、フェーズ滞在時間カードの母数表記 (本リリースの他項目) も辞書経由。

**日本語のまま残るもの (意図的)**: DB に保存する除外理由 (`誤取り込み`)、カテゴリのステータス値 (DB の enum、表示だけ辞書)、同期失敗理由 (保存データ)、fflogs.com 上で動くブックマークレットの文言。

**検証**: `tsc` / `eslint` / `check-fflogs-fight-detail` (英語ラベル 3 ケース追加) / `check-perf-tone` / `check-changelog`。

## 🌐 表示言語 第 3 段 (3/3): 軽減表 / ロット / 攻略 / 動画 / マクロ / ウェイマーク / コンテンツ編集を日英化

各コンテンツタブとその編集ダイアログを辞書 (`src/lib/i18n/dict/content.ts`、21 セクション・約 490 キー) に移しました。日本語の文言は変えていません。対象: タブページ (loading / loot / mitigation / strategy / videos / macros)、攻略・画像・動画・マクロ・ウェイマークの一覧、コンテンツ編集ダイアログ、軽減表の列設定・層タブ、ロット週次・BiS、画像 / リンクのフォームとメニュー、シート関連 (onboarding / 切替 / iframe / カード)、募集テンプレート本文。純関数 (`week-jst` / `link-site` / `waymark-preset` / `url-validation` / `sheet-csv` の `buildSheetCardRows`) は `locale` 引数 (既定 ja)。

これで **画面の文言は全タブが日英対応** になりました。残りはサーバー側 (Server Action のエラー文、Discord 通知本文) と、DB に保存されている値そのもの (カテゴリのステータス、ロット週次のステータス、シートの技名) です。

**日本語のまま残るもの (意図的)**: 上記の保存値 (表示だけ辞書で置換)、軽減表の列名候補に出すアビリティ名 (シートが日本語のため)、Server Action の `reason`。

**検証**: `tsc` / `eslint src` (既存の警告 1 件のみ) / `check-mitigation-cards` / `check-sheet-icons` / `check-waymark-preset` / `check-xivgear-set` / `check-ability-header` / `check-changelog`。実機で English に切り替えて各タブを確認してください。

## 🏳️ 言語切替をヘッダーへ (国旗アイコン) / 残っていた日本語 (メンテナンス結果・ページ見出し・エラー画面) を日英化

**実機要望**: 言語切替は設定の外、上部で切り替えたい。国旗で直感的に、UI 崩れなく。

- ヘッダー右側、テーマ切替の左に **国旗付きの言語メニュー** を置きました (`locale-switcher.tsx` の `menu` variant)。トリガーはテーマ切替と同じクラス (高さ・枠・余白が揃う) で、国旗 + 言語名 (スマホでは国旗のみ)。開くと日本語 / English を国旗付きで選べます。国旗は絵文字ではなく **小さな inline SVG** (`locale-flag.tsx`) — Windows では絵文字の国旗が「JP」「GB」の文字に化けるため。日本 = 日章旗、English = ユニオンジャック。
- 設定ダイアログの「表示言語」セクションは撤去 (ヘッダーに一本化)。ログイン画面の脚注は国旗付きの 2 択に。

**残っていた日本語の精査**: 文字列リテラルと JSX テキストを AST で走査するスクリプト `scripts/audit-ja-strings.mjs` を追加し (コメントは対象外、辞書 / 祝日 / 分類器 / server 側は除外)、UI に出るものを潰しました: メンテナンスメニューの結果パネル 4 種 (クリア日時 / Discord 取り込み / 動画メタデータ / 攻略サムネ、`settings.ts` に `maintenancePanels` 46 キー)、ページの `<title>` (スケジュール / コンテンツ) と root の description、コンテンツ一覧の説明と DB 接続エラー文、読み込み中の aria、ページエラー画面、スケジュール編集ダイアログ、設定ボタンの aria、並び替え失敗のトースト。

**意図的に日本語のまま**: DB に保存された値 (カテゴリ・ロット週次のステータス、除外理由)、軽減表の列名候補のアビリティ名、候補日ダイアログが DB キーを組む曜日文字、OAuth コールバックのエラー (サーバー)、開発者向け `throw`、client validator の `reason` (サーバー側と同じ扱い、次段)。

**検証**: `tsc` / `eslint` / `check-changelog` / `audit-ja-strings` (残 47 件はすべて上記の意図的なもの)。`/login` を ja / en で描画確認 (国旗付き 2 択)。ヘッダーは Supabase が無いため描画できず、実機で確認をお願いします。
## 🧩 同期: v1 経路 (unlisted) のレポートでもフェーズ遷移を読む / 旧レポートをもう 1 回だけ取り直す

**実機報告**: 同期しても常に同じ 15 レポート / 138 pull しか読まれず、フェーズ情報のある pull が 123 のまま増えない。

**読み**: その 15 本は直近 14 日の取り直し分 (毎回同じ) で、絶竜詩の旧レポートは対象に入っていません。旧レポートは unlisted のため v2 (OAuth) では読めず v1 API 経路で取られており、v1 経路ではフェーズ遷移も死亡イベントも保存していなかった上に、取り直し後は `synced_at` が更新されて対象から外れていました。

**変更**:
- v1 / 内部 JSON の fights にも `phases: [{ id, startTime }]` が付くことがあるので、あれば v2 と同じ形で保存します (無ければ従来どおり列を触りません)。
- 2026-09-07 00:00 UTC より前に同期したレポートをもう 1 回だけ取り直します (優先度は「旧レポートの取り直し」= private 再試行より前、新しいレポートより後)。1 回 40 件ずつ進むので、トーストの「残り N レポート」が 0 になるまで同期を繰り返してください。

**それでも埋まらない場合**: v1 の応答にフェーズが無いレポート (FFLogs 側の仕様) です。トーストの「経路: 代替 M 件」がその本数で、埋めるにはアップロード本人の FFLogs OAuth 連携 (v2 で本人の unlisted / private を読める) が必要です。死亡イベント (ワイプ原因) は v1 経路では引き続き取れません。

**検証**: `tsc` / `eslint` / `check-changelog`。FFLogs 実 API には届かないため、v1 応答の `phases` の有無は実機の次回同期で確認してください。

## 🩺 練習ログ: URL 取り込みは貼ったコンテンツのログとして取り込む / レポート診断と「このコンテンツに割り当て」を追加

**実機報告**: Ultimates (Legacy) の絶オメガ 9 本を URL 取り込みしても、絶オメガの練習ログに出ない。

DB を見られない側からは「取得できていないのか、取得できたが未分類なのか、別コンテンツに付いたのか」を絞れなかったので、3 つを入れました。

1. **URL 取り込みの既定カテゴリ**: 「URL から取り込む」を絶オメガのページで実行したら、動画リンクでも fight 名でもカテゴリが決まらないレポートは **絶オメガのログとして** 取り込みます (貼った人の意図)。fight 名で別コンテンツと分かる pull はそちらへ。これまでは未分類のまま残っていました。
2. **診断**: 取り込みダイアログに「診断」を追加。貼った URL について、台帳 (取得できたか / 失敗理由 / zone 名 / タイトル / 台帳のカテゴリ)、pull の保存先 (このコンテンツ / 別コンテンツ / 未分類の件数)、fight 名ごとの件数と **今の分類器がその名前をどこに落とすか** (difficulty / encounter ID 付き) を表示します。FFLogs は叩きません。
3. **このコンテンツに割り当て**: 診断で「このコンテンツ以外」の pull があれば、ボタン 1 つで貼ったレポートの pull を全部このコンテンツに移せます (台帳も更新)。分類器で決められないレポートの最終手段。

**使い方 (絶オメガの 9 本)**: 絶オメガの練習ログ → URL から取り込む → 9 本を貼る → まず「診断」。台帳に無ければ「取り込む」(今回から絶オメガとして入ります)。取得失敗なら理由が出ます (private なら本人 OAuth か Public 化)。取得済みで未分類 / 別コンテンツなら「割り当て」で移せます。診断に出る fight 名と分類器の判定を教えてもらえれば、分類器側も直します。

**検証**: `tsc` / `eslint` / `check-changelog`。

## 🎯 練習ログ: Legacy 絶の pull を encounter ID とボス名で振り分け (fight 名が「Omega」等のボス名だった)

**診断結果 (実機)**: Ultimates (Legacy) zone の絶オメガ 9 レポートは取得済み・未分類で、fight 名は "Omega" / "Omega / Omega-M"、encounter 1077、difficulty 100。前回の修正は fight 名が encounter 名 ("The Omega Protocol") である前提だったため、ボス名では分類器が「決められない」になっていました。

**変更** (`src/lib/fflogs-category.ts`): fight のカテゴリ解決の優先順位を次にしました。
1. **encounter ID** — 絶の既知 ID 表 (`ULTIMATE_ENCOUNTER_GROUPS`)。1077 = 絶オメガ (実機確認)。Legacy zone は UCOB / UWU / TEA / DSR / TOP の順に連番なので 1073〜1076 をその順に、Endwalker 期の 1060〜1062 / 1065 / 1068 と絶もうひとつの未来 1079 も併記。確度の低い旧拡張 (ShB / SB) の ID は入れていません (診断画面で見える値を基に追加できます)。
2. fight 名の内容分類 (従来)
3. **絶 zone 内のボス名** — "Omega" → 絶オメガ、"Thordan / Nidhogg" → 絶竜詩、"Twintania / Nael / Bahamut" → 絶バハ、"Garuda / Ifrit / Titan / Ultima Weapon" → 絶アルテマ、"Living Liquid / Brute Justice / Alexander" → 絶アレキ、"Fatebreaker / Usurper / Oracle / Pandora" → 絶もうひとつの未来。零式や討滅にも同名のボスがいるので、**zone 名が絶のときだけ** 使います。
4. レポート単位 / 動画リンク / 取り込み元

同期時の振り分け、オフライン再解決 (取得済み未分類レポートを次回同期で自動再分類)、診断画面の「分類器」表示の 3 箇所に適用。**取り込み済みの 9 本は次の「ログを同期」で絶オメガに付きます** (割り当てボタンを押す必要はありません)。

**検証**: `scripts/check-fflogs-category.mjs` に 6 ケース追加 (実機診断と同じ形の encounter 1077 + "Omega"、ID 不明時の絶 zone ボス名、零式 zone の "Omega" は決めない、ID がカテゴリ重複なら名前へ)。`tsc` / `eslint` / `check-changelog`。

## 🖼 動画登録: Google フォトのリンクに対応 (専用アイコン / 動画として分類)

**実機要望**: 動画登録に Google フォト対応もしたい。

Google フォトの共有リンクを「Web」ではなく **Google フォト** として認識するようにしました (`photos.app.goo.gl` の短縮リンク、`photos.google.com/share/...` のアルバムリンク、`photos.googleusercontent.com` の実体 URL)。

- 動画カードのサムネイル位置に、Google フォトの風車マーク (ブランド 4 色のインライン SVG) と「Google フォト」/「Google Photos」のラベルが出ます。クリックで新しいタブに開きます (Google フォトは外部から埋め込み再生・サムネイル取得ができないため、YouTube のようなインライン再生は行いません)。
- 攻略リンク側の粗い分類 (`coarseSite`) では **動画** 扱いになります。
- Google ドライブ / ドキュメント / 検索など、Google の他サービスは巻き込みません (`scripts/check-link-site.mjs` で固定)。

**検証**: `scripts/check-link-site.mjs` (新規、11 ケース) / `tsc` / `eslint` / `check-changelog`。
## 🩺 練習ログ: レポート診断に到達度の保存状況 (クリア数 / 残 HP%・フェーズの未取得数 / 生値 / 日付) を追加

**実機報告**: あるレポートが取り込まれて動画にも紐づき、pull 数にも入っていそうなのに、到達度として judged されていないように見える。

到達度 (残 HP% / フェーズ) が出ない理由は「保存されていない」「保存されているが単位が違う」「別の日付に入っている」のどれかで、画面からは区別できませんでした。取り込みダイアログの「診断」に、レポートごとの内訳を足しました。

- **クリア数**: `kill = true` の pull 数 (クリアは残 HP% ではなく CLEAR 表記になる)。
- **残 HP% 未取得 / フェーズ未取得**: `fight_percentage` / `last_phase` が null の pull 数。unlisted で v1 経路になったレポートは、FFLogs の応答にこれらが含まれないことがあります。
- **残 HP% の生値の範囲**: DB に入っている値をそのまま表示します。FFLogs は 100 倍整数 (6000 = 60%) で返す経路と実数で返す経路があり、読み出し側は「100 より大きければ 100 で割る」という推定で吸収しています。生値を見れば、その推定が効いているか (1% 未満の pull が 100 倍のまま表示されていないか) が分かります。
- **日付**: その pull 群の `session_date` (最大 5 件)。日ごとの到達度グラフのどこに入っているかが分かります。

**検証**: `tsc` / `eslint` / `check-changelog`。

## 🧹 練習ログ: 1 レポートに複数コンテンツが混ざると全 pull が片方に入っていたのを修正 (zone で振り分け / 再分類ボタン)

**実機報告と診断結果**: あるレポート (`6W8nkQV2BAtJqa3T`) が「取り込めて動画にも紐づき pull 数にも入っているのに到達度として判定されていないのでは」。診断してみると、到達度は正しく保存されていました (残 HP% 未取得 0 / フェーズ未取得 0 / 生値 0.01〜100 で単位も正常)。見つかったのは別の問題です。

```
zone: Asphodelos / 台帳のカテゴリ: 絶竜詩戦争
pull 63 件 — このコンテンツ(絶竜詩) 63 / 別コンテンツ 0 / 未分類 0
Ser Adelphel / Ser Grinnaux / Ser Charibert × 50 (encounter 1065) → 絶竜詩
Erichthonios / Hippokampos / Phoinix / Hesperos × 13 (encounter 78-81) → 決められない
```

**1 つのレポートに零式 (万魔殿:辺獄編) と絶竜詩が同居**していて、**零式の 13 pull まで絶竜詩戦争に入っていました**。絶竜詩の総 pull 数・練習日数・クリア数 (この 13 pull に含まれる 5 クリア) が水増しされ、到達度グラフにも 2022-04-30 の零式の日が混ざります。

**原因**: fight ごとのカテゴリ判定は「encounter ID → fight 名 → 絶 zone のボス名 → **動画リンク / 取り込み元のコンテンツ**」の順でした。零式のボス名 (Erichthonios など) は分類器に無いため、最後の「動画リンク由来 = 絶竜詩」に落ちていました。

**修正**:
- fight の判定に **レポートの zone から決まるコンテンツ** を足し、動画リンクより **先** に見るようにしました。zone は「そのレポートが本来どのコンテンツか」を示すので、個別に判定できない pull は動画リンクより zone を信じます。zone から決まらないとき (Ultimates (Legacy) や zone 名が取れない HTML 経路) は従来どおり動画リンク / 取り込み元です。
- 万魔殿の日本語ティア名 (**辺獄編 / 煉獄編 / 天獄編**) を分類器に追加しました。カテゴリ名を「万魔殿パンデモニウム零式:辺獄編」にしていると、zone 名 "Asphodelos" と突き合わせられませんでした (絶竜詩戦争と同じ種類の欠落)。
- カテゴリの **スラッグ** (`ASPHODELOS` など) も突き合わせの手掛かりに使います。日本語のカテゴリ名 + 英語スラッグという構成でそのまま当たるようになります。
- 取り込みダイアログの診断に **「いまの分類器で再分類」** を追加しました。貼ったレポートの pull を、保存済みの fight 名 / encounter ID / zone 名だけで決め直します (FFLogs は叩きません)。個別に判定できず zone からも決まらない pull は今のカテゴリを維持します。診断には「zone から決まるコンテンツ」も表示します。

**既に混ざって保存されている分の直し方**: 練習ログ → URL から取り込む → 該当レポートの URL を貼る → 「診断」→ **「いまの分類器で再分類」**。取り込み直し (「取り込む」) でも同じ結果になります。

**検証**: `scripts/check-fflogs-category.mjs` に混在レポートの 4 ケース (zone 解決 / 絶竜詩は encounter ID で残る / 零式は zone 由来へ / zone 不明なら従来どおり)、`scripts/check-content-groups.mjs` に万魔殿 3 ティアの日英突き合わせを追加。`tsc` / `eslint` / `check-changelog` 通過。

## 🐞 練習ログ: 明細が 1000 件で頭打ちになり、古いセッションが丸ごと出てこなかったのを修正

**実機報告**: 混在レポートの再分類は効いたが、絶竜詩戦争の 2022-04-30 のログが表示されない (総 pull 数には入っている)。

**原因**: 明細の取得が **PostgREST の既定行数上限 (1000 行)** に当たっていました。`.limit(1200)` を指定しても 1 リクエストで返るのは 1000 行までで、`start_ms` の降順で取っているため **最古の pull から溢れます**。絶竜詩は 1047 pull だったので、最古の 47 pull = 2022-04-30 のセッション丸ごとが明細に来ていませんでした。総 pull 数は別の count クエリなので 1047 のまま、という食い違いが起きます (統計カードの「直近 1000 件を表示」がその印でした)。

**修正**: `range()` でページングして上限 (1200 件) まで取り切るようにしました。同時刻の pull があってもページ境界でずれないよう `fight_id` を second key にしています。取り込みダイアログの診断と再分類も、25 レポート分の pull が 1000 行を超えると途中までしか見ていなかったので、同じくページングしました。

**検証**: `tsc` / `eslint` / `check-changelog`。実機では絶竜詩の 2022-04-30 が「日ごとの到達度」と「セッション振り返り」に出ること、統計カードの「直近 N 件を表示」が消えることを確認してください。

## 🚩 練習ログ: 右端の旗の意味を hover で表示 / フェーズ滞在時間の下に「各フェーズへの初到達まで」を追加

**旗の意味 (実機質問)**: 「日ごとの到達度」の右端に出る旗は **その日に自己ベストを更新した** 印です。緑の旗はその日が **初討伐** だったことを表します。読み上げ用のラベルしか付いておらず、マウスを乗せても何も出なかったので、hover で説明が出るようにしました。凡例も「右端 = 討伐」から「右端の旗 = 記録更新」に直しています (旗は討伐日だけでなくベスト更新日にも付くため、前の書き方は誤解を招いていました)。

**各フェーズへの初到達 (実機要望)**: フェーズ滞在時間カードの下に、**P2 以降の各フェーズに初めて到達するまでの累計戦闘時間** と、それが何本目の pull だったかを並べます (hover でその日の日付)。「どこで時間を使っているか」(滞在時間) の下に「どこまで何時間で来たか」(進捗の節目) が並ぶ形です。

- 累計戦闘時間は **到達した pull 自身を含む** 全 pull の戦闘時間の合計です (待機時間は含みません)。
- 1 本の pull で複数フェーズを一気に抜けた場合は、その pull で両方を記録します (実際に通過しているため)。
- 判定は `last_phase` を基本に、フェーズ遷移が取れていればその最大 ID とも突き合わせます。どちらも無い古い pull は本数だけ数えます。
- 集計は登録ログ **全件** が対象です (フェーズ滞在時間と同じく、表示中の明細に限りません)。

**検証**: `scripts/check-fflogs-fight-detail.mjs` に初到達の 4 ケース (開始時刻順の累積 / 飛ばしたフェーズ / 到達不明 / P1 止まり) を追加。`tsc` / `eslint` / `check-changelog` 通過。

## 🎬 練習ログ: 1 つのレポートに複数の動画を紐づけられるように (前半/後半・視点違い) — オフセットは動画ごとに個別

**狙い**: 実機要望「例えば、同日に複数動画が上げられた場合、練習ログに複数紐づけ出来るか。また、切り替えでオフセットも個別に入れられるか」。同じ練習日を前半 / 後半に分けて投稿したり、視点違いを 2 本上げたりすると、これまでは 1 レポートに 1 本しか紐づけられず、後から入れた動画で前の動画が上書きされていました。オフセット (動画のどこで pull #1 が始まるか) は投稿ごとに違うため、1 本目の秒数を 2 本目に流用することもできません。

**実装**:

- `fflogs_report_videos` の主キーを `report_code` → `id (uuid)` に張り替え、`sort_order` / `label` を追加しました。既存 DB からの移行は `schema.sql` 内の `DO $$` ブロックが現在の主キーを見て 1 回だけ行うので、`schema.sql` をそのまま流せます。一意キーは `(report_code, video_url)` — 同じ動画を同じレポートに二重登録できません (部分 index にしていないのは、自動 seed の `ON CONFLICT` 推論に使うため)。
- `fetchReportVideoLinks()` は `report_code → ReportVideoLink[]` を返すようになりました。並びは `sort_order` → 登録順で固定なので、UI の「動画 1 / 2」の番号がリロードで入れ替わりません。
- `setReportVideoAction()` は `id` があればその行を更新、無ければ 1 本追加します (末尾の `sort_order`、1 レポート 12 本まで、URL 重複は拒否)。紐づけを 1 本だけ外す `deleteReportVideoAction()` を追加しました。
- 管理バーは report ごとに 1 行になり、動画チップが横に並びます。各チップにはその動画のオフセット (`+0:56` のように符号付き) が出るので、どの動画に何秒入っているかが一覧で分かります。チップを押すとその動画だけの URL / オフセット / 表示名を編集できます (= 「切り替えでオフセットも個別に」)。「動画を追加」で 2 本目以降を足せます。
- pull 行の動画チップは 1 本 = 1 チップで、複数本の日は頭に番号 (1 / 2 …) が付きます。列幅はその日の最大本数ぶん確保するので、LOGS / ANALYSIS の位置は行ごとにずれません。
- 表示名 (任意 / 40 文字) を入れると「前半」「ヒラ視点」のように表示できます。未入力なら「動画 1」と連番になります。
- 「自動入力」は**すでにこのレポートへ紐づけ済みの URL を候補から外す**ようにしました。同じ日の動画は `logs_url` が同じレポートを指すため、除外しないと 2 本目の自動入力が 1 本目を繰り返してしまいます。
- 同期側の自動 seed は「まだ 1 行も無いレポート」だけを対象にしたままです — 人が決めた並びを後から機械が崩さないようにするためで、2 本目以降は UI から入れます。

**検証**: `tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。

## 🗑 練習ログ: 「ログ削除」をオフセットの真横から行の右端へ (押し間違い防止)

**狙い**: 実機要望「ログ削除のボタンをオフセットの真横に置くと押し間違えそうなので右端に出来るか」。レポート削除は pull を消して以後の同期からも除外する不可逆寄りの操作なので、日常的に押すオフセットのチップと隣り合っているのは危険でした。

**実装**: report ごとの行の右端 (`ml-auto`) へ離し、アイコンのみだったのを「ログ削除」ラベル付きの枠線ボタンにして、動画チップと見た目でも区別できるようにしました。動画の紐づけ解除 (レポートは残る) はオフセットダイアログの中に置いてあり、レポート削除とは別導線です。

**検証**: 上と同じ。

## 📈 練習ログ: 明細の上限を 1200 → 20000 pull に引き上げ (長期の固定でも古いセッションが消えない)

**狙い**: `MAX_FIGHTS = 1200` は「1 pull ≈ 200 B なので数千 pull で RSC ペイロードが MB 級になる」という見立てで置かれていた値ですが、実機の絶竜詩が 1047 pull まで来て上限が目前でした。同じ日に #328 で PostgREST の 1000 行上限は直したものの、明細そのものの上限が残っている限り、あと数百 pull で再び「pull はあるのに古いセッションが 1 日も出てこない」状態に戻ります。

**実測**: 見立てを検証したところ、上限の根拠が成り立っていませんでした。明細は同じ report code / 日付 / ボス名 / 技名が何十行も並ぶため圧縮が 14 倍効きます。

| pull | raw | gzip | brotli |
|---|---|---|---|
| 1200 | 0.63 MB | 46 KB | 24 KB |
| 6000 | 3.16 MB | 228 KB | 99 KB |

(絶 = フェーズ滞在区間込みの重い側。零式は約 3/4。) raw は確かに MB 級ですが、**転送量は 6000 pull でも gzip 228 KB** です。描画も「直近 10 日 + 人が開いた日だけ」なので DOM は pull 数に比例しません。

**実装**:

- `MAX_FIGHTS` を 20000 にしました。実質の撤廃ですが、誤分類で別コンテンツが大量に流れ込む事故でページを巻き込まないための安全弁として値は残しています (週 3 日 × 5 年 × 40 pull ≈ 31000 なので、超長期では届き得ます)。打ち切った場合は従来どおり `truncated` を立てて UI に明示します。
- 上限を上げると往復回数が増えて遅くなるため、**ページングを並列化**しました。1 ページ目で `count` を取り、残りのページは `Promise.all` で同時に投げます。6000 pull でも実質 2 往復です。ページ境界で行がずれないよう `fight_id` を第 2 ソートキーにしています (第 2 キーが無いと同時刻の pull で ORDER BY が非決定になり、同じ行が 2 ページに出たり抜けたりします)。
- 明細 (`fetchCategoryFights`) とフェーズ滞在時間 (`fetchCategoryPhaseTotals`) が別々に自前のページングループを持っていたのを、共通の `fetchAllCategoryFightRows()` に統合しました。上限の管理が 2 箇所に分かれていたため、実際に 1200 と 20000 (20 ページ) でずれていました。
- `scripts/check-fights-payload.mjs` を追加しました。合成した 6000 pull の明細で gzip 予算 (300 KB) と圧縮率 (10 倍以上) を検証します。`FightRow` に太いフィールド (死亡イベント全件・プレイヤー配列など) が足されると圧縮後サイズが跳ねて落ちるので、上限の根拠が崩れたことに気付けます。`MAX_FIGHTS` が検証済みの pull 数を下回っていないかも突き合わせます。

**検証**: `tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。

## ✅ CI: scripts/check-*.mjs を全部実行するように (これまで更新履歴の検査だけだった)

**狙い**: `scripts/check-*.mjs` は 18 本あり、純関数の契約検査 (フェーズ遷移の要約・コンテンツ分類・出欠時刻・ワイプ原因など) を担っていますが、CI で回していたのは `check-changelog` だけでした。残り 17 本はローカルで走らせ忘れると壊れたまま通ってしまう状態でした。

**実装**: CI の該当ステップを `for f in scripts/check-*.mjs` のループに置き換え、1 本でも落ちたらジョブを失敗させます。各スクリプトの出力は `::group::` で畳んで読みやすくしています。

**検証**: ローカルで同じループを実行し、18 本すべて通過することを確認。

## 🧱 練習ログ画面を 9 つの部品に分割 (2,546 → 1,320 行、表示は components/portal/logs へ)

**狙い**: 練習ログは機能追加が集中する画面で、2026-09-06〜07 の 1 週間だけでワイプ原因サマリー (W-1) / フェーズ滞在時間 (W-2) / 各フェーズへの初到達 / 1 レポート N 動画 / レポート診断 / 再分類ボタンが載り、`logs-view.tsx` が 1,827 → 2,546 行になりました。調査ノート第 4 回 8-1 でも「W-1〜W-4 を載せる前に、行ヘッダー / pull 行 / サマリの部品分割が要る」と指摘されていた箇所です。表示の変更はありません（内部整理のみ）。

**実装**: `src/components/portal/logs/` に 9 モジュールを切り出しました。既存の `settings/` `schedule/` と同じ置き方です。

| ファイル | 行数 | 役割 |
|---|---|---|
| `stat-card.tsx` | 102 | 上部サマリのタイルと総 pull の内訳チップ |
| `wipe-causes-card.tsx` | 90 | ワイプ原因の内訳カード (W-1) |
| `phase-time-card.tsx` | 124 | フェーズ滞在時間 + 各フェーズへの初到達 (W-2) |
| `phase-span-bar.tsx` | 37 | 1 pull のフェーズ滞在バー |
| `day-row.tsx` | 321 | 日ごとの行 (見出し + pull 一覧 + 管理バー) |
| `pull-row.tsx` | 363 | pull 1 本の行 |
| `failed-list.tsx` | 52 | 取り込めなかったレポートの一覧 |
| `offset-dialog.tsx` | 196 | 動画 URL / オフセット / 表示名の編集ダイアログ |
| `video-link.ts` | 35 | 動画リンクの表示ヘルパー (`day-row` と `pull-row` が共有) |

- `logs-view.tsx` (1,320 行) が持つのは**状態と組み立てだけ**になりました。同期・取り込み・診断・再分類のダイアログ、フィルタ、集計の `useMemo`、編集中の動画の保持です。依存の向きは一方向 (`logs-view` → `day-row` → `pull-row` → `phase-span-bar`) で、`src/components/portal/logs/README.md` に図と分割時の判断を書いてあります。
- 色のクラス表 (`FLOOR_TEXT_TONE` / `PHASE_TEXT_TONE` / `PHASE_BAR_TONE`) は `logs-view.tsx` のローカル定数でしたが、分割で 3 つ以上の部品から参照されるため `@/lib/fflogs-progress` へ移し、色相の対になっている `floorToneClass` / `phaseToneClass` の隣に `floorTextToneClass()` / `phaseTextToneClass()` / `phaseBarToneClass()` として置きました。フォールバック色が呼び出し側で `text-foreground/70` と `/75` に分かれていた (事故) のは `/75` に揃えています。
- Server Action の呼び出しを持つ部品は `offset-dialog.tsx` だけ (動画の保存 / 削除) です。

**検証**: `tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。加えて、移動対象の行を元ファイルと新モジュールで正規化して突き合わせ、**差分が lib へ移した色表の中身だけ (追加・欠落ゼロ)** であることを機械的に確認しました。

## 👀 攻略情報: リンクごとに「既読」を付けられるように (未読人数を表示 / 誰が未読かは管理者のみ)

**狙い**: 調査ノート第 4 回 5-3 で「共有した攻略情報が読まれない」(「何の話？」「見てなかった」) が**未解決**として残っていた項目 (W-27)。本 portal の Discord 自動取り込みは「集約」まではできていて、「到達」の確認手段がありませんでした。

**実装**:

- 攻略リンクのカード下段に「既読にする」トグルを置きました。押すと自分の既読が記録され、隣に「未読 3 人」(全員が読んだら「全員読んだ」) が出ます。
- **誰が未読かは管理者だけ**に見えます (hover で名前)。調査ノートで挙がっていた「監視感」への配慮で、既読者の一覧はそもそも作りません。
- これはデータの出口で担保しています。`category_link_reads` は RLS 有効 + **ポリシーを 1 つも張らない**設計で、anon / authenticated からは 1 行も読めません。読み取りは server 側 (`fetchCategoryLinkReads`) が service role で読んで**集計してから**返し、クライアントに渡るのは「自分が既読か」「未読人数」「(管理者のみ) 未読メンバーの名前」の 3 つだけです。UI 側の出し分けだけに頼ると RSC ペイロードに生データが載ってしまうため、この形にしました。
- 書き込みも `discord_user_id` を**サーバーが認証情報から取ります** (クライアントから受け取りません)。受け取る作りだと他人の既読を付け外しできてしまいます。公開デモの匿名ゲストは固定 ID を共有しているため明示的に弾いています (メンバーコメント編集と同じ理由)。
- 未読人数の母数は `native_schedule_members` の有効メンバーです。メンバー表を使っていない環境では人数を出さず、既読トグルだけが機能します (母数 0 で「未読 0 人」と出すと「全員読んだ」と誤読されるため)。

## 🏷 攻略情報: リンクにフェーズ / ギミックのタグを付けて絞り込めるように (タイトルから候補を提案)

**狙い**: 調査ノート第 1 回 B-1。Discord 自動取り込みでリンクは溜まりますが、「いま練習しているフェーズの資料」を探せませんでした。W-27 と同じカード改修で入るため同時に実装しています。

**実装**:

- カードにタグのチップを置き、管理者は「タグ」ボタンから追加 / × で削除できます。チップは**誰でもクリックで絞り込み**に使えます。
- 絞り込みは **AND** です (「P3 の 散開」を出すのが実際の用途で、OR だと選ぶほど結果が増えて絞り込みになりません)。リンク一覧の上にカテゴリ内の全タグがチップで並びます。
- 追加時にタイトルと URL から**候補**を提案します (`P3` / `p3` / `フェーズ3` / `Phase 3` / `3フェーズ目` を `P3` に正規化 + 「散開」「タイムライン」「マクロ」「軽減」「ギミック解説」「動画」「装備」)。**自動では付けません** — タイトルは投稿者の書き方に依存するので、機械的に付けると外れたタグが溜まって絞り込みがかえって使えなくなります。候補チップを人が押して確定します。
- 誤検出を抑えてあります: `P10` のような 2 桁、`MVP3` のような語中の P+数字、`2026 年` のような年号は拾いません。URL は**パスとクエリだけ**を見ます (ホスト名を混ぜると `phase3.example.com` のようなドメインで誤検出します)。
- タグの色はラベルから決定的に決まるので、同じタグはどのカードでも同じ色です。フェーズタグ (P1〜P7) は**練習ログのフェーズ色と同じ色相**にしてあります (同じ「P3」がログと攻略タブで別色だと対応が取れないため)。
- 既存の `tags` テーブルを使います (`target_type` に `category_link` を追加)。ラベルは 24 文字 / 1 リンク 8 個まで。大文字小文字だけ違うラベルは同じタグとして扱います (「p3」と「P3」で絞り込みが分裂しないよう)。
- タグで絞り込んでいる間はドラッグ並び替えを止めます (見えていないカードを含む並びを、見えている分だけで書き換えてしまうため)。

**検証**: `scripts/check-link-tags.mjs` に 40 ケース (正規化 / フェーズ表記の 5 形式 / 誤検出の除外 / URL / 候補の除外 / 色 / AND 絞り込み)。`tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。

## 🎚 コンテンツに「難易度」と「進行モデル (層 / フェーズ)」を設定できるように (8.0 の新難易度に備える)

**狙い**: 調査ノート第 4 回 W-33 ①（★★★ / 期限 2026-12）。練習ログはこれまで**カテゴリ名の文字列マッチ**だけで挙動を決めていました（`isUltimateContent` なら フェーズ (P1〜) 管理、`isSavageContent` なら 4 層構成）。パッチ 8.0「白銀のワンダラー」(2027-01) では**ノーマルと零式の中間の新難易度**が入りますが、2026-09 時点で**正式名称が未発表**です。名前が分からないものは辞書に足せないので、新難易度のカテゴリを作った瞬間に「層でもフェーズでもない」判定になり、層クラスタが崩れた表示になります。既存でも、英語名や通称で登録して判定を外したカテゴリを人が直す手段がありませんでした。

**実装**: コンテンツ編集に 2 つの設定を追加しました。

- **難易度 (任意)**: 自由記述 24 文字。練習ログの見出しにバッジで出ます。空欄なら従来どおり名前から推測します (零式 / 絶)。
- **進行モデル**: 「自動」(既定・従来挙動) / 「層」/ 「フェーズ」。名前から判別できないコンテンツで明示的に選べます。

**enum を増やしていない**のが要点です (調査ノートの「名称・仕様が未確定 → enum 固定にせず設定値で吸収」)。名称が判明したら `content-groups.ts` の辞書に足すだけで「自動」のまま通るようになり、それまでは明示指定で運用できます。

安全側の既定も入れています。「層」を明示しても、名前から零式と分からなければ層数は実データ任せにします — 4 層を決め打ちすると、新難易度が 1 ボス構成だった場合に最終層の判定 (= クリア数の数え方) が壊れるためです。名前も指定も無いときは**層**に倒します (「不明」という第 3 の状態を UI に持ち込まないため)。

**検証**: `scripts/check-content-model.mjs` に 36 ケース (既知の名前の回帰ガード / 未知の名前 / 明示指定の優先 / 層数の仮定 / ラベルの推測 / 色)。

## 📆 週制限の消化チェックを 2 週ウィンドウに対応 (8.0 のトームストーン遡り取得)

**狙い**: W-33 ②。8.0 ではデイリーが廃止されて週単位ボーナス制になり、アラガントームストーンが **2 週管理** (前週分を遡って取得できる) になります。ロット管理の消化チェックは「今週だけ開いている」前提だったので、前週の行が火曜 17:00 のリセットで締まってしまい、遡り取得を記録できません。

**実装**:

- 設定ダイアログに「週制限の消化ウィンドウ」を追加し、**今週だけ** (既定・7.x の挙動) / **今週 + 前週** から選べます。
- 「今週 + 前週」にすると、ロット管理に 2 枚のパネルが並びます。前週側は「前週 (遡り)」バッジが付き、既定で畳まれ、「次のリセットまで」は出しません (今週の話なので前週には無関係)。折りたたみ状態も別に保存されるので 2 枚が連動して開閉しません。
- **コードに「8.0 以降なら 2」と焼き込んでいません。** 仕様が実機で確認できるのは 2027-01 以降なので、切り替えは人が設定で行います。

**検証**: `scripts/check-loot-window.mjs` に 26 ケース。週識別子の境界 (火曜 08:00 UTC = JST 17:00) の回帰ガードと、前週計算が月末 / 年末 / うるう年を跨いでも正しいこと、設定値の不正入力が既定に倒れることを固定しています。

## 🌐 メンバー一覧にデータセンター表記を追加 (クロスプレイ前提)

**狙い**: W-33 ③。Switch 2 版が 2026-08-04 にサービス開始してクロスプレイ前提が固まり、別 DC のメンバーが混在する固定が増えています。

**実装**: 設定ダイアログのメンバー一覧に DC 欄 (自由記述 20 文字) を追加しました。DC 名は運営の再編で増減するので enum にせず自由記述です。空欄で未設定に戻せます。

**あわせて**: 8.0 対応の状況と、まだ決められないこと (④ 立ち上げウィザードを東京ファンフェス後に判断する理由) を `docs/patch-8.0-readiness.md` に整理しました。8.0 が来たときのチェックリストも入れてあります。

## ⏱ 練習ログ: 日ごとに「拘束 / 実戦闘 / 戦闘外 / 平均プル長」のセッションサマリーを表示

**狙い**: 調査ノート第 4 回 W-3 (★★)。固定運営で実際に議論になるのは「22:00 開始で 2 時間やって何本回せたか」なので、pull 数だけでは足りません。既存の列 (`start_ms` / `end_ms`) の集計だけで出せます。

**実装**: 日を開くと pull 一覧の直上に 1 行で出ます。

- **拘束** = 最初の pull の開始から最後の pull の終了まで。集合や休憩開始からではないので実際の拘束より短く出ます — pull の外はログに現れないため、これが観測できる上限です。
- **戦闘** = 各 pull の戦闘時間の合計。
- **戦闘外** = 拘束 − 戦闘。割合も併記します。
- **平均** = 平均プル長。
- 討伐 / ワイプの本数。

「戦闘外」には休憩・解説・作戦会議・リセット待ちが全部混ざります。**「無駄な時間」ではない**ので、ラベルは「戦闘外」に留めて価値判断を混ぜていません (調査ノートの「文言で吸収」)。hover でその説明が出ます。

pull が重なって記録されている場合 (別 PT のログが混ざった等) でも戦闘外が負にならないよう 0 で止めています。並び順にも依存しません (DB からは `start_ms` 降順で来ます)。

## 🏅 練習ログ: チーム実績バッジ (初討伐 / ノーデス討伐 / 最速討伐 / 討伐回数)

**狙い**: 調査ノート第 4 回 W-31 (★★)。既存の `deaths` / `kill` 列から算出でき、消化期のモチベに効きます。

**実装**: 討伐があるカテゴリの上部にバッジを並べます。

| バッジ | 条件 |
|---|---|
| 初討伐 | 登録ログのうち時系列で最初の討伐 (日付を添える) |
| ノーデス討伐 | `deaths = 0` の討伐があった |
| 最速討伐 | 討伐のうち戦闘時間が最短のもの (タイムを添える) |
| 討伐回数 | 2 回以上のときだけ (1 回なら初討伐と同じ情報なので出さない) |

- **チームの実績だけ**を出します。個人 DPS / 出席率のランキングは作らないという方針と揃えました。
- **種類は 4 つに絞っています。** バッジを乱発すると陳腐化して誰も見なくなる、というのが調査ノートのデメリット欄の指摘です。
- 討伐が 1 つも無いカテゴリでは**何も出しません** (練習中に空の枠が出ると「まだ何も無い」ことだけを主張する枠になってしまうため)。
- `deaths` が未取得 (null) の討伐は**ノーデス扱いしません**。0 と見なすと死亡数を取得していない古い pull が全部「ノーデス討伐」になってしまいます。
- 層クラスタ外 (同じレポートに混ざった別コンテンツ) の pull は除いてから判定します — 混ざった kill が「初討伐」になると日付が狂います。

**検証**: `scripts/check-fflogs-session.mjs` に 30 ケース (拘束時間が並び順に依存しないこと / 重なった pull で戦闘外が負にならないこと / `deaths` 未取得をノーデスと誤判定しないこと / 1 回の討伐で回数バッジを出さないこと など)。`tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。

## 🔧 公式メンテ日程を登録できるように / 活動予定と重なると次回開催カードに警告

**狙い**: 調査ノート第 4 回 W-30 (★★)。「今夜メンテだった」を防ぎます。

**実装**: 設定に「メンテナンス日程」を追加しました。開始・終了・メモを行で登録し、活動予定と時間帯が重なると次回開催カードにオレンジの警告が出ます。

- 公式に機械可読な情報が無いので**手入力**です。Lodestone のトピック監視まで自動化すると scrape 依存になり、告知ページの構造変更で黙って壊れます — 年に数回の入力で足りるものに、その保守コストは見合いません。
- 入力欄は `<input type="datetime-local">` で、値の書式 (`YYYY-MM-DDTHH:mm`) がそのまま保存形式と一致します (パースを自作していません)。
- 衝突の判定は**半開区間** `[start, end)` です。メンテ終了ちょうどに始まる活動は衝突扱いしません (「明けたら集合」の運用があるため)。
- 開催の絶対時刻は「挑戦中」表記の判定と**同じ計算**を使い回します。別々に計算すると片方だけ翌日跨ぎ (22:00〜0:00) を取りこぼします。
- 読み取り側は不正な行を黙って捨てますが、**保存時はエラーを返します** — 「登録したのに警告が出ない」状態に気付けないのを避けるためで、何行目が悪いかも出します。
- 値は `app_settings` の 1 キーに JSON で持ちます (多くても年 20〜30 件なので専用テーブルは作りません)。TOP 描画の一括読みに相乗りさせたので追加の DB 往復はありません。

**検証**: `scripts/check-maintenance-schedule.mjs` に 36 ケース (実在しない日付 / うるう年 / JST→UTC / 壊れた JSON / 半開区間の境界 / 日跨ぎ表示)。

## 📣 出欠催促の頻度を選べるように (期限に 1 回 / 期限 + 当日 / 毎日 1 回)

**狙い**: 調査ノート第 4 回 W-20 (★★)。国内固定では「前日に 1 回 + 当日にもう 1 回」や「埋まるまで毎日 1 回」も受容されているので、催促圧を運用に合わせて調整できるようにしました。

**実装**: 出欠催促の設定に「催促の頻度」を追加しました。

| 選択 | 挙動 |
|---|---|
| 期限に 1 回 (既定) | 設定した日数前に 1 通だけ — **これまでと同じ** |
| 期限 + 当日 | 設定した日数前と、開催当日にもう 1 通 |
| 毎日 1 回 | 設定した日数前から当日まで、未入力が残っている間は 1 日 1 通 |

- **既定は現行挙動**なので、既存の設定は何も変わりません。
- 二重送信を止めるマーカーに**暦日を混ぜ**ました。混ぜないと「当日分」も「送信済み」に当たって 1 通も飛びません。「期限に 1 回」のときだけは従来どおり開催日だけをキーにします。
- 未入力者がいる日を**期限が遠い順**に探します。当日を先に見ると、当日分が埋まっていて 2 日前が空のケースを取りこぼします。

**検証**: `scripts/check-attendance-reminder.mjs` に 17 ケース追加 (既定が現行挙動であること / lead=0 で重複しないこと / dedup マーカーが暦日で変わること)。

## 🔗 リンク判定辞書に 3 サイト追加 + 「募集」バッジを新設

**狙い**: 調査ノート第 4 回 W-32 (★★)。Discord 取り込みの整理精度が上がります。

**実装**: `knt-a.com` (攻略)、`xivraidplanner.app` (作図 — 作図そのものではありませんが「計画を共有するページ」として寄せました)、`xivrecruit.com` を追加し、募集サイト用に **「募集」種別**を新設しました (攻略資料ではないので「攻略」バッジが付くと中身を誤解させます)。

調査ノートが挙げていた ffreplay / thepfstrat 系は**公開ホスト名を確認できていないため追加していません**。誤ったホストを入れると別サイトのリンクに「ログ」バッジが付くので、確認できてから足します。

**検証**: `scripts/check-link-site.mjs` に 6 ケース追加 (サブドメイン / 似た名前のホストを巻き込まないこと / ja・en ラベル)。

## 📚 ドキュメント 2 本: ログ担当の手引き / Discord の推奨構成と設定手順

**狙い**: 調査ノート第 4 回 W-34 (★★)。導入障壁と「誰がログを回すか」の属人化を下げます。コード変更はありません。

- **`docs/guides/log-runner.md`** — 計測は portal の外 (FFLogs Uploader) で行う分担の説明、担当は 1 人 + サブ 1 人、**Unlisted 推奨の理由** (Public は個人成績が検索から辿れる / Private は取り込みが認証依存で失敗しやすい)、Auto Logging、**パッチ週の注意** (Uploader の更新待ち / zone・encounter ID の振り直し / 旧絶の Legacy zone 畳み込み)、取り込みが動かないときの確認順、動画とオフセットの登録手順、やらないこと (クライアント側の自動化 / 個人 DPS の集計)。
- **`docs/guides/discord-setup.md`** — 推奨チャンネル構成 4 つと分け方の指針 (攻略と動画は分ける / 通知先は 1 つに寄せる / コンテンツごとに分ける必要はない)、チャンネル ID の取得、**必要な Bot 権限** (管理者権限は不要)、出欠催促のメンション設定の順序、練習ログ通知は「まず初討伐だけ ON」、よくある詰まりの一覧。
## 📊 練習ログ: 進行トレンド (到達度の推移グラフ + クリアまでのペースの目安)

**狙い**: 調査ノート第 4 回 W-4 (★★)。「日ごとのバー」を時系列化して、累積 pull に対する到達度の伸びを 1 枚で見せます。元データは既に `progressTimeline()` が返している値なので、新しい取得もテーブルも要りません。

**実装**:

- **グラフライブラリは入れていません。** 調査ノートのデメリット欄は「グラフ描画ライブラリの追加 (bundle)」でしたが、必要なのは折れ線 2 本と点だけなので**インライン SVG** で描き、座標は純関数 (`sparklinePath()`) が組みます。recharts / chart.js は 50〜200 KB の client bundle が全ページに乗るので、この規模では釣り合いません。
- 折れ線は 2 本です。薄い方が「その日の到達度」(上下する)、濃い方が「そこまでの最高到達」(下がらない)。片方だけだと「調子の悪い日」か「伸びの傾向」のどちらかが読めません。初討伐の日には点を打ちます (クリア後は最高到達が 100% で張り付くので、線だけでは「いつクリアしたか」が読めません)。
- 座標は小数 2 桁に丸めています。SSR とクライアントで文字列が一致しないと hydration mismatch になるためです。

**「ペースの目安」の扱い**: 「このペースならクリアまで何 pull か」は要望として分かりますが、外挿は当たりません — 到達度は線形に伸びず、最後のフェーズで止まるのが普通です。そこで次の 3 条件を満たしたときだけ数字を出し、UI でも「目安」と明示しています。

- **直近 5 セッションだけ**を見る (古い急成長を混ぜない)
- **最高到達が伸びている場合だけ**返す (停滞・後退で「あと ∞ pull」を出さない)
- セッションが 3 回未満なら返さない

クリア済みのコンテンツでも出しません。2 セッション未満ではグラフ自体を描きません (傾向が存在しないため)。

**検証**: `scripts/check-fflogs-trend.mjs` に 27 ケース。特に「ペースを**出さない**条件」(停滞 / 後退 / クリア済み / セッション不足 / 直近ウィンドウ外の急成長) を重点的に固定しています。

## 🔔 練習ログ: 新レポート / ベスト更新 / 初討伐を Discord に通知 (すべて既定 OFF)

**狙い**: 調査ノート第 4 回 W-35 (★★)。portal を見に来る動機を作ります。同期のたびに何が起きたかを判定して Discord に流します。

**種類は 3 つ**です。

| 種類 | いつ飛ぶか |
|---|---|
| 新しいレポートの取り込み | 同期で新しいレポートが入ったとき (最も頻度が高い) |
| ベスト到達の更新 | 到達フェーズが進んだとき、または同じフェーズで残 HP% が下がったとき |
| 初討伐 | そのコンテンツを初めて討伐したとき (1 回だけ) |

調査ノートは「軽減表・マクロ更新」も挙げていましたが、**軽減表は Google Sheets が正**で portal がデータを持たないため更新を検出する手段がそもそもありません。マクロは portal 側のデータですが更新頻度が低い一方で保存経路すべてにフックが必要なので、費用対効果が釣り合わないと判断して外しました。

**すべて既定 OFF** です。通知過多が調査ノートのデメリット欄そのものなので、必要なものだけ管理者が ON にします。投稿先はスケジュール通知と同じ Discord チャンネル設定を使い回し、**メンションは一切付けません** (見に来る動機を作るのが目的で、全員を叩き起こすものではありません)。

**誤検出を防ぐ設計**:

- **初回同期ではベスト更新を通知しません。** 初めて取り込んだ全ログが「更新」として一斉に飛ぶのを避けるため、前回通知時の到達度を `fflogs_notify_state` に 1 行持ち、その行が無いカテゴリでは判定をスキップします。
- **フェーズが進んだときは残 HP% を比較しません。** 新しいフェーズの削りは必ず 100% から始まるので、比較すると必ず「後退」に見えます (P3 25% → P4 90%)。
- **フェーズが下がった日は更新扱いしません** (その日の調子が悪かっただけ)。
- 残 HP% は**その最深フェーズの pull だけ**で最小を採ります。フェーズを絞らないと、浅いフェーズで粘った pull (P2 で 1% など) が「ベスト」になってフェーズが進んでも後退判定になります。
- スナップショットは通知の可否に関わらず必ず進めます。OFF の間の更新を溜めて ON にした瞬間に全部飛ばす、という挙動を避けるためです。
- 通知は同期の付随処理なので、失敗しても同期自体は成功扱いにします。

**あわせて (W-5 の前段)**: FFLogs guild ID を記録する欄を設定に追加しました。**現時点では取り込みに使われません** — 「動画リンクを介さず guild からレポートを自動で見つける」(W-5) は固定内で「レポートを guild に上げる」運用に揃えてから実装する前提なので、記録場所だけを先に用意しています。UI にもその旨を明示しています。

**検証**: `scripts/check-logs-notify.mjs` に 32 ケース (既定 OFF / 初回同期 / フェーズ前進時の残 HP% 非比較 / 後退の除外 / 通知本文)。`tsc` / `eslint` / `next build` / 全 `scripts/check-*.mjs` 通過。

## 📝 募集文に変数を差し込めるように (コンテンツ / 日時は自動 / フェーズ・武器・DC はコピー時に入力)

**狙い**: 調査ノート第 4 回 W-28 (★★)。欠員が出たときの野良補充で、募集テンプレの本文に「コンテンツ / フェーズ / 希望武器 / 時間 / DC」を差し込めるようにしました。既存の `recruitment_templates` に**変数を足すだけ**で、テーブルは増やしていません。

**自動と手入力を分けています**:

| 種類 | 変数 |
|---|---|
| 自動（portal が知っている） | `{content}` `{difficulty}` `{date}` `{time_start}` `{time_end}` `{site_url}` |
| コピー時に入力（portal が知りようがない） | `{phase}` `{weapon}` `{dc}` |

全部を設定にすると「募集のたびに設定を開いて直す」ことになり、テンプレの意味が薄れます。手入力の値は**ブラウザに覚えさせる**ので毎回打ち直す必要はありません（DC は固定で変わらず、フェーズも数週間は同じ）。

- 手入力の変数を使っているテンプレのときだけダイアログが開き、**使われている変数の欄だけ**を出します（`{phase}` を使わないテンプレでフェーズを聞きません）。自動変数だけのテンプレは**従来どおり 1 クリックでコピー**します。
- ダイアログには**貼られる文面のプレビュー**を出します。
- **未指定の変数は空文字にせず `{phase}` のまま残します。** 空にすると「P{phase} 練習中」が「P 練習中」になって、貼ってから気付くことになります。変数が残っていればプレビューで気付けます。
- 値の中に `{...}` が含まれていても再展開しません（フェーズに「{weapon}」と入れられても意図しない置換になりません）。未知の `{foo}` はそのまま残します（本文には絵文字や `<t:...>` などの記号が入るため）。

**検証**: `scripts/check-recruitment-placeholders.mjs` に 23 ケース。

## 🛡 BiS に部位別の「取得済」チェックを追加 (装備 n/11 のバッジ)

**狙い**: 調査ノート第 4 回 W-23 (★★)。消化週の「残り目標」が一目で分かるようにしました。

**実装**: 攻略情報タブの BiS 行に `装備 3/11` のバッジが出て、押すと部位のチェックが並びます。

- 持つのは **部位 × 取得済** だけです。部位ごとのアイテム名やソースは **Google Sheets のロット表が正**なので、二重管理にしません（調査ノートのデメリット欄）。
- **分母はジョブで変わります。** 盾を装備するのはナイトだけなので、他ジョブは 11 部位、ナイトは 12 部位です。ジョブ未設定のときは 11 に倒します（分母を大きく見せて「まだ埋まっていない」と誤認させないため）。他ジョブで盾が取得済みになっていても分子・分母に入れません（ジョブを後から変えても「12/11 取得」になりません）。
- バッジの色は 3 段階です（完成 / 半分以上 / それ未満）。BiS は運用上この 3 状態しか意味を持ちません。
- **誰が入力するか**: サインイン済みのメンバーなら誰でもです。`category_bis_links.owner_name` は自由記述で Discord ID と紐づいていないため「本人だけ」を強制できず、実運用でも「今日ドロップした分をその場で誰かが付ける」のが自然で、本人待ちにすると埋まりません（ロット表 Sheets と同じ信頼モデル）。週次消化チェックの「本人だけ」とは扱いが違う点をコード側にも注記してあります。
- トグルは楽観反映です（連打しても押せていないように見えない）。失敗したら元に戻してエラーを出します。

**検証**: `scripts/check-bis-slots.mjs` に 26 ケース（ジョブ別の分母 / 数えない部位の無視 / ナイトは盾が無いと完成しない / 未知の部位名 / バッジの色）。

### 🗓 あわせて W-16 (iCal 購読フィード) は見送りとした

調査ノート第 4 回で ★★ として再提案されていましたが、**見送り**にしました。`src/lib/calendar-link.ts` に 2026-08-30 時点の判断として「iCal 配信はサーバー実装が要るうえ、認証付き portal の予定を外部 URL で撒くことになるため採らない」と明記されており、再提案の「メンバー別トークン URL」はその懸念を部分的にしか解消しないためです — トークン付き URL は実質パスワードで、Google / Apple のサーバーが**認証なしで取得する**前提は変わらず、端末単位の失効もできません。

既存の「Google カレンダーへ 1 クリック追加」リンクで用途は足りていると判断し、判断の経緯と再検討時の条件を調査ノートに追記しました。

## 🔠 文字サイズの下限を 11px に (10px 以下の 392 箇所を引き上げ / 本文は 12px)

**狙い**: 調査ノート第 4 回 8-4。第 2 回の UI 監査では「10px 基調 + 意味色」に寄せていましたが、外部の基準に照らすと **10px は下限を割っていました**。

| 基準 | 最小フォント |
|---|---|
| Apple HIG | 11pt |
| Material (label-small) | 11px |
| WCAG | 絶対値の規定は無いが、拡大しても崩れないことを要求 |

**方針転換**: 「10px 基調を他画面へ展開する」という第 2 回の残課題を撤回し、**逆方向 (引き上げ)** に転換しました。portal の基準は次の 3 行です。

- **本文 / 表の中身 = 12px 以上** (`tabular-nums` で桁を揃える)
- **ラベル / チップ = 11px** (大文字ラベル・枠付きバッジなど 1 行で収まる前提のもの)
- **10px 以下は使わない**

密度は行高と左右 padding で調整するので、「詰めたいから 10px に戻す」は取りません。

**実装**: `text-[10px]` 329 箇所 + `text-[9px]` 63 箇所 = 392 箇所を、次の分類で 11px / 12px に振り分けました。

- **11px にしたもの (270)**: 字送りを触っている (`uppercase` / `tracking-*`)、1 行前提 (`leading-none` / `leading-tight` / `truncate`)、枠付きチップ (`rounded` / `border`、`px-` と `py-` の両方) — いずれも「折り返さないラベル」なので 11px で足ります。
- **12px にしたもの (122)**: それ以外 — 設定ダイアログの補足文、`<p>` / `<summary>` / `<label>`、同期ログの一覧、数値の並ぶ行。`font-mono` だけを理由に 11px には落としていません (等幅の数値列はまさに「表の中身」なので 12px)。
- アイコン右上の件数バッジ 2 箇所は 8px でした。円が 10px しか無かったので、**円を 14px に広げて 11px** にしています (文字だけ大きくしても入りません)。

一括置換ではなく上の分類を規則として書き、置換後の差分を分類ごとに確認しています。

**逆戻り防止**: `scripts/check-font-sizes.mjs` を追加しました。`src/` の `text-[Npx]` を走査して **11px 未満が 1 つでもあれば CI を落とします** (`--list` で px ごとの棚卸しも出せます)。基準を決めても、次に画面を詰めたくなったときに 10px が 1 つ戻り、そこから広がるのが常なので、機械で止めます。

**検証**: `node scripts/check-font-sizes.mjs` (11px 未満 0 件 / 現在 11px 527・12px 189・13px 4・14px 1)、`npx tsc --noEmit`、`npx next build`。
## 🌏 残っていた日本語 116 件を 6 分類して固定 / 入力エラー文を日英化 (訳し忘れは CI で検出)

**狙い**: 表示言語 (ja / en) 対応の第 1〜3 段で画面の文言はほぼ辞書へ移しましたが、**移してはいけない日本語**が残ります。第 3 段の時点では「意図的に残した 47 件」という数字だけがリリースノートにある状態で、次に触る人が「これは訳し忘れか、意図か」を毎回コードから読み直す必要がありました。分類と理由を 1 箇所に持ち、増減を機械で検出します。

**まず自動判定**: `scripts/check-ja-strings.mjs` は次を「対応済み」として baseline に載せません。

1. `locale` 引数を取る関数の中 (純関数モジュールが辞書を import せず引数で切り替える形。`url-validation.ts` / `attendance-ui.ts` の方針)
2. `locale` を条件に持つ三項 / if の中
3. `FOO_JA` という定数で、同じファイルに `FOO_EN` があるもの

これで 209 件のうち 93 件が自動で落ち、残りを分類しました。

**6 分類 (計 116 件 / 24 ファイル)**:

| 分類 | 件数 | 内容 |
|---|---:|---|
| **A. DB に保存される値 / 突き合わせキー** | 46 | 訳すとデータが壊れる。`CategoryStatus` の `クリア済` / `未着手`、`LOOT_WEEKLY_STATUSES`、Google Sheets の列見出し、character-sheets の出力書式 (`■コメント`)、sync 互換の `yyyy/MM/dd(曜)` 書式、DB に入るタグ label の候補 |
| **B. 日本語側が正で、表示用の英語は別に持つ** | 38 | 既定値やキーとして日本語を保持し、描画時に locale 版へ差し替える。出欠の記号ラベル、サブタブ名、リンク種別のラベル |
| **C. Discord に送る文** | 8 | 送信先が日本語コミュニティの Discord。**閲覧者の表示言語ではなくサーバーの言語**に合わせる (いずれも管理者が編集可能) |
| **D. ゲーム内の固有名** | 16 | 軽減アビリティ名。英語表記は別物 (リプライザル = Reprisal) で、対応表を持たない限り訳せない |
| **E. 運用者 / 開発者向け** | 7 | 環境変数の不足、OAuth 失敗、Provider 外使用の throw。閲覧者の画面には出ない |
| **F. 誤検出** | 1 | ブックマークレットの JS コード |

**逆戻り防止**: baseline に無いファイルに日本語が入った、または baseline のファイルで件数が増えた / 減ったときに CI を落とします。増えたときのメッセージは「画面に出る文言なら辞書へ。意図的に日本語なら BASELINE に分類と理由を足す」と出るので、判断がその場で終わります。`--list` を付けると分類つきで全件出ます。旧 `scripts/audit-ja-strings.mjs` (列挙するだけ) はこれに置き換えました。

**あわせて訳したもの — 入力エラー文**: 「本文が長すぎます（最大 8000 文字）」系の文言が 4 つの client モジュール (マクロ / ウェイマーク / 募集テンプレ / 日付メモ) に**同じ形で重複していて、しかも日本語固定**でした。`src/lib/text-length-error.ts` に集約して `locale` で切り替えます。RLS で弾かれたときの「更新できませんでした（権限がない可能性があります）」も同じ場所に置きました (PostgREST は RLS で弾いた UPDATE / DELETE をエラーではなく 0 件で返すため、呼び出し側が失敗に変換している箇所です)。

- 複数の欄が超えていても **1 件だけ**返します。トーストは 1 行なので、並べても直す順番は変わりません。
- 長さは `.length` (UTF-16) で数えます。Postgres の `char_length` (コードポイント) 以上の値になるので、**ここを通れば DB も通る**安全側に倒れます (絵文字は 2 と数えるため厳しめに出ます)。

**死んでいたコードの削除**: 走査で 2 つ見つかったので消しました。`url-safe.ts` の `assertSafeUrl` (呼び出し 0 件。URL 検証は `isSafeUrl` / `httpUrlError` が担当) と、`perf-tone.ts` の `PERF_LABEL` (呼び出し 0 件。title / aria 用として置かれたが使われていなかった。`check-perf-tone.mjs` の検査対象からも外しました)。

**検証**: `scripts/check-ja-strings.mjs` / `scripts/check-text-length-error.mjs` (21 ケース) / `check-perf-tone.mjs`、`npx tsc --noEmit`、`npx eslint src`、`npx next build`。


## 🎯 動画オフセットを「動画を見ながら」決められるように (秒数を数えなくてよい / ±1 秒の微調整)

**狙い**: 調査ノート第 4 回 W-11 (★★、出典 xivodreview-local)。pull 行から動画の該当時刻へ飛べるのは、レポートごとに「**動画上で pull #1 の戦闘開始が何秒か**」= オフセットを 1 つ持っているためですが、これまではその秒数を人が動画を見ながら数えて手入力していました。

**実装**: オフセットのダイアログに「動画を見ながら合わせる」を追加しました。開くと動画が埋め込まれ、再生して基準の瞬間で止めて「いまの位置を基準にする」を押すとオフセットが逆算されます。

- **基準にする pull を選べます。** 前半を録っていない動画では #1 が映っていないので、映っている pull を基準にできます。選択肢はそのレポートの pull だけです (別レポートの pull を基準にするとオフセットの意味が変わるため)。選択肢は**時刻で示します** — 連番はレポート内の順序で、pull 行の「#N」は日単位の順序なので、同じ日に複数レポートがあると食い違います (マージ前レビューで検出)。
- **「推定位置へ移動」** で、いまのオフセットから計算した位置へ飛べます。合っているかの確認と、ずれの方向の把握がこれで済みます。
- **±1 秒ボタン**を数値欄の隣に置きました。動画で合わせたあと「開幕がちょっと早い」を数字を打ち直さずに詰められます。
- 丸めは**四捨五入**です。切り捨てだと常に動画の手前側にずれ、開幕が 1 秒切れて「もう始まっている」ように見えます (0.5 秒でも開幕は詰まっているので分かります)。
- **YouTube のときだけ**使えます。Google フォトとニコニコ動画は再生位置を外から読めないので、そちらは従来どおり手入力で、パネルにはその旨だけ出ます。

**外部 script を読み込んでいません**: YouTube の IFrame Player API (`iframe_api`) は本 portal の CSP (`script-src 'self' 'nonce-...'`、2026-06-09 の TODO #84) で止まります。オフセット校正のために XSS 経路を広げるのは釣り合わないので、埋め込み iframe への `postMessage` を直接使っています (`frame-src` には YouTube が既に入っています)。API script はこの postMessage の薄いラッパーなので、できることは変わりません。

- 受信メッセージの origin は **完全一致**で検証します (`endsWith("youtube.com")` のような判定は `https://evil-youtube.com` を通してしまいます)。送信先も `"*"` ではなく埋め込みの host 固定です。
- **再生位置が読めないときはボタンを押させません。** 読めないのに 0 秒として保存すると「押したら全 pull が動画の先頭に飛ぶ」状態になり、しかも原因が分かりません。応答が来るまでは「待っています」、送り直しても来なければ「読み取れません (手入力してください)」と出します。**折りたたむと iframe は破棄される**ので、読み取り済みの秒数もそこで捨てます — 残していると開き直した直後 (プレーヤーは 0 秒) に前の位置のまま押せてしまい、見ている位置と違うオフセットを書き込みます (マージ前レビューで検出)。
- handshake は 1 回では取りこぼす (プレーヤーの準備前に届くと無視される) ので、最初の応答が来るまで送り直します。応答の有無は state ではなく ref で持っています — state にすると再生中 (毎秒数回の `infoDelivery`) のたびに effect が張り替わります。
- iframe の `src` には**開いた時点の推定位置**を入れて固定しています。live な値を入れるとオフセットを 1 秒動かすたびに `src` が変わって再読込 (再生位置も失う) になります。

**検証**: `scripts/check-video-sync.mjs` に 58 ケース (順算と逆算の往復 / 前半を録っていない動画で負のオフセットになること / 丸め / 範囲 / origin の偽装 / 埋め込み URL のパラメータ / プレーヤーからの時刻の取り出しは「取れなければ必ず null」)。

## 🧾 マクロ / ウェイマーク / 募集文をコードブロック表示に (12px 等幅 / マクロは 15 行の上限を表示)

**狙い**: 調査ノート第 4 回 8-3 UI-6。マクロ / ウェイマーク (markercode) / ストラテジーボード共有コード / 募集文は、いずれも「そのままコピーして貼る」文字列です。これまでは素の `<pre>` を 11px 等幅で出していて、どこからどこまでが本文なのか (前後の UI との境目) が曖昧で、11px は**貼る文字列を読み取るには小さい**状態でした。

**実装**: 枠 + ヘッダー + 12px 等幅のコードブロックに揃えました (`src/components/portal/code-block.tsx`)。

- **マクロは行数を出します。** FF14 のマクロは **15 行まで**で、超えるとゲーム内に貼れません。これまでは「コピーしてゲームに戻ってから気付く」しかなかったので、`12 / 15 行` を出して超過は色を変えます。
- 数え方は**末尾の空行だけを落として物理行を数えます**。末尾の改行 (エディタが付ける) を 1 行と数えると 15 行のマクロが「16 行」と出て直しようのない警告になりますが、**途中の空行は 1 スロットを消費する**ので数えます (当初は途中も除いていましたが、それだと空行 1 つ入りの 16 行が「15/15」で通り、貼ると最後の行が落ちます — マージ前レビューで検出して直しました)。
- コピーボタンは**増やしていません**。マクロ / ウェイマークの行は折りたたんだままコピーできるボタンを既に行ヘッダーに持っているので、ブロック側にも常時置くと同じ操作が 2 つ並びます (必要な呼び出しだけ出せるようにしてあります)。
- 「ロール別ブロック」は入れていません。マクロ本文は自由記述で、ロールの区切りを機械が知る手段がありません (構造化入力が前提の UI-4 側の話になります)。

**検証**: `scripts/check-macro-lines.mjs` に 18 ケース (末尾改行 / 途中の空行 / 全角スペース / CRLF / 15 行ちょうど / 16 行 / 空行入りの 16 物理行)。

## 🚦 日付メモに重要度 (要対応 / 注意 / 参考) を付けて並び替え・絞り込み

**狙い**: 調査ノート第 4 回 8-3 UI-3 (出典 xivanalysis の Suggestions)。日付メモが平坦な時系列リストで、「今夜必ず直すこと」と「参考情報」が同じ見た目で並んでいました。

**実装**: メモに 3 段階のラベルを付けられるようにし、重要度順に並べ替えて表示します。

| ラベル | 記号 | 色 |
|---|---|---|
| 要対応 | ↑ | 赤 |
| 注意 | → | 橙 |
| 参考 | ↓ | 青 |

- **既定は「なし」(未設定) です。** 既存のメモに後から「注意」を割り当てると、ただの連絡が全部注意色で並んで色の意味が薄れます。重要度は付けたい人が付けるものにして、付いていないメモは今までと同じ見た目のままにしました。
- 並び順は **要対応 → 注意 → 未設定 → 参考**。同じ段階の中は従来どおり時系列です (安定ソート)。参考を未設定より後ろにするのは「参考 = 意識的に下げた」印なので、未設定 (まだ判断していない) より低く扱ってよいからです。
- **「参考 n 件を隠す」トグル**を付けました。件数を出すので、隠していることが分かります (黙って消すと「書いたのに無い」になります)。
- ラベルは**色・記号・文字の 3 つ**を必ず出します (調査ノートの注意書き)。赤緑の色覚多様性で読めない / モノクロで消える状態を作らないためです。練習ログの熱量色 (UI-12 の 5 段階) とは役割が違うので混ぜていません — あちらは「達成度」、こちらは「対応の優先度」です。

**スキーマ**: `schedule_session_memos.severity`。既定 `'none'`、CHECK は 4 値。想定外の値は既定へ丸めてから CHECK を張ります (既存の `text_sane` と同じ順)。列が無い状態の応答でも落ちないよう、読み出し側で既定へ倒します。

**検証**: `scripts/check-memo-severity.mjs` に 28 ケース (未知の値を「注意」に寄せないこと / 並び順 / 安定ソート / 入力を変更しないこと / 参考の絞り込みで未設定を隠さないこと / 色と記号)。

## 👥 予定表の各行に出欠の内訳チップを追加 (何人 OK / 未回答何人)

**狙い**: 調査ノート第 4 回 8-3 UI-9。予定表は「1 列 = 1 人」なので行を見れば 8 人分の記号は並びますが、「何人 OK で誰が未回答か」は **8 列を目で追う**必要がありました。

**実装**: 行のアイコン列に `6/8 +2?` のようなチップを 1 つ足しました (参加見込み / 必要人数、`+n?` は未回答の数)。

- **出す情報を絞っています。** 5 種類の記号を全部並べると行が横に伸びて、表そのものより読みにくくなります。内訳と**未回答者の名前は hover** に入れました — 誰が未回答かは催促するときにだけ必要な情報で、常時見せると監視感が出ます (W-27 の既読管理で「誰が未読かは管理者のみ」にしたのと同じ考え)。
- 参加見込みは **参加可 + 遅刻**です。8 人揃うかの判断では遅れて来る人も頭数に入ります (W-13 で遅刻を構造化したときと同じ扱い)。未定は入れません。
- 色は 3 段階で、**未回答が残っている間は「人数不足」を赤で出しません**。まだ回答が来ていない段階で「足りない」と出すと、実際には埋まる日に中止の判断を促してしまいます (調査ノート 6-2 の「完全自動中止は余地を潰す」と同じ考え)。
- 記号は設定画面で自由に編集できるので、**辞書外の記号を「参加可」に寄せません** (人数を偽ると成立判断が狂うため)。ただし逆に、**辞書が参加可と言っている記号は必ず数えます** — カスタム凡例の「全 / 昼 / 夜 / 早」(= 全日 / 昼 / 夜 / 早朝 参加可) を「その他」に落としていたため、その凡例を使っている固定では全員回答済みでも `0/8` と出ていました (マージ前レビューで検出して直しました)。昼 / 夜 / 早は時間帯つきの参加可として参加見込みに入れ、hover の内訳にも出します。
- 必要人数は 8 人固定です (8 人レイド)。ロスターが補欠込みで 9 人以上のときに「9 人揃わないと成立しない」と出さないよう上限として使います。

**検証**: `scripts/check-attendance-summary.mjs` に 27 ケース (記号の分類 / カスタム記号を参加可に寄せないこと / 未回答判定のゆらぎ 5 種 / 表に無い人を数えないこと / 段階の優先順位)。

### 🗂 あわせて UI-15 (ボスアイコン列で進行度) は見送りとした

調査ノート第 4 回 8-3 で挙がっていましたが、**見送り**にしました。ボスアイコンの素材が無く、用意する手段がどれも引っかかります — 公式素材の再配布は利用規約の範囲外で、第三者 API 経由でもゲームアセットの再配布であることは変わりません。自作アイコンはティアごとに描き続ける保守が発生し、「小」のコストに収まりません。

一方この項目の狙い (ティア見出しで討伐の進み具合が分かる) は、既存のステータスバッジと 2.16 で入れた進行トレンド / チーム実績バッジで実質的に足りています。判断の経緯と再検討の条件 (権利上問題なく使えるアイコンセットが現れたとき) は調査ノートに追記しました。

## 🔎 レポートの自動発見を追加 (貼られた URL のみ / guild から / 自分のアカウントから の 3 択)

**狙い**: 調査ノート第 4 回 W-5 (★★)。portal はこれまで**貼られた URL からしかレポートを知りませんでした** (動画の FFLogs URL、日付メモの URL、活動日に紐づけた URL)。誰かが URL を貼るまで存在を知らないので、貼り忘れたセッションは練習ログに出てきません。

**3 択にした理由**: どこを見るのが正しいかは**固定の運用で変わります**。

| 発見元 | 何を見るか | 向いている固定 |
|---|---|---|
| **貼られた URL のみ** (既定) | 従来どおり | URL を貼る運用が回っている固定 |
| **guild から自動** | FFLogs 上の static (guild) のレポート一覧 | Uploader で guild を選んで上げている固定 (guild ID の入力が必要) |
| **自分のアカウントから自動** | 接続した FFLogs アカウントのレポート一覧 | guild を作っていない固定 (計測担当が個人アカウントで上げている) |

どちらかに決め打つと片方の固定では機能しません。設定 → 練習ログの通知の「レポートの発見元」で選べます。

- **既定は従来のまま (自動発見しません)。** guild で討滅やレイド以外も回している固定では、無関係なレポートが取り込み候補に大量に入ります。1 回の取り込み枠 (40 件) を食い、未分類レポートの一覧も伸びて診断が読みにくくなるので、**明示的に選んだときだけ**動きます。
- **1 回の同期で直近 1 ページ (25 件) だけ**見ます。取り込み枠より多く発見してもその回では取り込めず、次回また同じ一覧の先頭を引くので、ページを跨いで漁る意味がありません (過去分をまとめて入れたいときは URL 貼り付けの導線があります)。
- **除外したレポートは復活しません。** 「ログ削除」で除外した report は自動発見側でも弾きます (ここを忘れると毎回復活します)。
- 発見したレポートの**振り分けは従来どおり** zone / encounter から行います (URL 貼り付けと同じ扱い)。カテゴリと日付は付けずに候補へ入れるだけです。
- 「設定したのに増えない」の原因が分かるよう、**同期結果に理由を出します** (guild ID 未設定 / 新しいレポートなし / API エラー)。設定画面側でも guild ID が空のときに警告を出します。
- **「自分のアカウントから」は Public のレポートしか拾えません。** FFLogs の API 仕様で、本人の OAuth でも Unlisted / Private は一覧に出てきません (2026-06 の実測で確定済み)。`docs/guides/log-runner.md` は Unlisted 運用を推奨しているので、その固定は guild か従来の URL 貼り付けを使ってください。guild 経由で Unlisted が取れるかは API の挙動が公開されていないため、**本番の guild での確認をお願いします**。
- **「guild から自動」には前提が 2 つあります** (2026-09-07 の実機報告を受けて API 仕様を調べ直した結果、リリース後に判明):
  1. **レポートが guild に属している必要があります。** v2 の `Report.guild` は「null ならユーザーの個人ログにアップロードされたレポート」と定義され、`reports(userID:)` も「その user の**個人**ログ」を返すと明記されています。guild への所属は**アップロード時に決まる**ので、Uploader でアップロード先に guild を選んでいなければ `guildID` で引いても 0 件です。**既存の履歴は後から guild に移せません** — guild モードはこれから上げる分にしか効きません。
  2. **一覧が Unlisted を返すかは未確認のままです。** v2 のスキーマは `report(code:)` にだけ `allowUnlisted` を持ち、一覧の `reports()` には**可視性の引数がありません**。実測できている経路 (userID / フィルタなし) はすべて Public のみでした。スキーマの形からは「一覧は公開インデックス、個別取得が限定共有の経路」と読め、Unlisted は返らない可能性が高いものの断定はできません。

**「練習日のみ 2 分ポーリング」は入れていません**。既存の日次 cron と手動同期に相乗りさせる (1 回の同期で 1 リクエスト追加) 形にとどめました — ライブ中に前プルのワイプ原因が出る価値は認めますが、専用のポーリング経路を持つと「練習日の判定」「多重起動の防止」「レート制限」の 3 つを新たに抱えます。まず発見経路が実運用で機能するかを確かめてから判断します。

**検証**: `scripts/check-fflogs-report-source.mjs` に 25 ケース (未設定 / 未知の値が必ず従来の挙動に倒れること、guild を選んで guild ID が空 / OAuth 未接続の検出)。ドキュメントは `docs/guides/log-runner.md` に発見経路の節を追加しました。

### 🔍 マージ前レビューで見つけて直したもの (2026-09-07)

この 5 件を main に入れる前に、差分 (38 ファイル / +2,739 行) を 6 観点で並列レビューし、各指摘を 2 つのレンズ (反証 / 到達可能性) で検証しました。21 件の指摘のうち重複を除いた 6 件が実質的で、全部この版に反映してあります。

| 直したもの | 症状 |
|---|---|
| W-11 折りたたみで読み取り値が残る | 開き直した直後 (プレーヤーは 0 秒) に前の位置のまま押せ、見ている位置と違うオフセットを保存できた |
| W-11 基準 pull の「#N」 | レポート内連番だったので、同じ日に複数レポートがあると pull 行の番号と食い違った → 時刻表示に変更 |
| UI-9 カスタム凡例の記号 | 「全 / 昼 / 夜 / 早」を「その他」に落としていたため、その凡例の固定では全員回答済みでも `0/8` と表示された |
| UI-6 マクロの行数 | 途中の空行を数えていなかったので、空行 1 つ入りの 16 行が「15/15」で通り、貼ると最後の行が落ちた |
| W-5 guild ID の説明文 | 「現時点では取り込みに使われません」のまま (この版で使われるようになったのに) |
| W-5 発見理由が出ない | 候補が 1 件も無いとき、まさに理由が要る場面で `discoveryNote` が捨てられていた |
| UI-3 schema 適用前の窓 | 重要度が既定でも列を送っていたため、schema 適用とデプロイが前後した数分間メモの投稿自体が失敗しうる形だった → 既定なら送らない |

`scripts/check-ja-strings.mjs` の baseline は 116 件 / 24 ファイル → **120 件 / 25 ファイル**になりました (UI-9 で character-sheets の凡例記号 4 つを突き合わせるようになったぶん。分類 A = DB の値 / 突き合わせキー)。この検査が新しい日本語を実際に捕まえたので、逆戻り防止としては機能しています。

**あわせて既存の穴を 1 つ塞ぎました**: `fflogs_report_blocklist` (取り込み除外リスト、2026-08-30 追加) が **30 テーブル中ただ 1 つ RLS 無効**でした。schema.sql に明示 GRANT が無く Supabase の default privileges で anon / authenticated が DML 権限を持つため、誰でも「任意のレポートを恒久的に取り込み不可にする」「admin が除外したレポートを復活させる」ことができる状態でした。W-5 はこの表を「発見しても取り込まない」ゲートとして参照するので、この版で 7 章の汎用ポリシーループに載せて **SELECT 開放 / 書き込みは admin** にしています (policy を張らない形にはできません — 同期処理と「ログ削除」がユーザースコープのクライアントでこの表を読み書きしているため、policy 0 本では除外リストが黙って空になります)。

### 🎯 オフセットの値の意味を実時刻で出す (2026-09-07 実機報告)

実機で「動画に紐づけたログと開始位置が合わない (オフセットは 22 秒)」という報告があり、2 点測っていただいて決着しました。

| 測った pull | リンクが示す位置 | 実際 | ずれ |
|---|---|---|---|
| #7 | 56:49 | 55:05 | 104 秒 |
| #11 (クリア) | 1:34:30 | 1:32:46 | 104 秒 |

**2 点とも同じ 104 秒 = 定数ずれ**でした (最初は 1 点だけから「動画に欠けがある」と推測しましたが、2 点目で比例ずれもカットも否定されました)。正しい値は **−82 秒**で、それだと #7 = 55:05 / #11 = 1:32:46 と両方一致します。

**原因は基準にした pull の取り違え**です。その動画は**最初の pull が映っておらず** (録画開始が 82 秒後)、0:22 で始まっていたのは **2 本目の pull** (最初の pull の 103 秒後) でした。それを 1 本目と見て 22 を入れたため、全リンクが一律 104 秒遅い位置を指していました。同じレポートに紐づいたもう 1 本の動画は最初から映っていたので正しく、**2 本のリンクの差は全 pull で正確に 23 秒** (= オフセットの差) — 計算式自体は正しく動いていました。

**直したのは「数字だけでは判断できない」ことの方です。** オフセットの入力欄の下に、その値が何を意味するかを実時刻で出すようにしました。

- 正の値: 「この値は『最初の pull (22:08) が動画の 0:22』という意味です」
- 負の値: 「この値は『動画が最初の pull (22:08) の 1:22 後から始まる』という意味です — **最初の pull は映っていません**」(琥珀色)
- 併せて「動画に最初に映る pull: #2 (22:10) = 動画の 0:21」

今回の取り違えは、この 2 行があれば入力した時点で見えました。W-11 の「動画を見ながら合わせる」で基準の pull を選べば、そもそも取り違えずに済みます (この動画なら基準を #2 にして 0:22 で押すと −81 が入ります)。

**検証**: `scripts/check-video-sync.mjs` に実機の値をそのまま再現したケースを追加 (誤った 22 では「最初の pull が映っている」ことになってしまう / 正しい −82 では最初に映るのが #2 になり #7 が 55:05 に来る)。

## 🏁 練習ログ: 零式の層ごとに「初討伐」を表示 (その層の pull 数 + 通算 / 所要時間 / 日時)

**狙い**: 実機要望 (`docs/backlog.md` の L-1)。絶には「各フェーズへの初到達」(`P2 8:38 (4 pull)` / `P3 2:54:43 (78 pull)`) がありますが、零式には層ごとの節目がありませんでした。チーム実績バッジの「初討伐」も**ティア全体で 1 つだけ**なので、「3 層をいつ抜けたか」がどこにも残りません。層フィルタ (1層 / 2層 / 3層 / 4層前半 / 4層後半) があるのだから層ごとに出す、というのがこの変更です。

**実装**: ワイプ原因の右隣 (2 カラムの 2 枠目 — 絶ではフェーズ滞在時間が占める枠) に「各層の初討伐」カードを足しました。1 項目が `4層後半 1:23:45 246 pull 通算 519` で、hover に初討伐の日時が出ます。

- **データは既存の `fflogs_fights` (`kill` / `encounter_id`) だけ**から出しており、FFLogs への新しい取得はありません。集計は純関数 `floorFirstClears` (`src/lib/fflogs-session.ts`)、表示は `src/components/portal/logs/floor-clear-card.tsx` です。
- **所要時間はその層だけの累計戦闘時間**です (討伐した pull を含む)。絶の「初到達まで」と同じ意味の値で、母数がフェーズではなく層になります。
- **pull 数は「その層 + 通算」を併記**しました。零式は**層を行き来する** (4 層で詰まっている間に 1〜3 層を消化で回す) ので、前に出すのは「その層で何本目か」の方が実態に合います。ティア開始からの通算は絶との一貫性のために後ろへ小さく添えています。
- **未討伐の層は出しません**。「まだ倒していない」以上の情報が無く、層フィルタのチップで足ります。
- **明細が打ち切られている (20,000 pull 超) カテゴリでは出しません**。明細は新しい順に切られるので、古い pull が落ちると初討伐も pull 数も過小になります。サマリータイルの「初クリア」を同じ理由で伏せているのに合わせました。
- 4 層前半 / 後半は**別の層として数えます** (色も rose / fuchsia で分けている既存の扱いに揃えました)。

**幅の検証 (併記が破綻しないか)**: 実機の第 1 希望が併記、「UI が崩れるなら『その層の pull だけ』に落とす」という条件付きだったので、Chromium で実測して判断しました (JetBrains Mono 400 / 12px + CJK フォールバック)。

| 測ったもの | 幅 |
|---|---|
| `4層後半 1:23:45 246 pull 通算 519` | 216 px |
| 英語表示 `F4b 1:23:45 246 pulls total 519` | 214 px |
| 通算を落とした場合 | 159 px |
| 絶の `P3 2:54:43 (78 pull)` / `P7` の最長ケース | 138 / 159 px |

カード内幅は同じ条件で **320px 端末 262px / 640px 266px (2 カラムへの切り替わりが最も狭い) / 1024px 以上 458px**。216px はどれにも収まるので**併記のまま**にしました。1 行に並ぶ数も 640px で 1 個 / 1024px で 2 個と、絶の「初到達まで」(同じ幅で 1 個 / 3 個) とほぼ同じ密度です。余裕は最も狭い 640px で 50px しかないので、**値を 1 つ増やすときは通算を hover へ退避**してください (hover の文言は既に日時と通算を持っています)。保険として項目自体を `flex-wrap` にしてあり、想定より狭い端末や 4 桁の pull 数では「通算 N」だけが次行へ落ちます。

**検証**: `scripts/check-fflogs-session.mjs` に 14 ケースを追加 (層を行き来する実データ相当の並びで「その層の pull 数」と「通算」が食い違うこと / 所要時間がその層だけの合計であること / DB から来る降順でも同じ結果になること / 毎週の消化で初討伐が上書きされないこと / 4 層前半・後半を混ぜないこと / NaN 行を数えないこと)。`tsc --noEmit` / `eslint` / `next build` / `scripts/check-*.mjs` 全 34 本。

### 👥 詳細ログ (過去分) では出欠の内訳チップを出さない

`docs/backlog.md` の L-2。UI-9 のチップが「PAST 詳細ログ (出欠表)」の各行にも出ていましたが、過去分では**外しました**。

チップの価値は「まだ埋まっていない予定の成立判断と催促」にあります。過去行は回答が確定していて、同じ行に 1 人 1 列で記号が並ぶので情報が重複し、幅だけを食っていました。PAST 詳細ログは upcoming とは別の table なので、縦揃えのための**プレースホルダごと**落として日程列を詰めてあります (縦揃えは table 単位でしか要りません)。upcoming 行のチップはそのままです。

## 🔗 練習ログ: 層 / フェーズの絞り込みを 1 段のセグメントに + 選択を URL で共有 / 軽減表の層タブをシート表示でも使えるように

**狙い**: 調査ノート第 4 回 8-3 UI-5「フェーズ・セグメント + URL 状態同期 (`?phase=2` を Discord に貼れる)」と、第 2 回 UI 監査の残課題 4「軽減表の層タブが card ビュー限定」。

### 練習ログの区間フィルタ

2026-08-30 に入れた層フィルタは独立したチップが横に並ぶ形で、押せることは分かっても「1 つだけ選ぶ排他の選択」には見えていませんでした。調査ノートの「ネストタブが深くなる → **セグメントコントロールで 1 段に**」に合わせ、枠を 1 つ持つ連結したコントロールにしました。

- **絶にもフィルタが付きました。** 従来は層モデルのカテゴリにしか絞り込みが無く、絶では 1000 pull が全部並ぶだけでした。同じセグメントでフェーズ (P1〜) を選べます。
- **選択が URL に載ります** (`?floor=4b` / `?phase=2`)。貼られた URL は人が読むので、内部 index (4層後半 = 5) ではなく画面のラベルに寄せた表記にしました。実在しない値は全件に倒します — 照合しないと、閲覧者が URL に書いた任意の値で「どの pull にも一致しない空の一覧」が出ます (軽減表の `?gid=` を登録済みタブに限っているのと同じ考え)。
- URL の更新は `history.replaceState` です。Next のドキュメント (linking-and-navigating の Native History API) が router と同期すると明記している経路で、サーバー往復もページ再取得も起きません — この絞り込みは**すでにブラウザに来ている明細**を間引くだけなので、往復させると押すたびに数百 KB を取り直すことになります。`push` ではなく `replace` なのは、フィルタが「移動」ではなく「今の画面の状態」だからです (連打したぶん戻るボタンを押す羽目にならない)。
- **見出しの集計値を直しました。** 以前は `day.fights` だけを差し替えていたので、「4層だけ」に絞っても日の見出しは「30 pull / 戦闘 2:10 / 残 12%」と全層の値のままで、下に並ぶ 12 本と食い違っていました。絞った pull で `summarize` に通し直します。

### 軽減表の層タブ

層タブ (2026-08-30) は card ビューの中にしか無く、シート (iframe) 表示に切り替えた瞬間に消えていました。タブを表示形式の外に出し、iframe 側の埋め込み URL も gid を差し替えます (差し替えないとタブを押しても iframe は最初のワークシートのまま)。published 形は `?gid=` + `single=true` (付けないと Google 側のタブバーが出て portal のタブと二重になる)、通常の共有形は `#gid=`。docs.google.com 以外・gid が数字でない・URL として壊れている場合は**素通し**します — 判定できない URL を組み替えると、いま動いている埋め込みを壊しうるためです。

CSV 取得に失敗したカテゴリでもタブが出るようになりました (タブ一覧は CSV とは別経路で取っているため)。

**検証**: `scripts/check-logs-filter-url.mjs` を新設 (32 ケース: 内部 index が URL に漏れないこと / 候補に無い値が null に倒れること / 「全層」でキーごと消えて他のキーは残ること)。`scripts/check-mitigation-cards.mjs` に gid 差し替えの 10 ケースを追加。

## 🟩 練習ログ: 日ごとの「プル・ボックス列」(1 pull = 1 小箱) / コンテンツカードに直近 8 週の到達度スパークライン

**狙い**: 調査ノート第 4 回 8-3 UI-1 / UI-2。

### UI-1 プル・ボックス列

元ネタは WoWAnalyzer の `PerformanceBoxRow`。現行の「1 pull = 1 行」は 1 日 30 pull で 30 行に伸び、日を開かないと中身が見えず、開くと縦に長すぎて「その日どう転がったか」が読めませんでした。日の見出しの直下に 1 pull = 1 小箱の列を出します。箱を押すとその日を開いて該当 pull までスクロールします。

- **色だけで意味を伝えません** (WCAG 1.4.1、調査ノートのリスク欄)。箱の中に到達区間の番号を必ず書きます (零式なら層番号、絶ならフェーズ番号)。討伐は `✓`。hover / 読み上げには時刻・区間・残 HP%・戦闘時間・死亡数が入ります。
- そのため **区間モデルを持つコンテンツでしか描きません**。討滅など区間が無いコンテンツでは箱に書ける略号が無く、色だけの列になってしまいます。
- **箱 20px + 間隔 4px = ピッチ 24px** (WCAG 2.5.8 の間隔例外)。24px 角にすると 1 日 30 pull がスマホで 4 行になり「1 行で俯瞰」の狙いが消えます。**大きさと間隔は対で変えてください。**

### UI-2 コンテンツカードのスパークライン

「今どこまで来たか」をタブを開かずに一覧で出します。練習ログ上部の「総 pull / 最深到達」タイルは既にあったので、残っていたのはカード側でした。

- 集計は **DB 側の新 RPC `category_progress_by_day`** (schema.sql 13c-3 節)。到達度は「突破済み区間数 + 現在区間の削り」なのでティア全体の区間数が要る一方、カードに出すのは直近 8 週だけです。JS でやると /category に全 pull を転送することになるので (13c-2 と同じ問題)、日 × カテゴリの数十行に縮約してから受け取ります。
- ティアの判定は「pull 数が最大の encounter から ±7」で、TS 側 `buildFloorMap` の「幅 8 の窓でクラスタ」の近似です。厳密な判定は従来どおり練習ログ画面が行います (カードは要約)。
- 残 HP% は **その日の最深区間の中**で最小を採ります。層を跨ぐと消化で下層を倒した日が必ず「残 0%」になります (2026-08-28 実機報告と同じ罠)。
- 線色は 5 段階スケール、**数値を必ず併記**します (1.4.11 の 3:1 と色覚多様性)。
- **RPC が無い DB (schema.sql を再実行していないデプロイ) では線が出ないだけ**で、他の機能に影響はありません。

**SQL の検証**: ローカルの Postgres 16 に本関数だけを載せ、合成データで実行して TS 側と突き合わせました (混入 encounter の除外 / 残 HP% が最深区間の中の最小になること / 最終 encounter の討伐だけが has_clear になること / `session_date` が NULL の行が `start_ms` の JST 暦日にまとまること / `p_days` の clamp)。**ここで 1 件見つけて直しています**: 区間数を「出現した encounter の個数」で数えていたため、1 層と 4 層しか回していない期間に 2 区間となり、同じ 4 層の到達度がカードと練習ログ画面で食い違っていました (encounter 100 / 103 / 104 のデータで TS の `buildFloorMap` が 5、SQL が 3)。encounter ID の**幅** (max − min + 1) で数える TS と同じ式に直してあります。

### 到達度の計算式を 1 つに

`progressTimeline` の中にあった式を `progressValue()` として切り出し、日ごとのバー / 進行トレンド / プル箱 / カードのスパークラインが全部これを通るようにしました。式が分かれると同じ日のバーと箱の色が食い違います。

**検証**: `scripts/check-fflogs-progress.mjs` を新設 (21 ケース: 突破済み区間 + 削りの式 / 区間が取れない pull を最も浅い区間に倒すこと / 層モデルで最終層以外の討伐を 100 にしないこと — 消化で 1 層を倒した pull が 100 になると箱列が緑に埋まる / フェーズモデル)。

## 🗓 定期枠 (毎週 X 曜) を設定できるように / 期間 + 曜日で候補日を一括生成 / 枠から外れた日に「臨時」「今回だけ」

**狙い**: 調査ノート第 4 回 7-B W-15「定期枠テンプレ + 例外 (毎週 X 曜 21:00 を期間 + 曜日で一括生成、「今週スキップ」「今回だけ 22:00」)」。

**いま何が起きていたか**: native モードは 2026-05-12 (TODO #81) から当月の**全日付**を候補日として auto-insert しています。「毎週の候補日入力が消える」という W-15 の狙いは一応満たされていましたが、代わりに活動しない曜日まで全部並びます。週 3 日の固定なら 1 か月 30 行のうち 21 行が無関係で、出欠表が縦に伸びるぶんだけ「次にどこを埋めればいいか」が読みにくくなっていました。

**入れたもの**:

1. **定期枠** (設定 → Recurring Slot): 毎週どの曜日に活動するかを選ぶと、候補日の自動追加が**その曜日だけ**になります。
2. **候補日ダイアログの「繰り返し」**: 期間 + 曜日 + 時刻で一括生成。曜日は定期枠から最初から入ります。既にある日付は飛ばします (上書きしません)。件数は送信前にその場で数えて出すので、期間と曜日の指定ミスにその場で気付けます。
3. **例外の印**: 定期枠に無い曜日は「臨時」、枠の曜日で時刻を変えた日は「今回だけ」。調査ノートのデメリット欄**「例外 UI が無いと定期化は逆効果」**への回答です — 定期枠を決めた瞬間に「いつもどおり」が既定になるぶん、そうでない日が見て分かる必要があります。

**決めたこと**:

- **曜日を 1 つも選ばない = 定期枠なし**で、従来どおり全日が候補になります。「0 件の枠」と解釈して候補日を作らない挙動にすると、設定をいじった瞬間に予定表が空になり、候補日が無いので戻す導線も消えます。既存デプロイの挙動も変わりません。
- **枠は曜日だけを持ち、時刻は既存の default を使います。** placeholder の `raw_date` は時刻を焼き込む形で、default 変更時の遡及更新 (schema 13d 節) が「`start_time` / `end_time` が両方 NULL の行」だけを対象にしています。枠ごとに時刻を持たせると placeholder が NOT NULL になり、(a) default 変更に追従しなくなり (b) 画面上は日個別の override (=「今回だけ」) と見分けが付かなくなります。曜日ごとに時刻を変えたいときは一括生成で明示して作ってください。
- **保存しても既にある候補日は消えません。** 消すとその日の出欠やメモまで巻き添えになります。要らない日は従来どおり中止にしてください。
- 「今週スキップ」と「今回だけ 22:00」の**操作自体は既にあります** (status トグルと時刻編集 popover)。W-15 で足したのは、それが例外だと分かる印です。
- 予定表の曜日は `raw_date` に焼き込まれた `(火)` から取ります。`parsed_date` の `getDay()` は閲覧者のタイムゾーンで動きうるためです (JST 21:00 開始を UTC で見ると前日になる)。

**検証**: `scripts/check-recurring-frames.mjs` を新設 (40 ケース: 枠が空のとき全日に倒れること / 期間の展開がタイムゾーンに依存しないこと / 存在しない日付 (2 月 30 日) を弾くこと / 上限で打ち切ったことが伝わること / 例外の判定)。

## 🐛 設定ダイアログの操作が無言でできなくなっていたのを修正 (「レポートの発見元が変更できない」の原因)

**狙い**: 実機報告「設定 → 練習ログの通知にある『レポートの発見元』で 3 択のどれも選び直せない」(L-3)。

**原因**: `"use server"` のファイルが **async 関数以外を export** していました (`native-schedule-actions.ts` の `BULK_CANDIDATE_MAX`、定期枠の実装で入ったもの)。Next.js はこれを `invalid-use-server-value` として**モジュール評価の時点で throw** するため、そのファイルを含む Server Action の束が丸ごと読み込めなくなり、**トップと練習ログのページで設定の読み書きが全部失敗**していました。本番の runtime ログにも 24 件記録されていました (初出 2026-09-08 02:24 UTC、定期枠のデプロイ直後)。CI の ESLint / 型チェックも本番ビルドも通るため、実行時まで表に出ませんでした。参照は 1 箇所も無かったので落としました。

**なぜ画面から原因が分からなかったか**: 通知の節の初期読み込みが `Promise.all([...]).then(...)` で **`.catch` を持っておらず**、`loaded` を true にするのは `.then` の中だけでした。節のコントロールは全部 `disabled={pending || !loaded}` なので、action が reject すると**節ごと無言で押せなくなる**構造でした。トーストも出ないので「壊れている」ことすら分かりません。

**入れたもの**: 読み込みが失敗したら理由をその場に赤枠で出し、「読み直す」ボタンで再試行できるようにしました。`ok: false` (ADMIN ロール不足など) も同じ扱いで、**表示されている値が実際の設定なのか既定値なのか**が読めるようにしています。原因の方は直しましたが、この「無言で固まる」構造そのものが欠陥なので両方入れています。

**検証**: dev preview で、修正前は赤枠に `invalid-use-server-value` が出てチェックボックスが全部 disabled になること、修正後はバナーが消えて 5 つのコントロールすべてが操作可能になることを実測。本番側は Vercel の runtime エラー集計で発生箇所 (`native-schedule-actions.ts` の module evaluation) を確認。

## 🔀 練習ログ: レポートの自動発見を「経路ごとの ON/OFF」に (guild と自分のアカウントを同時に ON できる)

**狙い**: 実機要望「貼られた URL のみでなく、自分のアカウントから自動や guild から自動は手動 ON/OFF で管理したい」(L-4)。

**いま何が起きていたか**: 発見元が `links` / `guild` / `user` の**排他 3 択**で、guild と自分のアカウントを**両方**見ることができませんでした。実際には「guild にも上げているし、個人アカウントの分もある」が普通で、排他にする理由がありませんでした。

**入れたもの**:

- **貼られた URL は常時 ON** にして設定から外しました (portal の土台の経路で、切ると台帳が空になります)。その上に「guild から自動」「自分のアカウントから自動」を**独立したトグル**で並べています。
- 条件が足りていない経路は**その経路の行に**警告が出ます (guild を ON にして guild ID が空のとき)。両方 ON のときに「どちらの設定を直せばいいか」が名指しできる形です。同期結果のトーストも経路ごとの理由を並べます。
- 両方 ON なら 2 経路の結果をマージして重複排除し、1 回の取り込み枠 (25 件) を分け合います。片方の一覧だけで枠を埋めると、もう片方が毎回 0 件になるためです。

**決めたこと**:

- 保存値は**同じキーの CSV** にして後方互換を取りました: `""` (自動発見なし) / `"guild"` / `"user"` / `"guild,user"`。旧値の `links` は空集合として読むので、**旧 3 択のどの値からも移行で設定が変わりません**。
- **⚠ Unlisted 運用ではどちらの経路も実質 0 件**です (API の一覧に可視性の引数が無く、実測できた経路はすべて Public のみ)。UI を直しても「0 件」は変わらないので、**期待値を画面に併記**しました。ここを消すと「設定したのに増えない」の問い合わせが戻ります。

**検証**: `scripts/check-fflogs-report-source.mjs` を集合モデル向けに書き直し (54 ケース: 旧 3 択からの移行で設定が変わらないこと / 未知の値が「自動発見なし」に倒れること / 保存形が並びまで一意なこと / ON にした経路ごとに不足条件を返すこと / 2 経路で枠を分け合うこと)。dev preview では DB を触らずに helper を一時的に固定して、集合の読み込み・経路ごとの警告・保存失敗時の巻き戻しを実測。

## 🗂 設定: 長い節を折りたたみ式に (開閉は端末ごとに記憶)

**狙い**: 実機要望「Settings が長くなってきたので、折り畳めそうなものは折り畳めるようにする」「練習ログの通知を折り畳めるようにしたい」。

**いま何が起きていたか**: 節が 15 を超えて、目的の節まで縦に長くスクロールする状態でした。折りたたみ自体は既に 3 箇所 (Danger Zone / 出欠の催促 / FFLogs) にありましたが、**それぞれが同じ `<details>` を手で書き写していました**。

**入れたもの**: 共通の `CollapsibleSection` を 1 つ作り、長い 11 節をそこに寄せました (練習ログの通知 / Discord 通知 / メンバー / 凡例 CSV / 中止行 / 過去ログ取り込み / FFLogs Sync / メンテナンス日程 / 週制限の消化ウィンドウ / 出欠の催促 / Danger Zone)。**既定は畳んだ状態**で、開閉は**端末ごとに** localStorage が覚えます。畳んだままでも運用状態が読めるよう、通知系の見出しには ON/OFF (通知は ON の件数) を出しています。

**決めたこと**:

- **畳まないまま残した節**: モードの選択 (畳むと下の節が出てくる理由が読めなくなる) / URL / 既定時刻 / 定期枠 / 自動成立 / 更新履歴のフッタ。短くて、開閉の手間が中身より大きいものです。
- 開閉は **localStorage** です。共有設定ではないので DB には持ちません (持つと他の人の画面でも畳まれます)。
- 状態は `useSyncExternalStore` で持っています。`useState` の初期値で localStorage を読むとハイドレーションが壊れ、`useEffect` + `setState` は `react-hooks/set-state-in-effect` の error で CI が落ちます。**初回描画が必ず畳んだ状態**になることが `getServerSnapshot` で構造的に保証される形にしました。真の値はモジュール内の Map に持ち、localStorage は永続化層としてだけ使います (書き込みが拒否される環境で、開いた直後に畳まれるのを防ぐため)。
- 矢印の回転は Tailwind の `group-open/<名前>` ではなく React の状態から出しています。group 名は節ごとに別名が必要で、入れ子の `<details>` (FFLogs 節) があると名前の衝突で外側の開閉に内側の矢印が反応します。

**検証**: dev preview で 7 節 (sync モードで出るもの) がすべて既定で畳まれていること、開いた 1 節だけがフルリロード後も開いたままで他は畳まれたままであること、`portal:settings-open:*` のキーが 1 件だけ立つこと、ハイドレーションエラーが出ないことを実測。

## 🟩 練習ログ: プル・ボックス列の仕上げ (色を弱める / 討伐マークの縦揃え / 飛んだ先を光らせる)

**狙い**: 実機報告 (L-5、UI-1 を入れた直後)。

1. **層の識別色が強すぎた。** 箱が横に 30 個並ぶと画面がその色で埋まり、日の見出しや残 HP% より箱の方が目立っていました。チップ 1 個ぶんの彩度は適切でも、**30 個並べると総量が違う**のが原因です。箱専用のトーン (`PERF_BOX`) を足し、背景の不透明度を半分 (10% → 5%)、文字を 1 段落としました。⚠ 5 段階の意味が読める範囲に留めるのが条件なので、これ以上は薄くしません (`scripts/check-perf-tone.mjs` で「チップより薄いこと」を固定しました)。

2. **討伐マークが中央より下に出ていた。** 原因を実測で特定しました: **`✓` (U+2713) は JetBrains Mono に無く、OS のフォールバックフォントが描いていました。** 同フォントの文字送りを測ると数字と英字は 60 単位で揃うのに `✓` だけ 68.3 単位で、等幅の枠に収まっていません。フォールバック先は OS ごとに違うため**縦位置が閲覧環境ごとに変わる**状態でした (Chromium での実測では 0.13px 上で目視できず、報告環境では下にずれて見えていた)。`place-items-center` では直りません — グリッドが中央に置くのは字形ではなく行ボックスで、その中の字形の位置はフォントのメトリクスが決めるためです。**SVG のチェックに差し替え**、箱の中心との差が 0.00px になりました。

3. **飛んだ先の pull が分からなかった。** 30 行の中へスクロールしても、どれに来たのか見えませんでした。該当行に 1.4 秒のリングを出してフェードで消します。同じ箱を続けて押しても光り直します (真偽値ではなく「何回目の選択か」を持ち、値が変わると要素を作り直してアニメを頭から再生する形)。`prefers-reduced-motion` では動きを外し、可視のまま残します — 「どこに来たか」を示すのが役割なので、消えない方が目的に合います。

**検証**: demo DB に pull データが無いため、合成データでコンポーネントだけを描く一時ページで実測しました (共有 DB には書いていません)。討伐マークの中心ずれ 0.00px、箱の背景 alpha 0.05、ハイライトは対象行に 1 個だけ出て 1.4 秒で opacity 0 まで進み、同じ箱の再押下で currentTime が 0 に戻ることを確認しました。

## 📏 練習ログ: pull 行が 2 行に折り返さないように (DPS はクリア時のみ / 技名は可変幅 / 動画チップを 1 グループに)

**狙い**: 実機報告 (L-6)。26 pull の日を開くと行によって内容が横幅を超えて 2 行になり、行の高さが揃わず一覧性が落ちていました。

**決めたこと**: 折り返しは `<li>` の `flex-wrap` で起きますが、**外すと今度は溢れます**。そこで「どの列を可変にするか」を先に決めました — **可変にできるのは技名 (ワイプ原因) だけ**で、他は桁数かリンクのラベル幅で決まります。

**入れたもの**:

- **PT 合計 DPS はクリア pull だけに出します。** 練習中の pull の DPS は「途中で落ちた分だけ低い」だけで判断材料になりません。クリア pull ではタイムの裏付けになるので残します。列幅の確保も、その日にクリアがある日だけにしました (= ほとんどの日でこの列が消えます)。死亡数は練習 pull でも意味があるので残しています。
- **技名は固定の `max-w` をやめ、残り幅に応じて縮むようにしました。** 狭い画面では隣のリンク群に寄りすぎ、広い画面では逆に余っていました。省略した全文は従来どおり `title` に入ります。
- **動画チップを 1 グループに畳みました。** 1 本 = 1 チップ (各 5.5rem) だと 2 本で折り返しの原因になっていました。FFLogs の 3 ビューと同じ「枠を共有した 1 グループ」にして、1 本目は時刻つき、2 本目以降は**番号だけ** (1.5rem) にしています。どの動画も**同じ pull の同じ瞬間**へ飛ぶので、2 本目以降で知りたいのは時刻ではなく「どの動画か」です。時刻と名前は title に入っています。**隠したリンクはありません** (N 本の幅が 5.5N → 4.75 + 1.5(N-1) rem になりました)。

**検証**: 合成データ 30 行で、DPS 有無 × 動画 0〜4 本の 8 通りすべてで行の高さが 33px に揃うこと (= 全行 1 行) を実測。DPS がクリア行だけに出ていることも確認しました。

⚠ **折り返しの閾値について、初出時に「640px までは 1 行」と書いていたのは誤りです。** その計測は**動画列が 0 本の状態**で行われていました。動画 1 本 + DPS 列ありの実データで測り直すと、この時点の閾値は **約 720px** です (それより狭いと 2 行になります)。この行は下の「動画チップをマークと番号だけにする」でさらに縮み、閾値が 670px 以下へ下がりました。

## 📈 練習ログ: 進行トレンドに到達度の目盛り・期間・凡例を追加 (薄い線と濃い線の意味を明記)

**狙い**: 実機要望「進行トレンドの横軸も欲しい。現状どこまで達したかが一目で分かる」「薄い線の意味が分かりにくい」。

**いま何が起きていたか**: 折れ線は 2 本 (その日の到達度 / そこまでの最高到達) ありましたが、**その説明がコードのコメントにしか無く画面に出ていませんでした**。目盛りも「幅が足りない」という理由で数字を出しておらず、25/50/75% のガイド線だけが引かれている状態でした。

**入れたもの**: 到達度の目盛り (100 / 50 / 0%) をグラフの左に、期間 (最初と最後のセッション日) を下に、凡例 (濃い線 = 最高到達 / 薄い線 = その日の到達度 / 緑の点 = 初討伐) をその下に出しました。

**決めたこと**: **目盛りの文字を SVG の中に置いてはいけません。** このグラフは `preserveAspectRatio="none"` で内部座標 300×56 をカード幅に引き伸ばしているので、中のテキストも一緒に横に潰れます。2026-09-07 の「幅が足りない」という判断は数字を SVG 内に置く前提のもので、左に gutter を作れば幅は足ります。目盛り・期間・凡例はすべて HTML 側に出し、SVG は線だけを描きます。

**検証**: 320 / 375 / 640px の各幅で、目盛りの 32px gutter を引いてもグラフ本体がカード幅の 60〜83% を保ち、横スクロールが出ないことを実測。

## 🔀 設定: レポートの自動発見を FFLogs Sync へ移動 / 週制限の消化ウィンドウを畳んだままでも現在値が読めるように

**狙い**: 実機要望「レポートの発見元は FFLOGS SYNC に入れたほうが良い」「自分のアカウントから自動と FFLOGS SYNC の機能が被っている気がする」「週制限の消化ウィンドウ右端にも現状の設定を表示してほしい」。

**指摘は当たっていました。** コードを追うと 2 つが**同じ API クエリ**を叩いていました:

| どこ | 関数 | クエリ |
|---|---|---|
| FFLogs Sync (連動) | `fetchFflogsReportsV2` | `reports(userID: me.id)` |
| 「自分のアカウントから自動」 | `fetchFflogsRecentOwnReports` | `reports(userID: me.id)` |

**取れるレポートの範囲は同じ**で、違うのは取り込み方だけです — 連動は「動画や日付メモの日付と合うレポート」だけを紐づけますが、自動発見を ON にすると**日付が合う先が無いレポートも台帳に未分類として入ります**。つまり後者は前者の上位互換ではなく「拾い漏れを台帳に落とす」差分だけです。

**入れたもの**: 発見元 (と FFLogs guild ID) を通知の節から **FFLogs Sync の OAuth / 表示名の直下**へ移しました。同じ API を叩く設定が並ぶので、2 か所を見比べずに済みます。`user` 経路の説明には**上の差分を明記**しました。通知の節には通知だけを残し、行き先の案内を 1 行だけ置いています。あわせて「週制限の消化ウィンドウ」の見出し右端に現在値 (「今週だけ」/「今週 + 前週」) を出しました — ロット画面で前週の行が出るかどうかがこれで決まるので、**開かずに知りたい運用状態**です。

**検証**: dev preview で、通知の節に発見元が無く案内文が出ること、FFLogs Sync 側に 2 つのトグルと guild ID 欄が出ること、`user` 経路の説明に差分が書かれていること、週制限の見出しに「今週だけ」が出ることを実測。

## 🎬 練習ログ: 動画チップを「マークと番号」だけに縮小 (再生開始時刻は hover へ)

**狙い**: 実機要望「動画に関しては再生開始時間はいらないかも。動画マークと動画ナンバーだけにして小さくできるか」。

**妥当な指摘でした**: どの動画も**同じ pull の同じ瞬間**へ飛ぶので、行の中で知りたいのは「動画があるか / どれを開くか」だけです。再生開始時刻は開いた先で見える値で、行に出す必要がありませんでした。

**入れたもの**: フィルムのマークをグループの先頭に **1 個だけ**置き、その右に番号のリンクを並べる形にしました (`▶ 1 2`)。マークを番号ごとに繰り返さないのは幅のためで、番号だけだと意味が読めないのでマークは残しています。時刻と動画名は従来どおり hover (title) に入っています。**押せるリンクの数は変わりません。**

**効果 (実データで実測)**: 動画 1 本の行で動画列が **76px → 40px**。同じ行 (動画 1 本 + DPS 列あり) で折り返しの閾値が **約 720px → 670px 以下**へ下がりました。

⚠ **前項「pull 行が 2 行に折り返さないように」の検証値の訂正**: そこで「640px までは 1 行」と書いていたのは誤りで、その計測は**動画列が 0 本の状態**で行われていました。動画 1 本 + DPS 列ありの実データでの閾値は、その時点で約 720px です。

## 🙋 予定表: 日ごとに「有志練習 (任意参加)」を付けられるように (自動確定・未回答の催促・出席の集計から外れる)

**狙い (調査ノート第 4 回 W-18)**: 「参加できる人だけ」の日を運用として公式化するためのフラグです。ノートの提案は「セッション種別」でしたが、**種別を増やすと確定判定 (status) との組み合わせが増える**ため、`native_schedule_sessions.is_optional` の **boolean 1 列**に留めました。

**入れたもの**:

- 時刻編集 popover (日付の右のとけいアイコン、管理者のみ) に **「有志練習 (任意参加)」のチェックボックス**を追加しました。備考のとなりで、保存は時刻 / 備考と同じ「保存」ボタン 1 つです (差分のある項目だけ更新します)。
- チェックした日は行に **「有志」バッジ**が出ます。管理者以外にも、過去の行にも出します — 「その日は任意参加だった」は全員に必要な情報で、振り返りでも要るからです。
- フラグを立てた日は次の 3 つから外れます。
  1. **全員入力での自動確定** — 「全員回答 + 人数閾値」は全員で行く日の条件なので、任意参加の日に当てると「来られない人がいるから確定しない」に化けます。
  2. **未回答の催促メンション** — 任意参加なのに催促を飛ばすのは矛盾していて、催促圧だけが残ります。
  3. **出席の集計** (W-19 で実装予定) — 母数に入れると「休んだ人」に見えてしまいます。

**入れなかったもの**: Discord 通知本文への印。通知の文面はテンプレート方式で、備考 (`{{note}}`) にそのまま書けるため、置換キーを増やす価値が薄いと判断しました。

**既定は OFF** (`is_optional = false`) なので、使わない固定では見た目も挙動もこれまでと同じです。

**検証**: `tsc --noEmit` / `eslint` / `node scripts/check-*.mjs` (Windows で動く 36 本)。実機では「チェックを入れる → 行に『有志』バッジが出る → 外すとバッジが消える」と、その日に未回答を残しても催促が飛ばないことを確認してください。

## 🧾 出席の自動突合の土台: ログに映っていた人をメンバーに紐づける (キャラクター名は保存しない)

**狙い (調査ノート第 4 回 W-6)**: 「○ と答えたのに来ていない」「△ だったが参加した」を手入力ゼロで拾い、出席サマリー (W-19) のデータ源にします。この PR は**土台まで** — 画面に出る突合の一覧は次の PR です。

**保存するもの / しないもの**: ユーザー判断 (2026-09-08) で **突合結果だけを持つ**設計にしました。ログから拾ったキャラクター名は同期処理のメモリ内で対応表に解決し、DB に入るのは `(レポートコード, メンバーキー, 映った pull 数)` だけです。名前を pull の隣に置くと、`death_events` (ジョブ名だけを持ち、意図的に名前を持たない) と結合して「誰が落ちたか」を復元できる状態になります。突合結果だけならその経路が存在しません。

**入れたもの**:

- 設定 → メンバー一覧に **「ログ名」** の欄 (FFLogs のキャラクター名)。**表示名でも一致を試す**ので、表示名がキャラ名と同じ人は空のままで構いません。名前を持つ列はここだけです。
- 同期時に、各 pull の参加者を数えてレポート単位で保存します。**日単位ではなくレポート単位**にしたのは、同期が時間予算で途中打ち切りになったときに「今回拾えた分」で日の値を上書きすると pull 数が減り、**「○ なのに不在」を捏造する**からです。レポート単位なら 1 レポート = 1 回の書き込みで冪等です。
- 同期後のトーストに突合の結果を出します。**0 件でも出します** — 参加者名が FFLogs の Summary table のどの項目に入るかは実データでしか確かめられないため、「1 件も拾えていない」状態が画面から分かる必要があります (対応表の問題ではなく取得側の問題だと切り分けるため)。対応表に無い名前はそのまま列挙します。
- 名前の一致は「キャラ名 → 表示名」の順。**同じ正規化キーに 2 人が当たる場合はどちらにも解決しません** — 取り違えて出席を記録する方が、未解決として管理者に見せるより悪いからです。

**ズレの判定** (`attendanceMismatch`) は 4 種類に分けました。「× なのに参加」と「△ なのに参加」を分けるのは**責める向きが違う**ためです (× で参加は歓迎される話、○ で不在は確認したい話、△ で参加は回答の更新漏れ)。辞書外の記号は「参加可か不明」なので、映っていなくてもズレとして出しません。

**検証**: `node scripts/check-attendance-actuals.mjs` (新規 24 ケース: 名前の解決 7 / ズレの判定 16 / 並び順 1) と `check-fflogs-fight-detail.mjs` に参加者名の抽出 8 ケースを追加。⚠ **FFLogs の実 API はこの環境から叩けないため、`composition` / `damageDone` のどちらが名前を持つかは実データで未確認です** (両方を順に試す実装にしてあります)。実機では同期を 1 回押して、トーストの「出席の突合」が 0 件でないことを確認してください。

## 🗒 出席サマリー: 回答 (○×△) とログの突合を予定表から見られるように (本人は自分の分 / 幹部は全員)

**狙い (調査ノート第 4 回 W-19)**: W-6 で保存した「実際に映っていた pull 数」と ○×△ の回答を突き合わせ、次層の枠決めや補欠検討の根拠にします。予定表ヘッダーのクリップボードのアイコンから開きます。

**作らなかったもの**: **公開ランキングと連続記録 (streak)**。8 人の固定で出席率を並べて公開すると個人攻撃になりやすく、streak は「休めない空気」を作ります (ノート 6-2)。

**可視範囲**: 幹部は全員の集計とズレ一覧、本人 (幹部以外) は自分の行と自分に関するズレだけ。**絞り込みはサーバー側**で行います — 画面で隠すだけでは通信を見れば読めてしまうためです。

**出すもの**:

- メンバーごとに「参加と回答 N/M」「実際に参加 N/M」「ズレ N」
- 日付つきのズレ一覧 (新しい順)。ズレは 4 種類に分けています。「× なのに参加」と「△ なのに参加」を分けるのは**責める向きが違う**ためです。

**突合できない日を分母に入れません。** これが今回いちばん重要な仕様です。

- **ログが 1 pull も無い日**は「来ていない」ではなく「分からない」。分母に入れると、ログ担当が休んだ日が全員の欠席に化けます。
- ⚠ **pull はあるのに参加者が 1 人も紐づいていない日も同じ扱い**にしました。対応表が空、または参加者名の取得が効いていないときにこうなります。**dev preview で実際に「参加と回答した全員が不在」と表示され**、その場で仕様を直しました。この状態のときは「全員休んだ」ではなく突合が効いていない、と画面に明記し、直し方 (メンバー一覧の「ログ名」を入れて同期し直す) を添えています。
- 有志練習 (W-18) と中止の日も外し、件数だけ出します。

**デモ**: `seed-demo.sql` に Section 5 (sentinel `demo_seed_w6_applied`) を追加し、合成レポートに出席の実績を入れました。ズレも意図的に 3 種類作ってあります。

**検証**: `node scripts/check-attendance-history.mjs` (新規 16 ケース。分母の扱い / 除外 / 並び / 「参加と回答」の範囲を固定) と `tsc` / `eslint` / `check-changelog` / `check-seed-sql` / `pglast`。dev preview では、突合結果を合成した状態で表とズレ一覧の表示を実測し、**空の状態 (突合 0 件)** も実測して文言を確認しました。

## ⌨ コマンドパレット (Ctrl+K): コンテンツへのジャンプと設定の各節を検索して開けるように

**狙い (調査ノート第 4 回 8-3 UI-8 / 第 2 回 UI 監査の残課題 3)**: 残課題 3 は **「管理操作が設定ダイアログの奥にある」**。目的の設定まで、ヘッダーの歯車 → 長い一覧をスクロール → 節を開く、の 3 手が必要でした。

**使い方**: `Ctrl+K` (Mac は `⌘K`) か、ヘッダーの虫眼鏡ボタン。`↑ ↓` で選び `Enter` で実行、`Esc` で閉じます。

**候補**:

- **ジャンプ** — スケジュール / コンテンツ一覧 / 各コンテンツ / 各コンテンツ × タブ (軽減表・ロット・攻略・動画・マクロ・練習ログ)。コンテンツ側で**無効にしたタブは候補に出しません** (開いても空の画面になるため)
- **設定** — 設定を開く / 設定の各節 (メンバー一覧・Discord 通知・出欠の催促・出欠の凡例・中止した候補日・FFLogs 連携・練習ログの通知・週制限のウィンドウ・過去の活動・メンテナンス日程・データの初期化)。節を選ぶと**その節を開いた状態でスクロールして止まります**

**設計の判断**:

- **既存の導線は 1 つも減らしていません。** パレットは追加の近道で、置き換えではありません (ノートのデメリット欄「恩恵はデスクトップ中心。モバイルは既存ダイアログを残す」)。
- **虫眼鏡ボタンも置きました。** キーボード近道だけでは存在に気付けず、「発見性の課題への回答が発見できない機能」になってしまいます。
- 絞り込みは**部分一致**。候補が数十件の規模でファジー一致を入れると、狙った項目より前に別の項目が来る事故のほうが多いためです。
- 候補は**すでにヘッダーが持っているカテゴリ一覧から組みます** (DB を引かないので、パレットのために往復が増えません)。
- ⚠ **単キー (`g` など) の割り当ては入れていません。** 入力欄にフォーカスが無い状態を条件にしても、popover やダイアログの中で誤爆する経路が多いためです。

**実装で踏んだ落とし穴 2 つ** (どちらも dev preview で発覚):

1. 設定ダイアログの本体は**初回クリックまで mount されない** (2.14 の軽量化) ため、本体側だけにパレットからの合図を受けるリスナーを置くと**1 回目が何も起きません**。遅延読み込みのラッパー側でも受け、mount 後に節を開く形にしました。
2. 「閉じたら検索語を effect でリセット」は `react-hooks/set-state-in-effect` の error で CI が落ちます。開閉のハンドラ側で初期化しています。

**あわせて直したもの (前項の出席サマリー)**: 375px 幅で表の「ズレ」列が画面外に出て横スクロールしないと読めず、ズレ一覧の「(1/6 pull)」が切れていました。表の最小幅を 24rem → 20rem にして名前を truncate し、ズレの文言に `min-w-0` を付けて折り返すようにしました (mobile 実測で横溢れ 0)。

**検証**: `tsc` / `eslint` / `check-font-sizes` / `check-changelog`。dev preview で「虫眼鏡 → 一覧表示」「"ロット" で 7 件に絞り込み」「Enter でロット管理へ遷移」「設定 / メンバー一覧 → ダイアログが開いてその節が展開 + スクロール」を実測しました。

## 💀 練習ログ: pull の番号を押すと、その pull の死亡を時刻順の一覧で開けるように

**狙い (調査ノート第 4 回 8-3 UI-14)**: 行に出しているワイプ原因は「**最初に**落ちたジョブと致命技」の 1 行だけです。その後どう崩れたか (連鎖して落ちたのか、別の場面でもう一度落ちたのか) は FFLogs を開かないと分かりませんでした。

**出るもの**: `4:59 P2 WAR 💀 重圧の呪詛` の形で、pull 開始からの時刻・フェーズ・ジョブ・致命技を時刻順に並べます。直前の死亡から 10 秒以内の死亡は**薄く + `+10s`** を添えます (1 件目の連鎖なのか、別の場面での死亡なのかを目で分けられるようにするため)。

⚠ **プレイヤー名は出ません。** 保存している死亡イベントがそもそも名前を持たない設計 (2026-09-06 の W-1) なので、「誰が落ちたか」ではなく「どのジョブが何で落ちたか」です。

**開閉は pull の番号 (`#`) をボタンにしました。** 右端に開閉ボタンを足すと行幅が約 22px 増え、**直前に下げた折り返しの閾値が押し戻されます** (実測でそうなりました)。`#` の記号自体は意味を持たないので、そこを開閉の矢印に差し替えれば**行幅は 1px も増えません** (実測: 開閉ボタンの幅 32px = 元の番号セルと同じ)。

**取得は開いたときだけ**です。死亡イベントを一覧の通信に載せると、明細 20,000 pull ぶんで gzip 予算 (300 KB) を超えます。開いた pull を 1 行だけ引くので、**一覧の重さは変わりません** (`check-fights-payload.mjs` は据え置きで通過)。一度開いた pull は閉じても再取得しません。

死亡が 0 件の pull (討伐 / 未取得) でも押せるようにしてあります。「取得できていない」と分かること自体に意味があるためで、その場合は理由 (代替経路での取り込み / 同期の時間切れ) と直し方を出します。

**あわせて整理したもの**: `death_events` / `phase_transitions` の防御的パース (`asDeathEvents` / `asPhaseTransitions`) を `supabase/fflogs-fights.ts` の private 関数から `lib/fflogs-fight-detail.ts` へ移しました (保存形を定義しているファイルへ寄せ、Server Action からも同じ関数で読めるようにするため)。

**検証**: `tsc` / `eslint` / `check-fflogs-fight-detail` / `check-fights-payload` / `check-font-sizes` / `check-use-server-exports`。dev preview で「番号を押す → 2 件の死亡が時刻順に出る (2 件目は `+10s` 付きで薄い)」「行の高さ 33 → 81px、隣の行は 33px のまま」「開閉ボタンの幅が 32px = 元の番号セルと同じ」を実測しました。

## 🏷 練習ログ: pull に「なぜ崩れたか」の注釈を付けられるように (既定はチーム帰属 / 傾向カード / Discord 用の振り返り)

**狙い (調査ノート第 4 回 7-A W-7)**: FFLogs は「**何が**起きたか」(誰がいつ何で落ちたか) までしか持ちません。「**なぜ**崩れたか」は人しか分からないので、そこに人の判断を重ねます。前項の展開行 (死亡の一覧) の真下に置いたので、**見ながら付けられます**。

**選択式にしました** — 自由記述だと集計できず、W-7 の本題である「傾向」が出せません。タグは 10 種類 (AoE 被弾 / 軽減不足 / スイッチ遅れ / 散開・位置 / ノックバック / 雑魚処理 / 回復が間に合わず / コール漏れ / 火力不足・時間切れ / その他) で、一言 (200 字) を任意で添えられます。

### ⚠ 帰属の既定はチーム

ノートのデメリット欄は **「個人責任の可視化は雰囲気悪化の恐れ」**。その回答が帰属の設計です。

- **既定は「チーム」** (誰のせいでもない)
- **「自分」は本人が自分に付けるときだけ。** 相手はサーバー側で**呼び出した本人の ID を埋める**ので、**他人に個人タグを付ける経路が存在しません** (画面に無いのではなく、構造上できません)
- **個人タグの中身は傾向カードにも Discord 用の本文にも出しません。** 本人が自分用に付けた印なので、件数だけ「個人メモ N 件」と添えます
- 消せるのは**自分が付けたもの、または管理者**だけ。付けた人以外が黙って消せると「無かったことにする」が起きます

### 傾向カードと Discord 用の振り返り

練習ログの上部に「ミス注釈の傾向」カードが出ます (注釈が 1 件も無い固定ではカードごと出ません)。ワイプ原因カードが「致命技」ベースで同じ問いに答えているのと対になる位置づけです。

「振り返りをコピー」でDiscord に貼れる本文を作ります。タグの集計 + 一言メモを並べ、**個人タグは件数だけ**です。

### データの置き場

`fflogs_pull_notes` は **RLS 有効 + ポリシー 0 本** (`fflogs_attendance_actuals` / `category_link_reads` と同じ形)。「誰がどの pull に何のタグを付けたか」を公開 anon key で列挙されると、それ自体が個人責任の可視化になるためです。読み書きの権限判定は Server Action に集約しています。

タグの語彙は DB では縛らず 32 字の自由文字列にしました。語彙を増やすたびに schema を触ると、デプロイ順の食い違いで新しいタグが弾かれます。集計はアプリ側の既知リストとの突き合わせで行い、**知らないタグは落とさず末尾に回します** (語彙を整理しても過去の注釈が消えません)。

**デモ**: `seed-demo.sql` に Section 6 (sentinel `demo_seed_w7_applied`) を追加。⚠ **チーム帰属だけ**入れています — デモは匿名で読めるので、架空でも「個人に紐づいた失敗の記録」を並べると機能の意図を誤解させます。

**検証**: `node scripts/check-pull-note-tags.mjs` (新規 11 ケース。並び順が実行ごとに揺れないこと / 知らないタグを落とさないこと / **Discord 用の本文に個人タグの中身が入らないこと**を固定) と `tsc` / `eslint` / `check-seed-sql` / `check-ja-strings` / `pglast`。dev preview では注釈を合成した状態で「展開行にチップが出る (チーム = 琥珀 / 自分 = ● + 紫)」「タグ 10 種の選択と チーム/自分 の切替」「傾向カードの棒と件数」を実測しました。

⚠ **保存・削除の経路は実機で 1 回試してください。** `fflogs_pull_notes` はこのリリースで追加した表なので、デプロイ前の dev preview では読み書きを実測できていません (表示は合成データで確認済み)。

## 🩺 練習ログ: 死亡時点の HP / シールド / かかっていた効果を FFLogs から読めるように (押したときだけ取得)

**狙い (調査ノート第 4 回 7-A W-8)**: 「軽減が乗っていなかった」を客観化します。pull の展開行 (死亡の一覧) の下に「死亡の直前を読む」ボタンが出て、押すとそれぞれの死亡時点の **HP / シールド / かかっていた効果** が並びます。

**押したときだけ取ります。保存もしません。**

- ノートのデメリット欄が **「`includeResources` で帯域大。+1 往復/死亡」**。同期に載せると、直近レポートの取り直しで毎晩この重いクエリが走り、**pull の取り込み自体が時間予算に押されます**。
- 保存しないのは、振り返りの最中に 1 回見る値であることと、「その瞬間に何がかかっていたか」を DB に持つと **個人の立ち回りの記録**になってしまうためです (個人の火力を序列化しない、という方針と衝突します)。

⚠ **軽減率の計算はしません。** ノートのデメリット欄が「What-if 軽減は軽減率テーブルの保守が要る」で、パッチごとに率が変わる表を抱えるのは割に合いません。**かかっていた効果を名前のまま並べる**ので、「堅陣が無い」は一覧を見れば読めます。例外は **ダメージダウン (`1002911`)** だけで、ID が固定かつ毎回確認する値なので専用の印を出します。

HP は `4500 / 150000 (3%)` の形で**割合も添えます** — 数字だけだと最大 HP を覚えていないと「即死だったのか削られていたのか」が判断できないためです。

**前提**: FFLogs の OAuth 接続と、v2 で読めるレポートが必要です。代替経路 (v1 / cookie) で取り込んだレポートや未接続のときは、その理由を出します。

**検証**: `node scripts/check-death-leadup.mjs` (新規 14 ケース。⚠ **`events` の `timestamp` はレポート相対**なので pull 相対に直すこと / 資源が `targetResources` と `sourceResources` のどちらでも読めること / ダメージダウンの判定 / 別 pull の混入を捨てること) と `tsc` / `eslint` / `check-font-sizes`。dev preview では「死亡がある pull にだけボタンが出る」「押すと未接続の理由が出る」を実測しました。

⚠ **FFLogs から実際に値が返る経路は、この環境からは確認できていません** (デモは合成レポートで OAuth 未接続、実 API に到達できません)。実機で 1 pull 押して、HP と効果が並ぶことを確認してください。並ばない場合は `parseDeathLeadUp` の入力側 (レスポンスの形) を疑ってください。

## 🎲 ロット: 部位ごとの「欲しい人」行列と取る順番の提案 (BiS の取得済から自動 / 確定はしない)

**狙い (調査ノート第 4 回 7-C W-24 + W-25)**: アイテムが落ちた瞬間に「これを欲しいのは誰か」「誰が取るのが妥当か」を数えずに出します。ロットタブの上に **部位 × メンバー**の行列が出ます。

**根拠にするのは W-23 で入れた BiS の「取得済」チェックだけ**です。ユーザー指定 (2026-09-08) の **「ロット表を作らない固定向けであっても良い」** に沿う形で、Google Sheets のロット表が無くてもこれだけで回ります。

### ⚠ 層 × 部位の対応表は作りませんでした

「どの層で何が落ちるか」はゲーム側のデータで、抱えるとパッチごとの保守が続きません。**アイテムが落ちた瞬間に知りたいのは「この部位を欲しいのは誰か」**であって層の分類ではないので、行は**部位**にしました (層は固定の人が知っています)。

### ⚠ 提案止まり

ノートのデメリット欄が **「ルールが固定ごとに多様 (左取り抜け / 優先制 / フリロ)」**。順番は提案として数字で出し、**確定するボタンは作っていません**。

順番の規則は 1 つだけです: **取得済が少ない人が先、同数なら残りが多い人が先**。同条件のときは表示名で並べます (実行ごとに順番が揺れないように)。理由 (取得済 n/11) はセルの hover に入っているので、提案を採るかどうかを人が判断できます。

### 3 色セマンティクス (W-25)

緑 = 提案 1 番目 / 黄 = 欲しいが 1 番目ではない (代替あり) / ✓ = 取得済 / `·` = そのジョブで数えない部位。**色だけで意味を伝えないよう**、緑と黄には順番の数字を、取得済にはチェックを入れています。

ナイトの盾 (OffHand) は、ナイトの BiS が登録されているときだけ行が出ます (他ジョブで数えると分母が 12 になり「11/12 で完成」になってしまう)。

### 入れなかったもの

**断章カウンタ** (週の断章枚数 vs 交換必要数)。交換に必要な枚数はゲーム側のデータで、portal は持っていません。持たせると層 × 部位の対応表と同じ保守問題を抱えるので入れていません。

**デモ**: `seed-demo.sql` に Section 7 (sentinel `demo_seed_w25_applied`) を追加。既存のデモデータは 4 人が別々の部位を欠く形で、**提案の 2 番目 (黄) が出てこない**ため、1 部位だけ 2 人が欲しい状態にしました。

**検証**: `node scripts/check-loot-priority.mjs` (新規 15 ケース。提案順が実行ごとに揺れないこと / ナイト以外で盾を数えないこと / ジョブを変えたときに「12/11 取得」にならないこと / セルの意味) と `tsc` / `eslint` / `check-font-sizes` / `check-seed-sql` / `pglast`。dev preview で**デモの実データ** (4 人 / 8〜9 部位取得済) を使って行列と凡例を実測しました。

⚠ 検査で 1 件、**私の期待値が間違っていました** — 取得済が同数のとき「残りが多い人が先」なので、11 部位のジョブより 12 部位のナイトが先に来ます。コードが正しく、テストを直しました。

## ⭐ マクロ: 「採用中」の印と、採用中との差分の行数 (複数の strat が並んでも「うちはどれか」が分かる)

**狙い (調査ノート第 4 回 8-3 UI-7)**: 層ごと・攻略サイトごとにマクロが並ぶと、**うちが使っているのはどれか**が画面から分かりませんでした。PF から別の strat が流れてきたときや配置を変えたときに毎回本文を読み比べる必要がありました。

**入れたもの**:

- マクロの行の星ボタンで **「採用中」** を切り替えます (もう一度押すと解除)。採用中の行は枠を光らせ、**「採用中」の文字**も出します (色だけに頼らないため)。
- 採用中でない行には **「採用中と +3 / -2 行」** の要点が出ます。hover でどの行が違うか (最大 3 行ずつ) を見られます。
- ⚠ **1 コンテンツに 1 本だけ**を DB で保証しています (部分 UNIQUE インデックス)。「採用中が 2 本」は表示が壊れるだけでなく、差分の基準が決まりません。切り替えは「他を解除 → 対象を設定」の順で行います (逆順だと一瞬 2 本になってインデックスに弾かれます)。

**差分の取り方**: **行の集合として比べ、順番は見ません。** マクロは同じ内容でも行の並びが入れ替わることが普通にあり (`/p` の並べ替え、コメント行の位置)、順番まで差分にすると**ほぼ全部が「違う」になって役に立ちません**。同じ行の**回数**は見ます (2 回コールするのは意味のある違い)。マクロ記号 (`/p` と `/say`) や絵文字は落としません — それこそが strat の違いであることが多いためです。

**検証**: `node scripts/check-macro-diff.mjs` (新規 15 ケース。並び替えを差分にしないこと / 回数を見ること / マクロ記号と絵文字を落とさないこと / 空行を無視すること) と `tsc` / `eslint` / `check-font-sizes`。dev preview では採用中を合成した状態で「採用中の行に枠と『採用中』が出る」「他の行に『採用中と +3 / -2 行』が出る」を実測しました。

⚠ **保存の経路は実機で 1 回試してください。** `is_current` はこのリリースで追加した列なので、デプロイ前の dev preview では書き込みを実測できていません (押すと切り替えに失敗するトーストが出るところまで確認済み)。


## 📱 予定表: スマホは縦のアジェンダ表示に (横スクロールなしで「次はいつか」と自分の回答が見える)

**狙い (調査ノート第 4 回 8-3 UI-10)**: 予定表は「1 列 = 1 人」の表で、8 人だと **375px 幅では横スクロールが必須**でした (実測: 内容幅 778px / 表示幅 341px)。開催直前にスマホでしたいことは「次はいつか」「自分の回答を入れる」の 2 つで、そのために横スクロールを 2 往復させるのは筋が悪い状態でした。

**入れたもの**: md 未満では 1 予定 = 1 枚の縦カードにします。載せるのは日付 + 時刻 / 確定 / 有志 (任意参加) / 出欠の内訳チップ / **自分の回答**で、自前作成式なら回答をその場で変更できます。

⚠ **表は消していません。md 未満だけ差し替えます。** 8 人の記号を一望できるのは表の強みで、PC ではそのままが最善です (ノートの UI-10 も「デスクトップは現状維持で二系統になる」を前提にしています)。

⚠ **8 人ぶんの記号はカードに載せません。** 内訳チップが「何人 OK / 未回答何人」を既に答えていて、個々の記号まで縦に積むと 1 枚が長くなり「次はいつか」が読めなくなります。詳しく見たいときは横向きにすると表になります。

**効果 (実測)**: スマホ幅での**横溢れが 0** になりました (これまでは予定表の内容幅 778px に対して表示幅 341px)。

**検証**: `tsc` / `eslint` / `check-font-sizes`。dev preview で **375px** (アジェンダが出る / 横溢れ 0 / 有志バッジも出る) と **1100px** (アジェンダは非表示、表が出る) の両方を実測しました。

## 🛡 軽減表: カードの絞り込みを「全部 / 自分のロール / 自分の担当だけ」の 3 段に (メンバーにロールを設定)

**狙い (調査ノート第 4 回 8-3 UI-4)**: ノートの UI-4 は「ロール別チートシート」で、前提に **攻略データの構造化入力**が要ると書かれていました。そこはまだ無いので、ノート自身が挙げている **「まず軽減カードの『自分の担当だけ』を拡張」** から入れます。

**入れたもの**:

- 設定 → メンバー一覧に **「ロール」** (タンク / ヒーラー / DPS / 未設定) を追加しました。
- 軽減表のカード表示の絞り込みが **全部 / 自分のロール / 自分の担当だけ** の 3 段になりました (これまでは「全部 / 自分の担当だけ」の 2 段)。

⚠ **ロールは 3 値に固定**しました。タンク / ヒーラー / DPS はゲームの構造で、運営の再編で増減しません (DC 名を自由記述にしたのとは事情が違います)。**MT/ST/H1/H2/D1〜D4 の細かい位置は持ちません** — 固定ごとに呼び方が違い、シートの列見出しが正だからです。

⚠ **自分のロールが分からないときは「自分のロール」のボタンを出しません。** 表示名が未設定・メンバー未登録・ロール未設定のいずれでも、押しても全部が残るだけのボタンは壊れているように見えます。ロールを 1 つも設定していない固定ではこれまでと同じ 2 段です。

⚠ **同名のメンバーが 2 人でロールが違う場合、その名前はどちらにも決めません。** 取り違えて他人の列を「自分のロール」に混ぜるより、絞らない方が害が小さいからです。

**入れなかったもの**: ノートの「Image Only」モードとフェーズタブ。前者は攻略データの構造化入力が前提で、後者は軽減表の層タブとして既にあります (UI-5 で表示形式の外へ出しました)。

**検証**: `node scripts/check-member-roles.mjs` (新規 13 ケース。名前の正規化 / 同名でロールが違う場合 / 自分のロールが分からないときに絞らないこと) と `tsc` / `eslint` / `check-font-sizes`。dev preview では**合成した軽減表とロール**で「3 段のボタンが出る」「自分のロール → タンク 2 人の列だけ」「自分の担当だけ → 1 列だけ」を実測しました。

⚠ **保存の経路は実機で 1 回試してください。** `role` はこのリリースで追加した列です。またデモには軽減表シートが無いため、カードの絞り込みは**合成データでの確認**になっています。

## 📋 練習ログ: pull からボスの大きい技を拾って軽減表の雛形を作れるように (Sheets に貼れる形でコピー)

**狙い (調査ノート第 4 回 7-A W-10)**: 新層初週の軽減表づくりは「ボスが何分何秒に何を撃つか」を書き出すところから始まります。それは既にログに入っているので、pull から拾って**表の雛形**にします。pull の展開行の「軽減表の雛形を作る」から使えます。

出るのは `時刻 / 技名 / 対象人数 / 合計ダメージ / 1 人あたり最大` の表と、**Sheets にそのまま貼れる形 (タブ区切り) のコピー**です。

**畳み方が本題です。** FFLogs の被ダメージは **1 ヒット = 1 件**で返るので、全体攻撃は 8 人ぶんが並びます。そのままでは軽減表になりません。「同じ技 × その行の先頭から 0.7 秒以内」を 1 行に畳んで、軽減表の 1 行と同じ粒度にしています。

⚠ 畳む基準を **「その行の先頭から」** にしたのには理由があります。丸め (1 秒単位など) だと境界をまたいだ同じ攻撃が 2 行に割れます。逆に「直前のヒットから」にすると、**一定間隔で刻む継続ダメージが際限なく 1 行に繋がります** (0.6 秒ごとの技が 1 行になり、対象人数が数十になって意味を失う)。検査でこの 3 通りを固定しています。

継続ダメージの細かい行を落とすため、合計 2 万未満の行は出しません。

⚠ **軽減の判定と軽減率の計算はしません。** 「どの技が軽減か」の許可リストはパッチごとの保守が続かないためです (前項の「死亡の直前」と同じ判断)。出すのは実測値だけです。

⚠ **押したときだけ取り、保存もしません。** 1 pull で数百件返るクエリなので、同期に載せると毎晩の取り直しで pull の取り込み自体が遅くなります。新層初週にしか使わない機能なので、その場で取ってその場で使う形にしました。

**W-9 (計画 × 実測の 3 要素バー) と UI-13 (レーン型タイムライン) は据え置きます。** シートの技名は固定ごとの略称で FFLogs の正式名と文字列一致せず、無理に突き合わせると「合っているのに合っていないと出る」方が害が大きいためです。ノート自身も **「8.0 の軽減再編後に着手する方が保守が楽」** と書いています。今回入れた「実測の時系列」はその x 軸に当たる部分です。

**検証**: `node scripts/check-boss-damage-timeline.mjs` (新規 18 ケース。8 ヒットの畳み込み / 畳む基準の 3 通り / 別の技は別行 / 吸収を含める / 閾値 / TSV がタブ区切りであること) と `tsc` / `eslint` / `check-font-sizes`。dev preview では「死亡がある pull に 2 つのボタンが縦に並ぶ」ことを実測しました。⚠ ここで **1 件直しています** — 器が縦積みでなかったため 2 つのボタンが同じ行に並んでいました (実測: 両方 top≈1520 → 修正後 1520 / 1550)。

⚠ **FFLogs から実際に値が返る経路は、この環境からは確認できていません** (デモは合成レポートで OAuth 未接続)。実機で 1 pull 押して、表が出て「貼り付け用にコピー」が Sheets に貼れることを確認してください。

## 🎓 攻略情報: 新規メンバー向けの学習パス (動画 → 散開図 → マクロ → 軽減表 の順序付きチェックリスト)

**狙い (調査ノート第 1 回 B-3 / 第 4 回 7-E)**: 新しく入った人が「何から見ればいいか」で止まらないように、順番を示すチェックリストを攻略情報タブの上に置きます。各項目はそのタブへのリンクになっています。

**順番は固定です。** 動画 → 散開図 → マクロ → 軽減表 は「全体像 → 位置 → 合図 → 担当」で依存関係の順になっていて、大半の固定に当てはまります。⚠ 並べ替えや項目追加の UI は作りませんでした — 固定ごとに変えられるようにすると「うちの順番は何が正しいのか」を決める作業が増え、**入れる前に止まります**。

⚠ **自動判定はしません。** 「動画を見た」は portal からは観測できず、観測できないものを自動で「済」にすると**見ていないのに済**になります。チェックは本人が手で付けます (攻略リンクの「見た」と同じ信頼モデル)。

⚠ **中身が無い項目は出しません。** 動画が 0 本のコンテンツで「動画を見る」を促しても空の画面に飛ぶだけです。分母からも外すので「1/4 しか終わっていない」に見えません (実測: 軽減表シート未設定のコンテンツでは 3 項目 = `1 / 3 完了`)。

⚠ **誰が終わっていないかは出しません。** 幹部には「3 人」のように人数だけ出します。攻略リンクの既読 (W-27) では「誰が未読かは幹部のみ」にしましたが、学習パスは**新規メンバーを追いかける形**になりやすいので、名前は出しません。

**全部済んだら 1 行に縮みます。** 済んだ人にとってこのカードはただの邪魔なので、新規メンバーだけに効く形にしてあります。

**検証**: `node scripts/check-onboarding-steps.mjs` (新規 13 ケース。順番の固定 / 中身が無い項目を分母から外すこと / `next` が中身のある未済の最初になること / 知らない済 id を数えないこと) と `tsc` / `eslint` / `check-font-sizes` / `pglast`。dev preview では**デモの実データ**で「`1 / 3 完了`」「1 番目が済で取り消し線」「2 番目に『次はここ』」「幹部には人数」を実測しました (軽減表シートが無いので 3 項目になるのも設計どおり)。

⚠ **保存の経路は実機で 1 回試してください。** `category_onboarding_steps` はこのリリースで追加した表です。

## 🙋 自分のページ (/me): 残り BiS・学習パス・出席を 1 枚に (コマンドパレットから開けます)

**狙い (調査ノート第 1 回 B-5)**: 「自分は何が残っているか」を探しに 3 つのタブを回らなくて済むようにします。`Ctrl+K` のコマンドパレットから「自分のページ」で開けます。

**新しいデータは 1 つも増やしていません。** 出席サマリー (W-19) / BiS の取得済 (W-23) / 学習パス (B-3) が揃ったので、**既にある値を自分の視点で集める**だけで成立します (ノート第 4 回 7-E の「W-19 + W-23 が揃うと自然に成立。新規データ無し」のとおり)。

**出るもの**: 自分の情報 (表示名 / ロール / ログ名) / 残り BiS (コンテンツごとに `8/11` と残りの部位名) / 学習パスの進捗 (コンテンツごとに `2/4` と次の項目) / 出席サマリー (右上のボタン、予定表と同じもの)。

⚠ **自分のぶんだけです。** 管理者でも他人の行は出しません — 全員の集計は予定表の出席サマリー側にあります。`/me` を開けば全員の進捗が見える導線は作りませんでした。

⚠ **自分の担当軽減は出しません。** 軽減表は Google Sheets が正で、担当はシートの列にあります。コンテンツをまたいで集めると**外部の取得がコンテンツ数ぶん走り、`/me` の表示が Sheets の応答に引きずられます**。軽減表タブに「自分のロールだけ」(UI-4) を入れたので、そこへの案内に留めました。

⚠ **ヘッダーにボタンは増やしていません。** 開く頻度が常時表示に見合わないので、コマンドパレットからの導線だけにしました。

**メンバー一覧に自分の行が無いとき**は、何を登録すれば出るようになるかを画面に書きます (空欄が並ぶだけの画面にしないため)。

**検証**: `tsc` / `eslint` / `check-font-sizes` / `check-changelog`。dev preview では**デモの実データ**で「表示名 / ログ名 (Vee Nes) / 残り BiS が `8/11` と `残り: 武器 / 脚 / 腕`」を実測し、未登録のときの案内文も実測しました。

## 🌐 練習ログ: ワイプ原因とボスの技名を表示言語で出す (英語のまま残っていた過去の pull も含む)

実機報告 (L-7) への対応です。ワイプ原因が `DRK ← Xtreme Wave` のように**英語のまま**出ていました。

**なぜ英語だったか**: 技名の日本語化は 2026-09-06 に入れたのですが、**同期の後段でしか走らない**作りでした。ログを取り込んだときに日本語名を保存するので、**それより前に取り込んだ pull には日本語名が無く**、英語のまま残ります。再同期は 1 回に 1 レポートしか取り直さないため、放っておいても埋まりません。

**直したこと**:

- **足りない技名を表示のときに引く**ようにしました。表示に必要なのは「最初の死亡の技名」だけで、その種類は 1 コンテンツで数十しかないので、**欠けているぶんだけを 1 〜 2 回の問い合わせ**で解決します (上限 100 件 / 締切 2.5 秒 / 7 日キャッシュ)。過去の pull も**押さずに**日本語になります。
- **表示言語に追従**するようにしました。これまでは日本語名があれば言語設定に関係なく日本語で出ていたので、**英語表示では英語名**が出ます。同期のときに日本語と英語の**両方**を保存します。
- pull を展開したときの死亡一覧 (UI-14) と、**軽減表の雛形 (W-10) のボスの技名**も表示言語で出します。

⚠ **解決できなかったときは元の名前のままです。** 技名の辞書はゲームデータの公開ダンプ (XIVAPI) から引いていて、自前のデータ表は持ちません。取得に失敗しても表示は止まりません (装飾であって依存先ではないため)。

⚠ **FFLogs は今も叩きません。** 練習ログのページが読むのは DB だけ、という設計はそのままです。

**検証**: `tsc` / `eslint` / 検査スクリプト全 48 本。**XIVAPI の実応答**で、日本語 (`邪竜爪牙` / `トータルエクリプス`) と英語 (`Hatebound` / `Total Eclipse`) が**それぞれ別の欄に入り**、表示言語で選ばれることを実測しました (日本語表示で `PLD ← 邪竜爪牙 +1`、英語表示で `PLD ← Hatebound +1`)。

## 🎭 軽減表: 自分のジョブを設定すると「自分のロール / 自分の担当だけ」が使えるように (ロールはジョブから決まります)

実機報告 (L-8) への対応です。「ロールの設定場所が非常に分かりにくい」「軽減表もどの列が対応するロール名か分からない」の 2 点でした。

**なぜ分かりにくかったか**:

- ロールの設定は**設定 → メンバー一覧の奥**にしかなく、しかも**幹部でないと触れません**でした。つまり**自分のロールを自分で設定する方法が無い**状態でした。
- 軽減表の絞り込みが見ていたのは「表示名」で、それを**シートの見出し行**と突き合わせていました。実物の軽減表は見出しがジョブのアイコン画像で、担当は `アドル (赤魔道士)` のように**ジョブ名**で書かれています。そのため 1 列も一致せず、「見出し行に一致する列が見つかりませんでした」しか出せていませんでした。
- 列の色分けは `MT` `ST` `H1` `D1` のような略号だけを見ていたので、ジョブ名で書かれたシートでは**色も付いていませんでした**。

**直したこと**:

- **自分のジョブを選べるようにしました。** 場所は**使うところ** — 軽減表タブの絞り込みの隣と、自分のページ (`/me`) です。**本人が保存できます** (幹部を待つ必要はありません)。幹部が代わりに入れる経路も設定のメンバー一覧に残しています。
- **ロールは選ばせません。** ジョブから決まります (赤魔道士 → DPS)。設定が 1 つ減り、しかも軽減表の列に当たるようになります。
- **軽減表の絞り込みをジョブで当てます。** 列ラベルの `(赤魔道士)` を読んで、「自分の担当だけ」＝自分のジョブの列、「自分のロール」＝同じロールのジョブの列に絞ります。日本語名 / 英語名 / 3 文字略称 (RDM) のいずれの表記でも当たります。表示名で見出しに当てる従来のやり方も**残しています** (見出しにメンバー名を書いている固定はそのまま動きます)。
- **どの色がどのロールかを画面に書きました。** チェックのチップをロール色 (タンク=青 / ヒーラー=緑 / DPS=赤) で塗り、その凡例を表の上に出します。絞り込みボタンにも「自分のロール (DPS)」のように**実際のロール名**を出します。

⚠ **ジョブ名の対応表は増やしていません。** 既にワイプ原因の略称表示に使っている 22 ジョブの表 (`JOB_ABBR`) をそのまま使い、日本語名とロールを足しただけです。片方だけ直すと食い違うので、対応が崩れたら CI が落ちます。

⚠ **知らない表記は「不明」として絞り込みから外れるだけ**です。1 文字の略記 (「白」「戦」) は別のジョブやアビリティ名に誤爆するので**当てません**。当たらなければ全部が表示されます (空の表を出すより害が小さいため)。

**検証**: `tsc` / `eslint` / 検査スクリプト全 49 本 (`check-jobs` を新規追加)。`schema.sql` と `seed-demo.sql` は libpg_query で構文検証。dev preview に**合成した軽減表**の一時ページを置き、`アドル (赤魔道士)` などの列ラベルから 5 列すべてジョブが解決されること、赤魔道士で「自分のロール」= アドル + 牽制 (侍)、「自分の担当だけ」= アドルだけに絞られること、チェックのチップが暗黒騎士=青 / 学者・白魔道士=緑 / 赤魔道士・侍=赤で塗られることを実測しました (一時ページは削除済み)。
