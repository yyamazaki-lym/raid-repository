/**
 * Hand-curated release notes. Shown in the settings dialog when the
 * user clicks the "更新履歴" button.
 *
 * Entries are user-facing (paste-into-a-newsletter level), not
 * commit-level.
 *
 * **File split** (TODO #67, 2026-05-02): only the latest release lives
 * in this module so the initial client bundle stays small. Older
 * entries are kept in `./changelog-archive.ts` (`RELEASES_ARCHIVE`)
 * and loaded lazily via `dynamic import("@/lib/changelog-archive")`
 * when the user clicks "過去の更新履歴を見る" in the settings dialog.
 * When a release graduates from this module, prepend it to the top
 * of `RELEASES_ARCHIVE`.
 *
 * Versioning scheme (from 2026-04-28, while staying on v1.9):
 *   `MAJOR.MINOR` + `(YYYY-MM-DD)` date suffix — patch dropped.
 *     - Small fixes / tweaks: keep MAJOR.MINOR, add a NEW entry with the
 *       new date. Multiple entries can share the same `version` field.
 *     - Notable feature additions / reworks: bump MINOR (e.g. 1.9 → 1.10).
 *     - Breaking / sweeping changes: bump MAJOR (e.g. 1.x → 2.0).
 *
 *   Pre-scheme entries (1.9.38 and earlier) used `MAJOR.MINOR.PATCH` and
 *   bumped patch per commit, which inflated 1.9 to 38 patches. Those
 *   entries are kept as-is for history (now in `changelog-archive.ts`).
 *
 * Order: newest first (the UI renders top-to-bottom as-is).
 */

export type ReleaseEntry = {
  version: string;
  /** ISO date `YYYY-MM-DD` of the bump. */
  date: string;
  /**
   * Short bullet points for the release. Markdown not supported.
   * 旧スキーム (1.9.38 以前) で使用、新スキームでも軽微な変更で使う。
   * `parts` と排他: 同時指定された場合 UI は `parts` を優先表示。
   */
  notes?: string[];
  /**
   * 1 日内に多数のコミットがある日 (新スキーム運用後の典型) で、
   * notes を「コミットごとの part」に分割して表示するためのフィールド。
   * 各 part は折りたたみ可能 (`<details>`) で、title だけ常時表示し、
   * 詳細 body はクリックで開閉する。
   */
  parts?: ReleasePart[];
};

export type ReleasePart = {
  /** 折りたたみ時に常時表示される 1 行サマリー (絵文字 + 短い見出し) */
  title: string;
  /** 展開時に表示される本文 (1〜数文程度の詳細) */
  body: string;
};

export const RELEASES: ReleaseEntry[] = [
  {
    version: "2.1",
    date: "2026-05-07",
    parts: [
      {
        title: "🩹 TODO #71 part3 — 残留検知時に `stale.remove()` で物理削除を追加 + selector を `:not([data-open])` に絞り込み (part2 でも本番白枠が継続したため強化)",
        body: "**症状継続 (part2 デプロイ後)**: 「コメント開いた後にホバー外すと白い枠が出続ける」報告継続。part2 で「DOM 残留検知時のみ remount」に縮退したが、key bump (= React tree remount) では Base UI の Portal 経由で document.body 直下に植えた node が消えないケースがあると判明 (production-only race の症状そのまま)。React 19 + Vercel Edge build で createPortal の cleanup が不完全で、React 側 unmount 後も DOM node が物理的に残るパターン。\n\n**修正** ([src/components/portal/comment-popover.tsx](src/components/portal/comment-popover.tsx)):\n- selector を `[data-slot=\"popover-content\"]` から `[data-slot=\"popover-content\"]:not([data-open])` に絞り込み — open 中の他 popover (recruitment-templates-button 等) を誤射しない\n- `querySelectorAll` で複数 stale node を網羅取得 (本来 1 件想定だが防御的)\n- 検知時は **`stale.remove()` で物理削除 + `setRemountKey` で React ツリー remount** の二段重ね — Portal cleanup 漏れに対しては DOM 直接操作が最も確実、加えて React 側も新規 instance に置換することで Base UI の internal state が同期する\n- 待機時間 200ms → 250ms (production の close transition + paint flush 完了をより確実に待つ)\n- コメント (L62-71) も二段重ね方式に書き換え\n\n**動作差** (part2 比):\n- 通常 close (DOM 正常 unmount): part2 = remount スキップ (selector ゼロ件) / part3 = 同じく remount スキップ → hover 連続反応維持を継続\n- DOM 残留 (production-only race): part2 = key bump で React tree remount (が Portal node に効かないケース) / part3 = `stale.remove()` で物理削除確実 + key bump 併用\n- selector 範囲: part2 = 任意の `data-slot=\"popover-content\"` / part3 = `:not([data-open])` で close 状態のみ → open 中の他 popover を誤射しない安全性向上\n\n**DOM 直接操作の副作用受入**: Base UI の internal state と DOM の整合が取れなくなる懸念があるが、key bump で React tree も remount するため Base UI instance も新規生成 → 古い ref を握っている古い instance が削除済 node にアクセスする経路は無し。仮に古い instance が dangling 参照を持っても、setState 後の reconciliation で破棄される。\n\n**検証**: `tsc --noEmit` PASS。dev preview で click open → close → 250ms 後の DOM カウント 0 件 (= remount 未発火、selector ゼロ件) を確認、連続 click 再 open も反応継続。selector + node.remove() の単独動作も `preview_eval` で確認 (1 件マッチ → remove → DOM から消滅)。console error / warning 0 件。production-only race そのものは dev 再現不能のため、本番デプロイ後にユーザー実機で「ホバー外しで白枠が出続ける」が解消されることを最終確認予定。",
      },
      {
        title: "🩹 TODO #71 follow-up — 常時 remount で hover 連続反応が阻害される副作用を「DOM 残留検知時のみ remount」に縮退",
        body: "**症状**: TODO #71 (本日 part1, PR #30) の `<Popover key={remountKey}>` 強制 remount で白枠残留は消えたが、副作用として hover 経路の UX が劣化。コメントアイコンに hover → mouseleave で close → 200ms 後に key bump で `<Popover>` ツリーが unmount → remount → trigger 要素も再生成、という流れで cursor が trigger 上に残ったままでも `mouseenter` が新規発火しないため (mouseenter は要素境界を新規に跨いだ時のみ発火する仕様)、hover による再 open が阻害され click が必要になっていた。ユーザー体感「クリック時 Open は割とだるい」。\n\n**ユーザー認識ズレ**: 「白枠残留はページ更新直後など稀な場合のみ出る」と想定していたが、TODO #71 の修正は **毎回の close で必ず remount** する設計だった。本番運用では白枠残留自体は実際に頻発していたものの、毎回 remount は過剰と判断し条件付き化に縮退。\n\n**修正** ([src/components/portal/comment-popover.tsx](src/components/portal/comment-popover.tsx)):\n- close 後 200ms の `setTimeout` callback 内で `document.querySelector('[data-slot=\"popover-content\"]')` で portal node の生存を判定\n- **残留時のみ** `setRemountKey((k) => k + 1)` で key bump (= 既存の DOM リセット挙動を発火、白枠根絶を維持)\n- **残留なし時** は何もしない (= 通常時 hover 連続反応を維持)\n- close → 200ms 待機中に再 open された場合のレース対策で `else if (open && remountTimer.current)` ブロックを追加し、open=true 復帰時に scheduled timer を clearTimeout (open 中に key bump で popover が瞬間消失するのを防止)\n- `remountKey` の用途コメントを「常に remount」→「DOM 残留検知時のみ remount」に書き換え\n\n**動作差** (part1 比):\n- 通常 close (DOM 正常 unmount): part1 = 200ms 後に必ず remount / part2 = remount スキップ → 連続 hover で再 open 可能\n- 稀な DOM 残留: part1 = 200ms 後に remount で根絶 / part2 = 同じく remount で根絶 (検知ロジック追加のみ、対応経路は等価)\n- 連続 hover で 200ms 内に再 open: part1 = remount → trigger 再生成で hover 復帰失敗 / part2 = timer cancel → スムーズに open 維持\n\n**副作用 / トレードオフ**:\n- selector `[data-slot=\"popover-content\"]` は他の同時 open popover (`recruitment-templates-button` 等) も拾う可能性があるが、自分が残留してない場合 remount しても害は trigger サブツリーの 1 度の再 mount だけ (open 中の他 popover は別ツリーで触らない) なので UX 上ほぼ気付かない\n- 厳密な identification (ref + aria-controls ベース) は Base UI internal id 体系に依存して実装コスト高 vs 効果薄なので不採用\n\n**検証**: `tsc --noEmit` PASS。dev preview で click open → outside click close → 250ms / 550ms / 650ms の各時点で `[data-slot=\"popover-content\"]` の DOM カウントを `preview_eval` で取得し、通常時に 0 件 (= remount 未発火) を確認。phantom node を手動 inject した残留シナリオでも再 click open が反応することを確認。console error / warning 0 件。本番デプロイ後にユーザー側で「hover 連続反応が click なしで復元している」を実機確認予定。",
      },
      {
        title: "🐛 TODO #71 クローズ — TODO #70 follow-up: production build のみで残る popover DOM 残留を `<Popover key={remountKey}>` 強制 remount で根絶",
        body: "**症状**: TODO #70 (PR #29, Strategy G の popover/tooltip 適用) 後も本番 deploy で「コメントアイコンに hover → カーソル外に移動 → 白枠だけ残る」が継続。条件: ページ更新後の **hover 経路だけ** + click 経路では非発生 + scroll でも残り続け、枠外 click や別ウィンドウ選択で消える。dev preview では完全消失するが production build のみで再発。\n\n**真因仮説**: TODO #70 で Strategy G を CSS exit transition 撤去で適用したが、本番では DOM ノード自体が `setOpen(false)` 後も React で unmount されず残る現象。Base UI Popover の `open` controlled prop 切替時、production build で portal close race が起きるか、もしくは trigger button の `:hover` paint が popover close と競合してブラウザの paint optimization で残留する。dev で再現しない = production-only。Strategy G の前提だった「CSS リバウンド」とは別系統。\n\n**修正** ([src/components/portal/comment-popover.tsx](src/components/portal/comment-popover.tsx)):\n- `remountKey` state (`useState<number>(0)`) を追加、`<Popover key={remountKey} open={open} onOpenChange={setOpen}>` で key driven な force remount\n- `prevOpen` ref (`useRef<boolean>(false)`) で open=true→false の遷移を検出する `useEffect`\n- close transition 完了後 (200ms) に `setRemountKey(k => k + 1)` を呼び出し → React が `<Popover>` ツリー全体を unmount + 新規 mount → portal / popup / trigger の DOM が完全リセット\n- cleanup の useEffect で `remountTimer` も clearTimeout (アンマウント時の memory leak 防止)\n\n**動作差**:\n- close 直後 (200ms 未満): DOM は通常通り (Base UI が unmount 試行)\n- 200ms 後: 仮に DOM が残っていても force remount で完全リセット\n- 連続 hover の場合: cursor が trigger 上に残ったまま remount すると新規 trigger に hover 再判定 → popover が再 open される可能性 (200ms 遅延でユーザーが移動済の確率が高い、許容)\n- click 経路への影響なし (もともと再現しなかった経路)\n\n**副作用**:\n- 200ms タイマー 1 つ追加 (cleanup 済)\n- React reconciler が close ごとに `<Popover>` サブツリーを破棄 + 新規生成 → 軽微な GC コスト (アイコン 6〜10 個の小規模 portal なので無視可能)\n- `hoverProps` (cancelClose / scheduleClose 120ms grace) のロジックは変更なし、UX 等価\n\n**検証**: `tsc --noEmit` PASS。dev preview で click open → outside click close → 250ms 後 / 650ms 後 (remount timer 完了後) の両時点で `[data-slot=\"popover-content\"]` が DOM から消失することを `preview_eval` で確認、再 click open でも問題なく動作。console error / warning 0 件。dev では DOM 残留が元々起きないため remount の効果検証は production deploy 後にユーザー側で「ページ更新後 hover → カーソル外移動 → 白枠が消える」を実機確認予定。\n\n**前提**: TODO #70 (popover.tsx / tooltip.tsx の Strategy G) は維持。今回の修正と相補的に効く (Strategy G で zoom-out リバウンドを排除 + remount で DOM 残留も排除)。",
      },
      {
        title: "🐛 TODO #70 クローズ — スケジュール各員コメント popover の close 時に「白枠だけ残る」現象を Strategy G (CSS exit transition 撤去) で根絶",
        body: "**症状**: スケジュールページ TOP の表で、メンバーごとのコメントアイコンに hover すると `CommentPopover` が開いてコメントが表示されるが、カーソルを popover / トリガーから外側に移動すると、コメント本文と背景は消えるのに「白い枠だけ」が表示されたまま残ることがあった (毎回ではなく散発、特に rapid hover-leave で再現しやすい)。\n\n**真因**: TODO #65 (PR #22, commit `18a338f`) で `dropdown-menu.tsx` に対して同根のバグを「3 段階ちらつき」として既に修正済みだったが、`popover.tsx` / `tooltip.tsx` には同じ Base UI close 時アニメ (`data-closed:animate-out / fade-out-0 / zoom-out-95`) が残存していた。close transition (~100ms) の間に zoom スケール 95% への縮小と fade-out 透明化のタイミングずれが発生すると、`bg-popover` の中身は透明化していく一方で `ring-1 ring-foreground/10` + `shadow-md` の輪郭だけが visible に残るフレームが visual artifact として観測される現象。`CommentPopover` は 120ms grace period を持つため open/close の頻度が高く、特に発火しやすかった。\n\n**修正** (Strategy G の popover/tooltip 版):\n- [src/components/ui/popover.tsx](src/components/ui/popover.tsx): `PopoverContent` の `<PopoverPrimitive.Popup>` className から `data-closed:animate-out` / `data-closed:fade-out-0` / `data-closed:zoom-out-95` の 3 クラスを削除\n- [src/components/ui/tooltip.tsx](src/components/ui/tooltip.tsx): `TooltipContent` の `<TooltipPrimitive.Popup>` className から同 3 クラスを削除 (今回のバグには直接関与しないが、Base UI overlay 三兄弟 dropdown / popover / tooltip で方針統一する予防修正)\n\n入場アニメーション (`data-open:animate-in / fade-in-0 / zoom-in-95` + `data-[side=*]:slide-in-from-*`) は維持。close 時は瞬間消滅、open 時は従来通り 100ms かけて zoom-in + fade-in。\n\n**副作用受入**: Base UI overlay 全種類で「閉じる時のフェードアウト」が消えて瞬間消滅になる。`CommentPopover` の hover 制御 (`cancelClose` / `scheduleClose` 120ms grace) のロジック自体は変更なし。recruitment-templates-button (`PopoverContent` の唯一の他使用箇所) も close 瞬間消滅になるが click open / click outside close の機能上問題なし。dropdown-menu と UX 統一が取れる。\n\n**動作差** (修正前 → 修正後):\n- hover → leave → close: 「中身は消えるが白枠だけ残る」現象 → close 即時 unmount で白枠が物理的に出現しない\n- DOM 実観測 (preview_eval): mouseleave 後 250ms で `[data-slot=\"popover-content\"]` ノードが完全に消滅、`data-closed` 属性が DOM に現れるフェーズ自体が観測されない\n\n**Strategy G の前例**: dropdown-menu (TODO #65, PR #22) で同方針を本番適用済 + UX 許容確認済。本修正は同パターンの popover / tooltip 版として一貫性を持つ。将来フェードアウト復活したい場合は `duration-50` + `opacity-only` 版 (zoom-out なし → リバウンド再発リスク低) で復活可能。\n\n**検証**: `tsc --noEmit` PASS。dev preview (worktree, `.env.local` を main repo からコピー) でスケジュールページ TOP の `CommentPopover` を `mouseenter` で open → `mouseleave` で close、close 後 250ms で popover ノードが DOM から完全消失することを `preview_eval` で確認。screenshot で popover open 状態と close 後状態の両方を撮影し、close 後に白枠が残っていないことを目視確認。console error / warning 0 件。",
      },
      {
        title: "🐛 TODO #69 クローズ — schedule_past_sessions に CANDIDATE 行が混入するバグを修正 (snapshot DECISION-only filter + 既存 CANDIDATE row 自動 cleanup)",
        body: "**狙い**: ユーザー報告「確定日以外の日付も過去日程として記録に残ってしまっている」の修正。`schedule_past_sessions` テーブルには本来 **実開催が確定した DECISION 行のみ** が入る前提だが、snapshot path に穴があり CANDIDATE 行も混入していた。\n\n**真因** ([src/lib/server/schedule-snapshot.ts](src/lib/server/schedule-snapshot.ts)): `runScheduleSnapshot()` が `fetchScheduleRaw()` で得た sessions を **status (DECISION/CANDIDATE) を見ずに全件 UPSERT** していた。これが `mergeStoredPastSessions` ([src/lib/schedule/next-session.ts:185-220](src/lib/schedule/next-session.ts:185)) の「raw_date 照合だけで verified set に入れて DECISION 強制」挙動と組み合わさり、CANDIDATE 由来 row が **過去確定日として表示** されていた。`supabase/schema.sql` の `schedule_past_sessions` には status 相当列が無く (`source` のみ)、永続化レイヤで DECISION/CANDIDATE を区別できないため、入口 (snapshot) 側で混入を止める方針で修正。\n\n**Discord 取り込み (`importDiscordScheduleHistory`) は問題なし** — 「本日YYYY/MM/DD…は固定活動予定日です」当日通知メッセージのみ scrape するため CANDIDATE 混入は構造的に発生しない。\n\n**修正**:\n- [schedule-snapshot.ts](src/lib/server/schedule-snapshot.ts) `runScheduleSnapshot()`:\n  - `decisionSessions = sessions.filter(s => s.status === \"DECISION\")` で **DECISION 行のみ snapshot 対象** に絞る\n  - 残りの `candidateRawDates` に該当する `source='snapshot'` row を delete (次回 snapshot 実行時に自動 cleanup)。`source='discord'` / `'manual'` は authoritative / admin 操作なので touch しない\n  - 戻り値に `cleanedCandidates: number` を追加 (cleanup 件数を可視化)\n- [categories-actions.ts](src/lib/server/categories-actions.ts): `ScheduleSnapshotResult` 型に `cleanedCandidates` 追加、`snapshotScheduleNow` の admin gate fallback 戻り値も追従\n- [past-sessions-section.tsx](src/components/portal/settings/past-sessions-section.tsx): snapshot 結果表示 (toast + 結果パネル) に `候補日 cleanup N` を追加表示、説明文を「DECISION 行のみ保存 / CANDIDATE 行は自動 cleanup」に書き換え\n- [api/cron/snapshot-schedule/route.ts](src/app/api/cron/snapshot-schedule/route.ts): cron response JSON に `cleanedCandidates` を含める (Vercel cron logs で確認可)\n\n**運用前提**: char-sheets 上で当日 21:50 JST (cron 実行時刻) までに DECISION マークされた session のみ snapshot に乗る。CANDIDATE のままだと attendance データが永続化されないため、「実施前に DECISION マークする」運用ルールでカバー (元々の運用前提どおり)。\n\n**既存 CANDIDATE 由来 row のクリーンアップ**: 次回 snapshot 実行 (cron 21:50 JST or settings dialog の「出席状況を即時保存」ボタン) で自動的に削除される。手動 SQL 実行は不要。実行後は settings dialog の「DB の保存件数」で row 一覧を確認可能 (各 row に source ラベルが表示される)。\n\n**検証**: `node D:/workd/portal/node_modules/typescript/bin/tsc --noEmit` PASS。実発火経路 (Vercel cron + Supabase + char-sheets fetch) は worktree dev preview では再現不能のため、本番デプロイ後に「snapshot 即時実行 → settings dialog の DB 保存件数で snapshot row 一覧を確認 → CANDIDATE 由来 row が消えている」を目視確認予定。",
      },
    ],
  },
  {
    version: "2.1",
    date: "2026-05-02",
    parts: [
      {
        title: "🔧 TODO #68 part10 — 詳細診断 chunk を「details 開時のみマウント」する controlled 構造に強化",
        body: "**狙い**: PR #24 の TODO #68 完了版では `logsResult.diag` が真値になった瞬間に `<FflogsDiagnosticsPanel>` が常に mount されており、詳細診断 details を **開かなくても** chunk fetch が発火する設計だった (連動実行 = 必ず fetch)。TODO 文言「details を開いた時に load」を厳密に満たすよう、root details を親側に持ってきて `useState diagOpen` で controlled 化、open=true 時だけ panel を mount する形に再構成。連動実行のみで詳細診断を開かない通常運用では chunk fetch を完全回避できる。\n\n**実装**:\n- [fflogs-diagnostics-panel.tsx](src/components/portal/settings/fflogs-diagnostics-panel.tsx): root `<details>` + `<summary>` を撤去し、内部の `<div>` (詳細診断 body) のみを返す形に変更。export 形態は named (`FflogsDiagnosticsPanel`) + default export 両対応 (dynamic import 側は default を使う)\n- [fflogs-sync-section.tsx](src/components/portal/settings/fflogs-sync-section.tsx): `useState<boolean>(false)` の `diagOpen` state を追加、`<details className=\"mt-2 group/diag\" onToggle={(e) => setDiagOpen(e.currentTarget.open)}>` で wrap、body 部分は `{diagOpen && <FflogsDiagnosticsPanel diag={...} userTypeFields={...} />}` で条件マウント。details が閉じている間は panel 自体が unmount = chunk も load されない\n\n**`<details onToggle>` パターン**: uncontrolled-with-listener パターン (browser native details の開閉動作を維持しつつ open 状態を React state に同期) を採用。React の controlled `<details open={diagOpen}>` を使うと user click による native トグルが効かなくなるため avoid。\n\n**動作差** (PR #24 比):\n- 連動実行 → 詳細診断 details を **開かない**: PR #24 = chunk fetch される / part10 = chunk **fetch されない**\n- 連動実行 → 詳細診断 details を **開く**: PR #24 = 既に fetch 済 / part10 = 開いた瞬間に fetch 開始\n- 連動実行 → details を開いた後 **閉じる**: PR #24 = panel mount 維持 / part10 = panel unmount (再度開けば再 mount = render は走るが chunk 自体は browser cache から)\n\n**副作用**: 詳細診断 details を閉じる時に内側の入れ子 details (htmlSample / titleDateMissSample / userTypeFields) の open 状態がリセットされる (再度親 details を開いた時、これらは閉じた状態に戻る)。ユーザーが直接見ない状態の話なので UX 等価。\n\n**検証**: `tsc --noEmit` PASS。dev preview で Settings dialog を開き FFLogs section 主要 UI (連動 / 表示名 / Session Cookie / クリア) 全描画 + 連動未実行時に「詳細診断」summary 非表示 + console error 0 件を確認。",
      },
      {
        title: "📦 TODO #68 クローズ — `fflogs-sync-section.tsx` 内の詳細診断パネル (~190 行) を `next/dynamic({ ssr: false })` で別 chunk に分離",
        body: "**狙い**: TODO #66 で settings-dialog を 5 つの sub-component に分割した際、最大セクション `fflogs-sync-section.tsx` (962 行) 内の詳細診断パネル (`logsResult.diag` + `userTypeFields` 描画、HTML サンプル / titleDateMissSample / v2OwnersSample 含む ~190 行) が後続最適化として残されていた。FFLogs 連動を実行して結果が「合うレポートなし」になり、かつ詳細診断 details を開いた時にしか見えない稀な UI なので、`next/dynamic({ ssr: false })` で別 chunk 化すれば本物の lazy load 効果が得られる (TODO #67 で確立した archive lazy import パターンの React component 版)。\n\n**実装**:\n- 新規 [src/components/portal/settings/fflogs-diagnostics-panel.tsx](src/components/portal/settings/fflogs-diagnostics-panel.tsx) (203 行): 旧 inline JSX の `<details>` ブロック (詳細診断パネル全体 + 内部の HTML サンプル / titleDateMissSample / userTypeFields nested details) を独立 client component 化。Props は `diag: FflogsDiagInfo` + `userTypeFields?: string[]`。`FflogsDiagInfo` 型もこのファイルで export\n- [src/components/portal/settings/fflogs-sync-section.tsx](src/components/portal/settings/fflogs-sync-section.tsx): `dynamic(() => import(\"./fflogs-diagnostics-panel\"), { ssr: false })` で wrap した `FflogsDiagnosticsPanel` を import、旧 inline `{logsResult.diag && <details>...</details>}` を `{logsResult.diag && <FflogsDiagnosticsPanel diag={logsResult.diag} userTypeFields={logsResult.userTypeFields} />}` に置換。962 行 → 802 行 (-160 行)\n\n**chunk 分離検証**: dev build artifact `.next/dev/static/chunks/0qc1_src_components_portal_settings_fflogs-diagnostics-panel_tsx_07ut2zu._.js` として独立 chunk 生成を確認。\n\n**Network タブ挙動 (dev preview)**:\n- 初期ページロード時: `fflogs-diagnostics-panel` chunk は **未 fetch** (`document.querySelectorAll('script[src]')` 全 45 件中該当 chunk なし)\n- Settings dialog open 時: 引き続き **未 fetch** (FflogsSyncSection が mount されても dynamic 側は遅延)\n- 「FFLogs と動画を連動」ボタンクリック後: linkFflogsReports 実行 → `logsResult.diag` set → `<FflogsDiagnosticsPanel>` mount → chunk **fetch される** (script tag 47 件に増加、該当 chunk が登場)\n\n**UI 動作等価性**: 連動結果として `詳細診断` / `日付抽出に失敗したタイトル` / `User 型のフィールド一覧` などすべての nested details が従前通り render。console error / warning なし。tsc --noEmit PASS。\n\n**今後**: 同パターンを他の重い conditional UI (例: chunk 化候補があれば) にも適用可能。",
      },
      {
        title: "🪟 TODO #65 クローズ — Film/Logs dropdown スクロール時の 3 段階ちらつきを Strategy G (CSS exit transition 撤去) で根絶",
        body: "**狙い**: PR #19 (`disableAnchorTracking`) + PR #20 (re-open lock) でも残った「dropdown 開→ wheel scroll で 1 度消え → 1 度出 → 最終 close」の 3 段階 re-toggle を解消。schedule 過去詳細表で動画 / Logs アイコンを開いてスクロールした時の挙動を本番 UX レベルに整える。\n\n**真因確定** (PR #21 debug log で本番観測): wheel scroll 時の React state 遷移は `onOpenChange(true) → setOpen(true) → armed → scroll-close fired → cleanup` の **open→close 単一遷移** で完結しており、`IGNORED — locked` も 2 回目の `onOpenChange(true)` も発生していなかった。つまり「現れる」段階は React state 由来ではなく **CSS exit transition のリバウンド** が真因。Base UI Menu が `setOpen(false)` を受けて `data-closed` 属性を付け、`data-closed:animate-out / fade-out-0 / zoom-out-95 / overflow-hidden` の組合せで 100ms かけて消えていく途中で zoom-out のスケール変化が visual rebound を生んでいた。\n\n**修正** (Strategy G — [src/components/ui/dropdown-menu.tsx](src/components/ui/dropdown-menu.tsx)):\n- `DropdownMenuContent` / `DropdownMenuSubContent` の className から `data-closed:animate-out` / `data-closed:overflow-hidden` / `data-closed:fade-out-0` / `data-closed:zoom-out-95` を削除\n- 入場アニメーション (`data-open:animate-in / fade-in-0 / zoom-in-95`) は維持\n- 結果: close 時は瞬間消滅 (フェードアウトなし)、open 時は従来通り 100ms かけて zoom-in + fade-in\n\n**副作用受入**: 全 dropdown で「閉じる時のフェードアウト」が消えて瞬間消滅になる。本来意図せぬ全体仕様変更で UX 軽微損失だが、3 段階ちらつき根絶を優先 (本番 UX 判定で許容確認済)。将来フェードアウト復活したい場合は `duration-50` + `opacity-only` 版 (zoom-out なし) でリバウンド再発リスク低い妥協案あり。\n\n**Strategy A/B の整理**:\n- A (`disableAnchorTracking`, PR #19) は維持 — 真因と無関係だったが副作用なし\n- B (re-open lock, PR #20) は debug log のみ撤去して維持 — Base UI / React 19 race の防御的 guard として残置\n- debug console.log (PR #21) は撤去 — 真因特定済\n\n**ファイル**: [src/components/ui/dropdown-menu.tsx](src/components/ui/dropdown-menu.tsx) -2 行 / [src/lib/use-scroll-closing-menu.ts](src/lib/use-scroll-closing-menu.ts) -45 行 (debug log 撤去 + コメント整理)\n\n**検証**: `tsc --noEmit` PASS。dev preview は Film/Logs dropdown が demo data に存在せず再現不能だったが、本番デプロイ後にユーザー側で過去詳細表 → 動画 dropdown 開 → wheel scroll で 3 段階ちらつき完全消滅 + close 時瞬間消滅 UX 許容を確認。",
      },
      {
        title: "🧹 TODO #66 クローズ — `settings-dialog.tsx` (1,761 行 / 88 KB) を 5 つの sub-component に機能別分割",
        body: "**狙い**: 2026-05-02 のプロジェクト肥大化精査で settings-dialog + schedule-list が src/ 全体の 11.6% を占めると判明した件への対応。settings-dialog 単体で 1,761 行 / 88 KB あり編集 / 読解 / レビューの摩擦が高かったため、ロジカルなセクション境界に沿って 5 つの sub-component に分割し、各セクション固有の state を section 内部に閉じ込めて親シェルを薄くする。\n\n**実装** (新規ディレクトリ [src/components/portal/settings/](src/components/portal/settings/)):\n- [schedule-source-section.tsx](src/components/portal/settings/schedule-source-section.tsx) (103 行): character-sheets URL 入力 UI のみの presentational component。url state は親が保持 (フッター保存ボタンで channelId と一括保存するため)\n- [past-sessions-section.tsx](src/components/portal/settings/past-sessions-section.tsx) (352 行): Discord channel ID + 取り込み / snapshot / 件数 / 個別削除。`importing / importResult / counting / storedInfo / deletingRow / snapshotting / snapshotResult` の state とハンドラを section 内に局所化。channelId のみ親が保持\n- [fflogs-sync-section.tsx](src/components/portal/settings/fflogs-sync-section.tsx) (962 行): v1 username + v2 OAuth + Session Cookie + 連動実行 + 結果パネル + 詳細診断パネル。最大セクション。`fflogsUsername / oauthStatus / cookieStatus / sessionCookieInput / linkingLogs / logsResult / clearingLogs / disconnecting / savingCookie / savingUsername` を section 内に局所化。open 時の OAuth/cookie/username fetch も section 自前\n- [changelog-footer.tsx](src/components/portal/settings/changelog-footer.tsx) (232 行): 更新履歴トグル + GitHub / Lodestone リンク + Sign out + dynamic archive lazy load。`showChangelog / archiveReleases / loadingArchive / archiveError` を section 内に。GithubMark inline SVG もここに移動\n- [danger-zone-section.tsx](src/components/portal/settings/danger-zone-section.tsx) (58 行): 全データ初期化 (TODO #23) のトリガーボタン + DataInitConfirmDialog の wrap。`showDataInitDialog` state を section 内に。onComplete callback で親 dialog の close + router.refresh\n\n**親シェル**: [settings-dialog.tsx](src/components/portal/settings-dialog.tsx) を 1,761 行 → **208 行** (-88%) に縮小。保持する state は dialog 制御 (`open`) + フッター保存ボタンが両方更新する `url / channelId / busy` のみ。OAuth callback handler (`?fflogs_oauth_connected` 検出 → setOpen(true)) は dialog 制御 state を握る親に残す。url + channelId の初期 fetch も親自前 (子 section の channelId は controlled prop)。\n\n**初期 fetch の分散**: 旧 1 つの useEffect [open] で 5 件 Promise.all していたものを「親 = url + channelId」「FflogsSyncSection = OAuth + cookie + username」の 2 つに分割。同時並列性は維持され、各 section が自己完結する。\n\n**動作影響**: UI 出力は等価 (4 セクション header + 全ボタン + 全リンク + lazy archive 動作 + Danger Zone confirm dialog 動作確認済)。Sub-component 分割自体は client bundle 合計サイズは変えない (chunk 分割しても全部使うため)。本 TODO のメイン狙いはファイル肥大化解消 + 編集 / レビュー時の認知負荷削減であり、初期 bundle 削減は TODO #67 (changelog archive lazy) で実現済。\n\n**検証**: `tsc --noEmit` PASS。dev preview (worktree、`.env.local` を main repo からコピー) で Settings dialog を開き、(1) 4 section header 描画、(2) 全 10+ ボタン / 5 リンク表示、(3) 更新履歴トグル → 1 件 → archive 押下で 10 件結合 + ボタン消滅、(4) 全データ初期化 confirm dialog 開閉、を eval 経由で確認。console errors / warnings なし。\n\n**今後の拡張余地**: fflogs-sync-section の詳細診断パネル (logsResult.diag、HTML サンプル含む ~150 行) を `next/dynamic` で別 chunk 化すれば true lazy load 可能だが、本 TODO のスコープ外として別途検討。",
      },
      {
        title: "📦 TODO #67 クローズ — `changelog.ts` (629 行 / 234 KB) を最新リリース 1 件 + `changelog-archive.ts` に分離 + dynamic import で lazy load",
        body: "**狙い**: 2026-05-02 のプロジェクト肥大化精査で、`changelog.ts` が git tracked サイズ 234 KB (リポ全体で `package-lock.json` に次ぐ 2 位) と判明。`settings-dialog` から `import { RELEASES } from '@/lib/changelog'` で全件読み込んでおり、初期 client bundle に過去 10 リリース分の本文が常時混入していた。`updateTag` ベースの cache (TODO #55 で復活) と同じ思想で「使う時だけ load」する設計に変更し、初期 bundle を縮小する。\n\n**実装**:\n- 新規 [src/lib/changelog-archive.ts](src/lib/changelog-archive.ts): 過去 9 リリース (2.1 / 2026-05-01 から 1.9.35 まで) を `RELEASES_ARCHIVE: ReleaseEntry[]` として export。型は本体から `import type` で参照\n- [src/lib/changelog.ts](src/lib/changelog.ts): `RELEASES` を最新 1 件 (現状 `2.1 (2026-05-02)`) のみに削減。本体サイズ 234 KB → 19.6 KB (-91.6%)。ヘッダーコメントに分離方針 + graduate ルール (リリース更新時は最古を archive 先頭に移す) を明記\n- [src/components/portal/settings-dialog.tsx](src/components/portal/settings-dialog.tsx): `archiveReleases / loadingArchive / archiveError` の useState 追加。更新履歴セクションは `RELEASES` のみ即時表示し、末尾に「↓ 過去の更新履歴を見る」ボタンを追加。クリックで `import('@/lib/changelog-archive')` を発火 → `RELEASES_ARCHIVE` を state に格納 → `displayReleases = [...RELEASES, ...archiveReleases]` を map。読み込み中は disabled + 「読み込み中…」、失敗時は rose tone で error メッセージ表示\n- 既存「↗ これより前の履歴を GitHub で見る」リンクの文言を「↗ commit log を GitHub で見る」に変更 (archive で全更新履歴が見られるため、GitHub link は commit 単位の参照として位置付け直し)\n\n**動作影響**: 設定ダイアログを開いて「更新履歴」を押すと最新 1 リリースのみ表示。「過去の更新履歴を見る」を押すと archive (~210 KB) を dynamic chunk として fetch し、結合表示に切替わる。今後リリースを追加する時は `changelog.ts` の `RELEASES[0]` を新エントリで置き換え、旧 `RELEASES[0]` を `changelog-archive.ts` の `RELEASES_ARCHIVE[0]` に prepend する graduation 運用。\n\n**検証**: `tsc --noEmit` PASS。dev preview で `(1) RELEASES のみ 1 件表示 → (2) archive ボタンクリック → (3) 10 件結合表示 + ボタン消滅` を eval 経由で確認。console エラーなし。site-header (`RELEASES[0].version + .date` 参照) も正常 (ヘッダーで `v2.1 (2026-05-02)` 表示維持)。",
      },
      {
        title: "🗂 TODO #64 クローズ — schedule_past_sessions の logs_url を子テーブル `schedule_past_session_logs` に分離 (1 セッション = 複数 FFLogs URL)",
        body: "**狙い**: 同じ session 日付に対して複数の FFLogs report を紐付けたいケース (午前/午後分けて log を取った、ロビー失敗で同日 2 report 等) を素直に扱えるようにする。これまでは `schedule_past_sessions.logs_url` 単数列で 1 URL しか保持できず、後発の URL は popover の編集で **上書き** するしかなかった。子テーブル化で 1:N 関係を表現し、popover 側も「行ごとに開く + × 削除 + 末尾で追加」の簡素 UI に整理する。\n\n**スキーマ変更** ([supabase/schema.sql](supabase/schema.sql)):\n- 新規テーブル `public.schedule_past_session_logs` を Section 5d として追加: `id uuid PK / raw_date text REFERENCES schedule_past_sessions(raw_date) ON DELETE CASCADE / url text NOT NULL / source text CHECK (source IN ('auto','manual')) / created_at / UNIQUE (raw_date, url)`。同 URL の重複 INSERT は UNIQUE 制約で弾く\n- ワンショット seed: `DO $$ ... IF EXISTS (... column_name = 'logs_url') THEN INSERT ... SELECT raw_date, logs_url, COALESCE(NULLIF(logs_url_source,''),'manual') FROM schedule_past_sessions WHERE logs_url IS NOT NULL ON CONFLICT DO NOTHING; END IF; END $$` で旧 1:1 列を行に変換 (information_schema ガードで再実行安全)\n- 旧列 DROP: `ALTER TABLE schedule_past_sessions DROP COLUMN IF EXISTS logs_url, DROP COLUMN IF EXISTS logs_url_source` + CHECK 制約 drop。`category_links.logs_url` / `logs_url_source` は動画用なので残置 (今回スコープ外)\n- 新テーブルを RLS / REPLICA IDENTITY FULL / supabase_realtime publication の各配列に追加 (Section 7 / 7b / 8 の DO $$ ループに `schedule_past_session_logs` を追記)\n\n**Server-side reader 改修** ([fflogs.ts](src/lib/server/fflogs.ts)):\n- `fetchSessionLogsByDate()` の戻り値を `Record<string, string>` から `Record<string, SessionLogEntry[]>` に変更。`schedule_past_session_logs` を `created_at asc` で全件 select し、`{id, url, source}` 形に整形して rawDate ごとに配列で集約\n- `linkReportsToSessions()` (FFLogs sync auto-linker): 旧 `update logs_url=... where logs_url IS NULL` を、新 `schedule_past_session_logs` への INSERT (source='auto') に置換。事前に「既存ログがあるセッションは skip」する filter を追加し、manual 行を持つセッションには auto 行を併設しない (= 旧 IS NULL filter 相当の挙動を維持)\n- 1.9.10 wipe: 旧 `schedule_past_sessions.logs_url=null where logs_url_source='auto'` を `schedule_past_session_logs.delete().eq('source','auto')` に置換\n- 1.9.11 ONE-TIME bootstrap: `schedule_past_sessions` 側の flip ブランチを撤去 (column 自体が無くなるため)。`category_links` 側は残置\n\n**Server Action 再編** ([categories-actions.ts](src/lib/server/categories-actions.ts)):\n- `setSessionLogsUrl(rawDate, logsUrl|null, sessionDetails?)` を **撤廃**\n- 新 `addSessionLogsUrl(rawDate, logsUrl, sessionDetails?)`: URL バリデーション → 親 `schedule_past_sessions` 行を upsert (live session 用) → `schedule_past_session_logs` に INSERT (source='manual')。UNIQUE 衝突 (PG code 23505) は friendlier reason に変換\n- 新 `deleteSessionLogsUrl(id)`: 子テーブル PK 指定 1 行削除 (auto/manual 両方削除可、誤 auto-link を popover から直接消せる)\n- `clearAllFflogsLinks()` の session 部分: 旧 `update logs_url=null` から新テーブルの全 DELETE に置換 (legacy 同様、source 区別せず一掃)\n\n**Popover UI 簡素化** ([session-memo-popover.tsx](src/components/portal/session-memo-popover.tsx)):\n- prop `currentLogsUrl: string | null` を `sessionLogs: SessionLogEntry[]` に置換 (型は `src/lib/schedule/session-logs.ts` で共有)\n- 旧 \"単一 URL の input + 保存 / クリア\" UI を撤去\n- 新レイアウト: 既存エントリを 1 行ずつ list 表示 (URL + source バッジ + 開く + ×)、その下に input + 追加 ボタン。編集 (in-place edit) は廃止して append-only に簡素化\n\n**呼出側型追従** ([schedule-page-body.tsx](src/components/portal/schedule-page-body.tsx) / [schedule-list.tsx](src/components/portal/schedule-list.tsx) / [schedule-past-simple.tsx](src/components/portal/schedule-past-simple.tsx)):\n- `sessionLogsByDate?: Record<string, SessionLogEntry[]>` に統一\n- SessionRow / SessionActionIcons の prop を `sessionLogsUrl: string | null` から `sessionLogs: SessionLogEntry[]` に変更\n- Logs 候補集約 (動画 logsUrl + sessionLogs) は配列を flat dedup する形に書換、auto / manual はラベルで区別表示\n- DateChip (簡易チップ) は `sessionLogs[0]?.url` で 1 件目だけを single-icon リンクとして surface (multi-URL 編集は popover に閉じる)\n\n**admin-actions.ts**: `initializeAllDataAction()` の DataInitCounts に `schedule_past_session_logs: number` を追加し、削除順 steps[] でも親 `schedule_past_sessions` の手前に挿入 (FK CASCADE 任せでも動くが件数別表示用)\n\n**運用** (案 B = PR 一括デプロイ): 本 PR のコード変更が main へ merge → Vercel 自動デプロイ完了直後に MCP `apply_migration` で本番 Supabase にスキーマ反映。デプロイ完了直後の **数十秒** だけ「新コードが旧スキーマを参照しに行く」窓ができるが、`fetchSessionLogsByDate` は try/catch で `{}` フォールバックなので Logs アイコン非表示で済み (`addSessionLogsUrl` を popover で踏まれた時のみ厳密にエラー)。低トラフィック単一テナントの運用許容範囲。`category_links.logs_url` / `logs_url_source` は今回スコープ外で従来通り (動画用)。\n\n**検証**: `tsc --noEmit` PASS。dev preview は本 commit 時点では本番スキーマが旧のままなので skip — 本番デプロイ + apply_migration 完了後にブラウザで複数 URL の追加 / 削除 / dropdown 選択を確認予定。",
      },
      {
        title: "⚡ TODO #55 クローズ — スケジュール TOP の Vercel Data Cache を `updateTag` 即時無効化方式で復活 (FCP 短縮)",
        body: "**狙い**: TODO #61 で `cache: \"no-store\"` 固定にした影響で character-sheets HTML scrape の TTFB が毎回 1〜3s かかり、本番 Speed Insights で `/` route の FCP が 2.86s (P75 Needs Improvement) に悪化していた。Vercel Data Cache の stale 問題を引き起こさずにキャッシュ命中率を取り戻すため、Next.js 16 の `updateTag` (Server Action 専用 read-your-own-writes API) で iframe edit dialog 閉じる時に明示無効化する仕組みを導入。\n\n**変更**:\n- [next-session.ts](src/lib/schedule/next-session.ts) `fetchHtmlOrNull` の fetch options を `cache: \"no-store\"` から `next: { revalidate: 60, tags: [SCHEDULE_CACHE_TAG] }` に変更。60s TTL で外部編集 (portal を介さない character-sheets 直接編集) の最大 lag を 1 分に抑える\n- 新 export `SCHEDULE_CACHE_TAG = \"schedule\"` を next-session.ts に追加\n- 新規 [schedule-cache-actions.ts](src/lib/server/schedule-cache-actions.ts) (`\"use server\"`) に `invalidateScheduleCache()` Action を追加、`updateTag(SCHEDULE_CACHE_TAG)` で read-your-own-writes 即時無効化\n- [schedule-edit-frame-dialog.tsx](src/components/portal/schedule-edit-frame-dialog.tsx) の onOpenChange close ハンドラで `invalidateScheduleCache().then(() => router.refresh())` を呼出。portal 経由の編集はキャッシュ即時無効化 + RSC 再 fetch で fresh HTML を取得\n\n**TODO #61 との整合**: TODO #61 は `revalidatePath(\"/\")` が fetch cache key 単位で外せないケースに遭遇したが、`updateTag` は tag-based で cache key を直接 invalidate するため stale 問題は再発しない。HANDOFF にも「将来 `revalidateTag` ベースで戻す余地を残す」と明記済の方針。\n\n**期待効果**: cache hit 時 TTFB 数百ms → ほぼ瞬時、FCP 大幅改善見込み。1 週間後の Speed Insights P75 で評価。\n\n**検証**: `tsc --noEmit` PASS。dev preview でスケジュールページ正常描画 + console エラーなしを確認。",
      },
      {
        title: "🎨 設定タブの更新履歴を 1 行サマリーのみ表示に簡略化 — part body 折りたたみ撤去",
        body: "**狙い**: 更新履歴が冗長で「ぱっと一覧したい」用途に不適だったため、各 release 内の part を `<details>` 折りたたみ + body から、title だけのフラットな箇条書きに変更。release 単位の `<details>` (version+date 行) は残し、最新 1 件 default open は維持。\n\n**変更**: [settings-dialog.tsx:1610](src/components/portal/settings-dialog.tsx) で part レンダリングを `<details><summary>title</summary><p>body</p></details>` から `<li>・{title}</li>` に置換。`r.parts.slice(0, 5)` の制限と `+N more` 表記は維持。`r.notes` (旧スキーム) のフラット箇条書きは元から `<ul>` 形式だったので無変更。\n\n**動作影響**: ユーザーは各 part の詳細 body を見られなくなるが、title に TODO 番号 + 1 行サマリーがあるので把握には十分。詳細は GitHub commits / changelog.ts source で参照可能 (フッターの「↗ これより前の履歴を GitHub で見る」リンク経由)。",
      },
      {
        title: "🎨 TODO #62 クローズ — schedule 凡例を character-sheets `/schedule/edit` の「日程オプション」から動的生成",
        body: "**狙い**: character-sheets の `/schedule/edit` ページで管理者が登録した「日程オプション」(デモでは `value=\"全昼夜\"`、運用カスタム記号のマスター) を portal 側の凡例 + セル描画に反映する。これまで `ATT_LEGEND` がハードコード 5 種 (◯⏰△×ー) 固定だったため、character-sheets で 全/昼/夜 のような独自選択肢を登録しても portal 凡例には出ず、TODO #60 で許容したカスタムラベルが「色は付くが凡例で説明されない」状態になっていた。\n\n**真因**: portal の凡例情報源が `/schedule/list` HTML (= 各セルの記号のみ) で、選択肢マスターが書かれている `/schedule/edit` HTML (`<input id=\"choiceValues\" value=\"全昼夜\">`) を一切 fetch していなかった。`/list` 側には凡例ブロックも choiceValues も無いので、list だけからではマスター復元不可能 (未投票の選択肢が欠落する)。\n\n**実装**:\n- [parse.ts](src/lib/schedule/parse.ts) に `ScheduleAttendanceOptions` 型 (`choices: string[]` + `source: \"edit-page\" | \"fallback-from-list\" | \"unavailable\"`) を新設、`ParsedSchedule.attendanceOptions` として export。`parseAttendanceChoicesFromEditHtml(editHtml)` で `<input id=\"choiceValues\" value=\"...\">` を抜き出し codepoint 単位 split (HTML 説明 \"選択肢を1〜4個...×は入力不要\" 通り 1 文字 1 選択肢)、× / ー は IGNORE。失敗時は sessions の attendance 集合から fallback、両方失敗で `unavailable`\n- [next-session.ts](src/lib/schedule/next-session.ts) `fetchScheduleRaw()` で list URL から `/schedule/edit?key=...` を派生し `Promise.all([fetchHtmlOrNull(list), fetchHtmlOrNull(edit)])` で並列 fetch。edit 失敗は致命的でない (parser 側で fallback) ので null 許容で受ける。両方 `cache: \"no-store\"` (TODO #61 と整合)\n- [schedule-list.tsx](src/components/portal/schedule-list.tsx) の `ATT_LEGEND` ハードコード配列を撤去 → `buildAttendanceLegend(choices)` 関数で動的生成 (choices 先頭 + `×` `－` 末尾固定、空なら従来 5 種フォールバック)。`ATT_LABEL_DICT` に portal 側ラベル辞書を新設 (`◯→参加可` ほか + カスタム `全→全日参加可` `昼→昼参加可` `夜→夜参加可` `早→早朝参加可`)、辞書外は記号のみ表示。`ATT_TONE` には全/昼/夜/早 を amber fallback トーンで明示マッピング (辞書外は `??` で同 fallback に落ちる)\n\n**ラベル方針**: edit 側には記号のみで日本語説明が無いため portal で持つ。マッピング外は説明 null で記号のみ凡例表示する (ユーザー要望)。表示順は edit の choiceValues 文字順 → 末尾に `×` `－` を追加で固定。\n\n**Cache 戦略**: list と同じく `cache: \"no-store\"` で毎リクエスト fresh fetch。TTFB +1〜3s 増だが TODO #61 で確立した方針と整合 (single-tenant 低トラフィック portal の運用許容範囲)。\n\n**検証**: `tsc --noEmit` PASS。デモ `/schedule/edit` HTML を curl で取得し parser ロジックを Node 単体実行 → `choiceValues=\"全昼夜\"` から `choices=[全,昼,夜]` を抽出 + 凡例 5 件 (`全→全日参加可 / 昼→昼参加可 / 夜→夜参加可 / ×→不可 / －→未回答`) 構築を実証。portal 上の本番固定 (標準 5 種) でも従来表示と同等の凡例 + regression なしを確認 (画面 screenshot)。\n\n**動作影響**: `/schedule/edit` の choiceValues に登録された記号が凡例先頭に並び、辞書登録済 (全/昼/夜/早) なら日本語ラベル併記、未登録カスタムは記号のみ表示。標準 5 種 (◯⏰△×ー) のみ運用なら従来表示と完全一致。`/edit` fetch 失敗時は sessions の出欠集合から復元 → それも失敗 (= 完全に取れない) なら標準 5 種にフォールバック。",
      },
      {
        title: "🐛 TODO #61 クローズ — DECISION 行が CANDIDATE 描画される + ルールに「日程状況一覧」混入を解消",
        body: "**狙い**: TODO #60 修正後の demo サイト確認で発見した 2 症状を解決。(1) character-sheets で `value=\"DECISION\"` を入れた 5/02・5/17・5/30 が portal 上で全て CANDIDATE (`·`) 描画 + 次回開催カードも「未確定」表示で連動、(2) ルール popover (運用ルール / 注意事項) の冒頭に「日程状況一覧」という character-sheets 側の見出しラベルが混入。\n\n**真因**:\n- (1) **Vercel Data Cache stale**: [next-session.ts](src/lib/schedule/next-session.ts) の `fetch({ next: { revalidate: 1800 } })` で 30 分 TTL の Data Cache を使っていたが、character-sheets で DECISION マークを更新しても Vercel 側の cache key 単位では invalidate されず古い CANDIDATE 一色 HTML が居座り続ける症状。`revalidatePath(\"/\")` を mutate 経路で呼んでも fetch cache が消えないケースが Edge runtime + Next.js 16 の組み合わせで再現。HANDOFF 起票時 (TODO #61) の「fresh deploy 後も再現」も同根 (= 再 deploy では Data Cache は消えない、別ストレージに persist)\n- (2) **parseTopText の regex 漏れ**: [parse.ts](src/lib/schedule/parse.ts) `parseTopText()` が `<table>` 直前の `<p|pre|blockquote|h2|h3|h4>` ブロックを拾うヒューリスティック。character-sheets が table 直前に「日程状況一覧」というラベル見出しを `<h2>`/`<h3>` 等で出していると `■コメント` 切り捨てより前の段階で混入。最近の character-sheets テンプレ変更で見出しが追加された可能性\n\n**切り分け方法**:\n- temp `console.warn(\"[parse-debug]\", ...)` を `parseSessions()` 行頭に投入して Vercel Runtime Logs で実 statusRaw を観測 → 5/02 の statusRaw=`CANDIDATE` (length=9, きれいな ASCII) と判明 → regex 不一致 (a) / 後段上書き (b) は排除、HTML 自体が違う (c) で確定\n- `cache: \"no-store\"` に切替えて再 deploy → `[parse-summary]` log で `decisionCount: 0 → 3` に変化 + ブラウザ確認で 5/02・5/17・5/30 全件で確定 chip 描画 + 「次回開催 = 2026/05/02 確定」表示を確認 → c-1 (Cache stale) 確定\n\n**修正**:\n- [next-session.ts](src/lib/schedule/next-session.ts) `fetchScheduleRaw()` の fetch を `next: { revalidate: 1800 }` から `cache: \"no-store\"` に変更。毎リクエスト fresh fetch (1〜3s TTFB) を許容する代わりに stale 問題を根絶。single-tenant 低トラフィック portal では運用上素直な選択\n- [parse.ts](src/lib/schedule/parse.ts) `CHARSHEETS_LABEL_NOISE` Set を新設 (現状「日程状況一覧」のみ)、`parseTopText` の block ループ内で完全一致時のみ skip。同表現を含むユーザー本文 (例: 「〜の日程状況一覧について」) は誤って消さない\n\n**TTFB トレードオフ**: TODO #55 で `revalidate: 600 → 1800` に伸ばして cache 命中率を稼いでいた経緯と相反するが、stale CANDIDATE で portal が機能しないより毎回 fresh が優先と判断。将来的に `revalidateTag` ベースで iframe edit 完了時 / admin mutate 時に明示 invalidate する形 (option C) に戻す余地は残す。\n\n**動作影響**: スケジュール TOP の TTFB が +1〜3s (cache miss 相当)。代わりに character-sheets 側の DECISION / CANDIDATE / 出欠記号変更が即時反映される。ルール popover に「日程状況一覧」の余分な行が出なくなる (運用ルール / 注意事項のユーザー記述のみ表示)。\n\n**検証**: `tsc --noEmit` PASS。Claude in Chrome で demo サイトを直接ブラウザ確認、5/02・5/17・5/30 全件で「確定」 chip 描画 + 次回開催カード = 2026/05/02 + 確定 chip + ルール popover に「日程状況一覧」が表示されないことを確認 (commit 直前)。",
      },
    ],
  },
];
