# TODO #51 — マイクロインタラクション / ユーザビリティ向上

> 観点リスト + 優先順位 (2026-06-10 作成、コードベース横断調査に基づく)。
> 性能数値の最適化は TODO #11 (休眠中) のスコープ、本 TODO は **操作の体感品質** を portal 全体で 1 周 polish する。
> framer-motion は維持方針 (springy な質感)。1 phase = 1 PR、changelog.ts 追記同梱 (memory ルール)。

## ⚠ 制約 (先行 TODO の教訓、違反禁止)

- **popover / tooltip / dropdown-menu への close transition 再追加禁止**: `popover.tsx` / `tooltip.tsx` / `dropdown-menu.tsx` の `data-closed:animate-out` / `data-closed:fade-out-0` / `data-closed:zoom-out-95` 系列は visual rebound + 本番 DOM 残留 (白枠) 再発のため再追加禁止 (TODO #65 part8 / #70〜#72 の経緯)。フェードアウトを入れたい場合は `duration-50` + opacity-only 版 (zoom-out なし) のみ可
- **初期 bundle を太らせない**: TODO #11/#67 で削った client bundle を polish で逆戻りさせない。skeleton は CSS-only、illustration 系は lucide icon 流用か `next/dynamic`、新規アニメライブラリ追加は不可 (framer-motion 既存分で賄う)
- **popover 系の新規実装は TODO #72 パターン必須**: `<Popover open={open} onOpenChange={setOpen}>` controlled + `{open && <PopoverContent finalFocus={false}>}` unmount

## 観点リスト (優先順位付き)

### P1 — 効果大 / リスク小 (最初の 1 PR 候補)

| # | 観点 | 現状 | やること |
|---|---|---|---|
| 1 | **toast 設定の明示化** | `sonner.tsx` は icons + theme 連動のみ実装、position / duration / richColors は sonner default のまま。メッセージ文体も呼び出し側でばらつき | Toaster に position / duration を明示設定 (現運用の体感に合わせて決定)。success / error メッセージの文体ガイドライン (「〜しました」「〜に失敗しました: 理由」) を決めて主要呼び出し箇所を統一 |
| 2 | **press feedback の統一** | `button.tsx` の `active:not-aria-[haspopup]:translate-y-px` のみ。icon button 単独実装・クリック可能行・リンクカード・DnD ハンドルには press 反応なし | 共通の press トークン (`active:scale-[0.98]` or translate-y-px) を決め、クリック可能要素へ横展開。スマホ (TODO #7 関連) では hover が効かないため press が主要フィードバックになる点を重視 |
| 3 | **focus-visible 欠落の補修** | primitive (button / input / textarea) は `focus-visible:ring-3 ring-ring/50` で統一済。custom popover trigger (`schedule-list.tsx` 等) で欠落、`native-attendance-popover.tsx` は `ring-2` の独自定義 | primitive と同じ ring 定義に揃える。キーボード Tab 巡回で「いまどこにいるか見えない」要素をゼロにする |

### P2 — 効果大 / 設計が必要

| # | 観点 | 現状 | やること |
|---|---|---|---|
| 4 | **保存系 button の pending 表示** | server action 発火 button に spinner / disabled の共通パターンなし (`useTransition` isPending 未活用)。連打防止も個別実装頼み | `isPending` 中は spinner (`Loader2` 回転) + disabled にする共通パターン (or `<SubmitButton>` 抽出) を設計し、settings dialog / 各 form dialog の保存 button へ展開 |
| 5 | **skeleton (CSS-only)** | schedule top は `ScheduleLoadingFallback` (spinner + 遅延 fade-in) 済。`/category/[slug]` 配下の sub-page には Suspense fallback なし | sub-page 用の CSS-only skeleton (pulse する placeholder 行) を追加。bundle 影響ゼロの server component で |
| 6 | **フォーム即時 validation** | validation は submit 時のみ (inline error box)。`input.tsx` に `aria-invalid:border-destructive` 定義済みだが**呼び出し側が aria-invalid を設定しておらず未活用** | URL 入力等で onBlur validation + `aria-invalid` 設定。エラー表示は inline (toast との使い分け: 入力起因 = inline、サーバ起因 = toast) |
| 7 | **空状態の component 化** | 「コンテンツがまだ登録されていません」等テキストのみ、箇所ごとに個別実装 | `<EmptyState icon= message= action= >` 共通 component 化。illustration はまず lucide icon + muted 色で (画像 asset 追加はしない) |

### P3 — 質感向上 / 慎重に (P1/P2 完了後)

| # | 観点 | 現状 | やること |
|---|---|---|---|
| 8 | **hover elevation の統一** | icon button (`hover:scale-110` + neon shadow) は良好。card 系 (`card.tsx` hover なし) / リンクカード / menu item は弱いか無し | card / リンクカードに subtle elevation (`hover:shadow` + border 色昇華) を統一適用。neon shadow のテーマ色運用 (violet/cyan) は現行踏襲 |
| 9 | **transition duration / easing の token 化** | `duration-100/150/200/300` が混在、easing 未指定が大多数 | 用途別 token (micro: 150 / panel: 200 等) を決めて寄せる。一括置換ではなく観点 8 とセットで触った箇所から統一 |
| 10 | **framer-motion 活用拡大** | tab 下線の `layoutId` spring のみ (category-switcher / main-tabs / sub-tabs) | リスト並び替え時の layout アニメ等、**open/close transition 以外** (上記制約) で springy な質感を足せる箇所を選定。AnimatePresence による page transition は SSR/Suspense 相性リスクがあり原則やらない |

## 進め方

1. P1 (#1〜#3) を 1 PR で着手 → 本番実機で体感確認
2. P2 は観点ごとに 1 PR (規模次第で #6+#7 合流可)
3. P3 は P1/P2 で得た体感を踏まえて要否再判断 (やらない判断もあり)
4. 各 PR で `npx tsc --noEmit` + `npm run build` + changelog.ts 追記

## 調査メモ (2026-06-10 時点の現状根拠)

- press: `src/components/ui/button.tsx` L7 / `tabs.tsx` L61-64 のみ
- toast: `src/components/ui/sonner.tsx` (position/duration/richColors 未設定を実読で確認)
- loading: `src/app/(portal)/page.tsx` L80-102 `ScheduleLoadingFallback` + `globals.css` `scheduleLoadingFadeIn`
- focus: `globals.css` L405 `outline-ring/50` global + primitive 単位の `focus-visible:ring-3`
- validation: `src/components/portal/link-form-dialog.tsx` L123-200 (submit 時 URL regex + inline error)
- empty: `src/components/portal/category-switcher.tsx` L139-142 ほかテキストのみ
- framer-motion: `category-switcher.tsx` / `main-tabs.tsx` / `sub-tabs.tsx` の tab 下線 layoutId のみ
